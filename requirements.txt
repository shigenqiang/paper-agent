# 依赖定义已统一到 pyproject.toml
# 安装方式: pip install -e .
# 或: pip install -e ".[api]"  (含 FastAPI)

# === 核心依赖 ===
pydantic>=2.5
langchain>=0.3
langchain-openai>=0.3
openai>=1.0
httpx>=0.27
aiohttp>=3.8
requests>=2.28
pdfplumber>=0.10
arxiv>=1.4
biopython>=1.81
pyyaml>=6.0
python-dotenv>=1.0
orjson>=3.8
loguru>=0.7

# === API (可选) ===
# fastapi>=0.110
# uvicorn>=0.27

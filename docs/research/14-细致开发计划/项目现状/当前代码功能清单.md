# 当前代码功能清单

更新时间：2026-06-05（全链路 E2E 测试 + 报告读写修复 + ScopeQA 增强）

本文档记录 `src/agents_v3/research_workspace/` 当前代码已实现的全部功能。

代码规模：约 16,000 行（重组后分布在 8 个子包中）。测试规模：30 个测试文件，约 5,286 行。

---

## 1. 项目整体架构

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                            FastAPI  API 层                                  │
│              api/app.py + api/routes/*（7 个路由模块，28 端点）              │
│         项目 / 论文 / 解析 / 卡片 / 证据 / 图谱 / Scope / QA / 报告          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ ProjectService│  │PaperLibrary  │  │  ParserService│  │CardGenerator │    │
│  │   项目管理    │  │  论文库管理   │  │  PDF 解析     │  │  卡片生成     │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │                 │             │
│  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴───────┐    │
│  │              │  │SearchOrch    │  │TextPostProc  │  │ContextSelect │    │
│  │              │  │ 搜索编排      │  │ 文本后处理    │  │ 上下文选择    │    │
│  │              │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│  │              │         │                 │                 │             │
│  │              │  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴───────┐    │
│  │              │  │ 4 Adapters   │  │5 PDF Parsers │  │LLM + Fallback│    │
│  │              │  │ openalex/    │  │pymupdf4llm/  │  │ 提取 + 校验   │    │
│  │              │  │ arxiv/s2/epmc│  │plumber/fitz/ │  │              │    │
│  │              │  └──────────────┘  └──────────────┘  └──────────────┘    │
│  │              │                                                          │
│  ├──────────────┴──────────────────────────────────────────────────────┐    │
│  │                    EvidenceTableService                             │    │
│  │                    证据表：卡片 → 结构化证据                          │    │
│  ├─────────────────────────────────────────────────────────────────────┤    │
│  │                    GraphService                                     │    │
│  │                    知识图谱：实体/关系/gap 检测                       │    │
│  ├─────────────────────────────────────────────────────────────────────┤    │
│  │                    RetrievalScopeService                            │    │
│  │                    范围解析：6 种 scope 类型 + ScopeGuard             │    │
│  ├──────────────┬──────────────────────┬───────────────────────────────┤    │
│  │ ScopeQAService│ LiteratureReview    │ InnovationReportGenerator     │    │
│  │  范围问答     │  综述生成            │  创新点报告                    │    │
│  ├──────────────┴──────────────────────┴───────────────────────────────┤    │
│  │                    HierarchicalRetriever                            │    │
│  │              L0→L1→L2 渐进式向量检索 + 回退策略                      │    │
│  └──────┬───────┘──────────────┬──────┴──────────────────┬────────────┘    │
│         │                     │                         │                 │
│  ┌──────┴─────────────────────┴─────────────────────────┴──────────────┐    │
│  │                         ReportService                               │    │
│  │              报告 CRUD / 版本 / 来源索引 / 校验 / 导出                │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                          LLM 服务层                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  LLMService   │  │  json_utils  │  │  prompts     │  │  logging     │    │
│  │  invoke/json  │  │  JSON 提取    │  │  Prompt注册   │  │  脱敏/日志   │    │
│  │  /structured  │  │  + 修复       │  │  版本管理     │  │              │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│                          评估与监控层                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  evaluator    │  │  gates       │  │  golden      │  │  metrics     │    │
│  │  8 项检查     │  │  6 道门禁    │  │  测试用例     │  │  指标收集     │    │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│                          存储层                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                       │
│  │PostgresStorage│  │VectorStorage │  │EmbeddingProv │                       │
│  │ PostgreSQL   │  │ Qdrant L0/L1/L2│ │ local/cloud │                       │
│  └──────────────┘  │ + where 过滤  │  │ 统一切换     │                       │
│                     └──────────────┘  └──────────────┘                       │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 数据流主链路

```text
用户上传/搜索论文
       │
       ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ PaperLibrary │───▶│ ParserService│───▶│CardGenerator│
│ 论文入库      │    │ PDF 解析     │    │ 卡片生成     │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                             │
       ┌─────────────────────────────────────┘
       ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│EvidenceTable │───▶│ GraphService │───▶│  Scope 解析  │
│ 证据表       │    │ 知识图谱     │    │ 范围选择     │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                             │
       ┌─────────────────────────────────────┘
       ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  ScopeQA    │    │ 综述生成     │    │ 创新点报告   │
│  范围问答    │    │             │    │             │
└──────┬──────┘    └──────┬──────┘    └──────┬──────┘
       │                  │                  │
       └──────────────────┴──────────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │ReportService │
                   │ 版本/导出    │
                   └─────────────┘
```

### 数据关联路径（paper_id 三表联动）

```text
┌──────────────────────────────────────────────────────────────────┐
│  PostgreSQL papers 表                                            │
│  paper_id（主键）← 所有下游引用的锚点                            │
│  title / abstract / authors / year / venue / doi / source_platform│
└──────────────┬───────────────────────────────────────────────────┘
│ paper_id
       ┌───────┼───────┐
       ▼       ▼       ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Qdrant   │ │ Qdrant   │ │ Qdrant   │
│ L0       │ │ L1       │ │ L2       │
│paper_prof│ │paper_sec │ │paper_chk │
│iles      │ │tions     │ │unks      │
│          │ │          │ │          │
│dense+sp  │ │dense+sp  │ │dense+sp  │
│论文级向量 │ │章节级向量 │ │chunk级向量│
│title+abs │ │summary   │ │child only│
│          │ │          │ │          │
│metadata: │ │metadata: │ │metadata: │
│ paper_id │ │ paper_id │ │ paper_id │
│ project_id│ │ section_ │ │ section_id│
│ title    │ │ type     │ │ section_ │
│          │ │ section_ │ │ type     │
│          │ │ title    │ │ chunk_   │
│          │ │ project_id│ │ type     │
│          │ │          │ │ parent_id│
│          │ │          │ │ page_*   │
│          │ │          │ │ token_   │
│          │ │          │ │ count    │
└──────────┘ └──────────┘ └──────────┘

embed_paper() 三写：
  ① paper_chunks：每个 child chunk 一个向量点（L2）
  ② paper_profiles：每篇论文一个向量点 title+abstract（L0）
  ③ paper_sections：每个有 summary 的章节一个向量点（L1）

HierarchicalRetriever 渐进式检索：
  L0(project_id filter) → L1(paper_id filter) → L2(section_id filter)
  回退：L0 为空 → flat L2；L1 为空 → L2 with paper_ids

EmbeddingProvider 统一接口（config.yaml embedding.mode 切换）：
  cloud 模式：CloudEmbeddingProvider → Qdrant Cloud inference API（临时集合提取向量）
  local 模式：LocalEmbeddingProvider → EmbeddingService（sentence-transformers + TF-IDF）
  所有组件（HybridRanker/ParserService/HierarchicalRetriever/PaperLibraryService）统一调用
```

---

## 代码结构：文件职责与依赖关系

### 目录总览

```text
src/agents_v3/research_workspace/
├── __init__.py                  # 包入口，re-export 所有公共 API（facade）
├── config.py                    # YAML 配置加载（search 排序参数）
├── utils.py                     # 公共工具：normalize_label, stable_hash, hash_text, redact_text
│
├── models/                      # 数据模型子包（从 models.py 拆分）
│   ├── __init__.py              # re-export 全部模型
│   ├── enums.py                 # 8 个枚举：PaperStatus, ChunkType, SectionType 等
│   ├── paper.py                 # Paper, PaperChunk, ParseResult 等
│   ├── paper_profile.py         # PaperProfile, PaperSection, Reference, CitationContext
│   ├── knowledge_graph.py       # GraphNode/Edge, KGNode* 系列
│   └── reports.py               # Project, Report, QA*, RetrievalResult, EvidenceRecord 等
│
├── storage/                     # 存储子包（从 6 个根文件归组）
│   ├── __init__.py              # re-export 全部
│   ├── backend.py               # StorageBackend Protocol 定义
│   ├── postgres.py              # PostgresStorage：PostgreSQL 持久化
│   ├── vector.py                # VectorStorage：Qdrant 向量存储（含 search_hybrid）
│   ├── qdrant.py                # Qdrant 集合初始化（5 个集合定义）
│   ├── embedding.py             # EmbeddingService：dense + sparse 编码（本地模式）
│   └── embedding_provider.py    # EmbeddingProvider 统一接口：local/cloud 切换
│
├── parser/                      # 解析子包（从 parser_service.py 拆分）
│   ├── __init__.py              # re-export 全部
│   ├── adapters.py              # 5 个解析器适配器
│   ├── postprocess.py           # TextPostProcessor, ChunkCleaner/Dedup/Scorer/Filter
│   ├── service.py               # ParserService（含 embed_paper 三写 L0+L1+L2）
│   └── section_extractor.py     # 论文章节级 LLM 结构化提取
│
├── services/                    # 业务服务子包（11 个服务）
│   ├── __init__.py              # re-export 全部
│   ├── project_service.py       # 项目 CRUD + 统计
│   ├── paper_library.py         # 论文库：搜索/导入/去重/排序/提交
│   ├── paper_card.py            # 论文卡片生成（LLM 提取 + 质量校验）
│   ├── evidence_table.py        # 证据表：卡片 → 结构化证据记录
│   ├── graph_service.py         # 知识图谱：实体/关系/gap 检测
│   ├── graph_extractor.py       # LLM 实体/关系提取（从 paper_sections 全文）
│   ├── scope.py                 # RetrievalScope 解析（6 种 scope 类型）
│   ├── scope_qa.py              # 基于 Scope 的问答服务
│   ├── review_generator.py      # 文献综述生成器
│   ├── innovation_generator.py  # 创新点报告生成器
│   ├── report_service.py        # 报告 CRUD / 版本 / 来源索引 / 导出
│   └── hierarchical_retriever.py # HierarchicalRetriever：L0→L1→L2 渐进式检索
│
├── api/                         # FastAPI API 子包（重组后路由拆分）
│   ├── __init__.py
│   ├── app.py                   # FastAPI 工厂 + lifespan（~100 行）
│   ├── deps.py                  # 依赖注入（14 个 get_* 函数）
│   ├── models.py                # API 请求/响应模型
│   ├── errors.py                # API 错误处理（5 种错误类型）
│   ├── tasks.py                 # TaskService（后台任务管理）
│   └── routes/                  # 路由模块（7 个）
│       ├── __init__.py          # register_routers(app)
│       ├── projects.py          # 项目 CRUD 路由
│       ├── papers.py            # 论文库路由
│       ├── parsing.py           # PDF 解析路由
│       ├── knowledge.py         # 卡片/证据/图谱路由
│       ├── scope_qa.py          # Scope + QA 路由
│       ├── reports.py           # 报告生成/导出路由
│       └── tasks.py             # 任务查询路由
│
├── search/                      # 搜索子包（不变）
│   ├── __init__.py
│   ├── base.py                  # SearchQuery/SearchResult/SearchResponse 模型
│   ├── openalex_client.py       # OpenAlex API 适配器
│   ├── semantic_scholar_client.py # Semantic Scholar API 适配器
│   ├── arxiv_client.py          # arXiv API 适配器
│   ├── europepmc_client.py      # Europe PMC API 适配器
│   ├── factory.py               # 适配器工厂
│   ├── orchestrator.py          # SearchOrchestrator
│   ├── merger.py                # SearchResultMerger（多源去重融合）
│   ├── dedup.py                 # 去重服务（DOI/ID/Jaccard）
│   ├── hybrid_ranker.py         # HybridRanker（dense+sparse RRF 融合）
│   ├── quality_filter.py        # 质量过滤（引用+时效+来源）
│   ├── query_optimizer.py       # 查询优化（LLM 提取学术关键词）
│   ├── sparse_encoder.py        # TF-IDF 稀疏编码器
│   ├── rate_limit.py            # RateManager 限流控制
│   ├── strategies.py            # SearchStrategy 枚举
│   └── errors.py                # 搜索错误类型
│
├── llm/                         # LLM 服务子包（不变）
│   ├── __init__.py
│   ├── service.py               # LLMService（invoke/invoke_json/structured）
│   ├── prompts.py               # PromptRegistry（版本化提示词管理）
│   ├── json_utils.py            # JSON 提取 + LLM 修复
│   ├── errors.py                # LLM 错误类型（7 种）
│   └── logging.py               # LLM 调用日志（从 utils 导入 hash_text/redact_text）
│
└── evaluation/                  # 评估子包（不变）
    ├── __init__.py
    ├── evaluator.py             # 8 项检查函数
    ├── gates.py                 # 6 道质量门禁
    ├── golden.py                # 4 个 golden test cases
    ├── metrics.py               # MetricsCollector 指标收集
    └── logging_utils.py         # 从 utils 导入 hash_text/redact_text
```

### 文件依赖关系（核心链路）

```text
                    ┌─────────────────────────────────────────────────────────────────┐
                    │                         API 层                                   │
                    │  api/app.py → api/deps.py → api/routes/*（7 个路由模块）         │
                    └───────────────────────────┬─────────────────────────────────────┘
                                                │ 依赖注入
                    ┌───────────────────────────▼─────────────────────────────────────┐
                    │                   services/ 业务服务层                            │
                    │                                                                 │
                    │  project_service.py ──┐                                         │
                    │  paper_library.py ────┤                                         │
                    │  paper_card.py ───────┤                                         │
                    │  evidence_table.py ───┤                                         │
                    │  graph_service.py ────┤                                         │
                    │  scope.py ────────────┤                                         │
                    │  scope_qa.py ─────────┤                                         │
                    │  review_generator.py ─┤                                         │
                    │  innovation_generator.py ┤                                      │
                    │  report_service.py ───┤                                         │
                    │  hierarchical_retriever.py ┘                                    │
                    │       │                                                         │
                    │       │ 全部依赖                                                 │
                    │       ▼                                                         │
                    │  parser/（PDF 解析 + 嵌入）                                      │
                    │  models/（数据契约）                                             │
                    │  storage/（PG + Qdrant + Embedding）                            │
                    │  llm/（LLM 调用）                                               │
                    │  search/*（搜索能力）                                            │
                    │  utils.py（normalize_label, hash_text 等公共工具）              │
                    └─────────────────────────────────────────────────────────────────┘
```

### 各文件职责速查表

| 文件 | 行数 | 职责 | 上游调用方 | 下游依赖 |
|------|------|------|-----------|----------|
| **models/\*.py** | 774 | 30+ Pydantic 数据模型（拆分为 5 文件） | 所有模块 | pydantic |
| **config.py** | 44 | YAML 配置加载 | search/hybrid_ranker.py | yaml |
| **utils.py** | ~70 | normalize_label, stable_hash, hash_text, redact_text | services/*, llm, evaluation | hashlib/re |
| **storage/backend.py** | 34 | StorageBackend Protocol | - | - |
| **storage/postgres.py** | 800+ | PostgreSQL 持久化（17 张表）+ 16 个 JSONStorage 兼容方法（upsert_item/get_item/load_collection/save_to_folder 等，全代码库统一调用入口） | 所有业务模块 | psycopg2 |
| **storage/vector.py** | 830 | Qdrant 向量存储（5 集合，search_hybrid 含 where 过滤） | paper_library, parser, hierarchical_retriever | qdrant_client |
| **storage/embedding.py** | 113 | dense + sparse 向量编码（本地模式） | embedding_provider | sentence_transformers |
| **storage/embedding_provider.py** | 405 | EmbeddingProvider 统一接口：local/cloud 切换 | hybrid_ranker, parser, hierarchical_retriever, paper_library | qdrant_client, embedding |
| **storage/qdrant.py** | 161 | Qdrant 集合初始化 | 启动脚本 | qdrant_client |
| **services/project_service.py** | 192 | 项目 CRUD + 统计 | api/deps.py | storage/ |
| **services/paper_library.py** | 928 | 论文搜索/导入/去重/排序 | api/deps.py | search/*, storage/, embedding |
| **parser/service.py** | 1500+ | PDF 解析 + 分块（含 embed_paper 三写 L0+L1+L2） | api/deps.py | storage/, models/ |
| **parser/adapters.py** | ~270 | 5 个 PDF 解析器适配器 | parser/service | - |
| **parser/postprocess.py** | ~630 | TextPostProcessor, ChunkCleaner/Dedup/Scorer/Filter | parser/service | utils |
| **parser/section_extractor.py** | 484 | 章节级 LLM 结构化提取 | parser/service | llm/service, storage/, models/ |
| **services/paper_card.py** | 650 | 论文卡片生成 | api/deps.py | llm/service, storage/, models/ |
| **services/evidence_table.py** | 160 | 证据表：卡片→证据 | api/deps.py | storage/, models/ |
| **services/graph_service.py** | 900+ | 知识图谱：实体/关系/gap | api/deps.py | storage/, models/, utils |
| **services/graph_extractor.py** | 200+ | LLM 实体/关系提取 | parser/service.py | llm/, storage/ |
| **services/scope.py** | 700+ | RetrievalScope 解析 | api/deps.py | storage/, models/, utils |
| **services/scope_qa.py** | 575+ | 基于 Scope 的问答 | api/deps.py | llm/service, scope, storage/, models/ |
| **services/review_generator.py** | 700+ | 文献综述生成 | api/deps.py | llm/service, scope, storage/, models/ |
| **services/innovation_generator.py** | 800+ | 创新点报告生成 | api/deps.py | llm/service, scope, storage/, models/, utils |
| **services/report_service.py** | 450+ | 报告 CRUD/版本/导出 | api/deps.py | storage/, models/ |
| **services/hierarchical_retriever.py** | ~300 | L0→L1→L2 渐进式检索 | scope_qa, api | storage/vector, storage/embedding |

### 子包职责速查表

| 子包 | 文件数 | 职责 | 核心入口 |
|------|--------|------|---------|
| **models/** | 5 | 30+ Pydantic 数据模型（enums/paper/profile/graph/reports） | __init__.py re-export |
| **storage/** | 6 | PostgreSQL + Qdrant + EmbeddingProvider 统一 embedding | get_storage, get_vector_storage, get_embedding_provider |
| **parser/** | 4 | PDF 解析 + 分块 + 后处理 + 章节提取 + 三写嵌入 | ParserService |
| **services/** | 11 | 业务服务（项目/论文/卡片/证据/图谱/QA/报告/检索） | 各 Service 类 |
| **api/** | 10 | FastAPI 工厂 + 路由（7 模块）+ 依赖注入 + 错误处理 | app.py, routes/ |
| **search/** | 21 | 多源论文搜索 + 去重 + 排序 + 质量过滤 | SearchOrchestrator |
| **llm/** | 6 | LLM 调用 + JSON 提取 + 提示词管理 | LLMService |
| **evaluation/** | 5 | 质量检查 + 门禁 + 指标 + 日志 | evaluator.py, gates.py |

### 存储后端职责分工

```text
┌─────────────────────────────────────────────────────────────────────────┐
│  PostgresStorage（唯一存储后端）                                        │
│  - 原生方法：upsert / get / query / delete / list_all / count / close  │
│  - 兼容层（16 方法）：upsert_item / get_item / delete_item             │
│    load_collection / save_collection / save_to_folder / load_from_folder│
│    list_folder / delete_from_folder / load/save_project_metadata       │
│    ensure_dirs — 全代码库统一调用入口（原 JSONStorage 接口）            │
│  - 17 张表：projects / papers_pool / paper_chunks / paper_cards        │
│    evidence_records / parse_results / graphs / reports / report_versions│
│    tasks / qa_history / queries / topic_scores                         │
│    paper_references / kg_nodes / kg_edges / topic_scores               │
│    citation_contexts / paper_sections                                  │
│  - JSONB 存储复杂字段，级联删除                                         │
│  - 项目名 UNIQUE 约束                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  VectorStorage（Qdrant）                                               │
│  - 5 个集合：                                                          │
│    paper_chunks：chunk 级 dense+sparse 向量（child chunks）            │
│    paper_profiles：论文级 dense+sparse 向量（title+abstract）          │
│    search_queries：查询向量（历史查询匹配）                             │
│    citation_contexts：引用上下文向量（立场分析）                         │
│    paper_sections：章节级向量（summary embedding）                      │
│  - 混合检索：RRF / DBSF 融合 dense + sparse                           │
│  - 云端推理：Document 自动向量（通过 EmbeddingProvider）                │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 01-核心模型与存储

### 在系统中的角色

核心模型与存储是整个系统的数据地基。所有业务模块都依赖它来定义数据结构和持久化。它不包含业务逻辑，只提供数据契约和存储接口。

### 模块架构

#### models.py（739 行）— 数据契约层

```text
┌──────────────────────────────────────────────────────────────────────────┐
│  8 枚举                                                                  │
│  PaperStatus(7) / ChunkType(13) / SectionType(14) / ReportType(2)       │
│  ScopeType(6) / NodeType(14) / EdgeType(24)                             │
├──────────────────────────────────────────────────────────────────────────┤
│  核心模型（30+ Pydantic BaseModel）                                      │
│                                                                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐          │
│  │ Project          │  │ Paper            │  │ PaperChunk       │          │
│  │ id/name/desc     │  │ id/title/abstract│  │ id/section/text  │          │
│  │ discipline/goal  │  │ +7 子模型嵌套     │  │ page/token_count │          │
│  └─────────────────┘  │ Author/Identifiers│  │ quality_flags    │          │
│                        │ Dates/Source/OA   │  └─────────────────┘          │
│                        │ Classify/Citation │                               │
│                        └─────────────────┘  ┌─────────────────┐          │
│  ┌─────────────────┐  ┌─────────────────┐  │ PaperCard        │          │
│  │ PaperProfile(L1) │  │ PaperSection(L2) │  │ question/method  │          │
│  │ 30+ 字段         │  │ summary(~200tok) │  │ findings/limits  │          │
│  │ 背景/动机/贡献    │  │ claims/entities  │  │ source_spans     │          │
│  │ 方法/发现/局限    │  │ figures/tables   │  │ quality_score    │          │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘          │
│                                                                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐          │
│  │ EvidenceRecord   │  │ KnowledgeGraph   │  │ RetrievalScope   │          │
│  │ topic/finding    │  │ nodes[]/edges[]  │  │ 6 种 scope_type │          │
│  │ limitation/quote │  │ GraphNode/Edge   │  │ paper/evidence/  │          │
│  └─────────────────┘  └─────────────────┘  │ graph_node IDs   │          │
│                                              └─────────────────┘          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐          │
│  │ QAResponse       │  │ InnovationPoint  │  │ Report/Version   │          │
│  │ answer/intent    │  │ 5 维评分          │  │ CRUD/版本/导出   │          │
│  │ evidence/paths   │  │ gap/evidence     │  │ section_sources  │          │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘          │
├──────────────────────────────────────────────────────────────────────────┤
│  12 类型化 KG 节点（KGNodeBase + Literal node_type + 域字段）             │
│  PaperNode / TopicNode / MethodNode / FindingNode / LimitationNode      │
│  GapNode / AuthorNode / VenueNode / InnovationNode / TaskNode           │
│  MaterialNode / MetricNode                                              │
└──────────────────────────────────────────────────────────────────────────┘
```

#### 存储后端

```text
┌─── PostgresStorage（800+ 行）──┐  ┌── VectorStorage（830 行）──┐
│  17 张表（TABLE_SCHEMAS）      │  │  Qdrant 客户端              │
│  projects/papers/paper_chunks  │  │  add_documents/chunks       │
│  paper_cards/evidence_records  │  │  add_paper_profiles（L0）   │
│  parse_results/graphs/reports  │  │  add_paper_sections（L1）   │
│  report_versions/tasks         │  │  query_by_embedding         │
│  qa_history/topic_scores       │  │  search_hybrid（含 where）  │
│  paper_references/kg_nodes/edges│  │  search_hybrid_by_text      │
│  topic_scores/paper_sections   │  │  search_chunks              │
│  queries/citation_contexts     │  │  delete_by_paper/papers     │
│  JSONB 存储 + 10 列迁移       │  │  UUID5 确定性 ID            │
│  级联删除（9 张子表）          │  │  dense+sparse 混合检索       │
│  项目名 UNIQUE 约束            │  │  where 过滤（project/section）│
│  ── 兼容层（16 方法）──        │  │  云端推理（Document 自动向量）│
│  upsert_item / get_item        │  └─────────────────────────────┘
│  delete_item / load_collection │
│  save_collection / list_folder │  ┌── EmbeddingProvider（405 行）──┐
│  save_to_folder / load_from_*  │  │  统一接口：local/cloud 切换    │
│  load/save_project_metadata    │  │  embed_texts / embed_query    │
│  ensure_dirs（空操作）         │  │  fit_sparse / embed_*_sparse  │
└────────────────────────────────┘  embed_*_hybrid               │
                                    └─────────────────────────────┘
                                    ┌── EmbeddingProvider（405 行）──┐
┌── StorageBackend Protocol（33 行）┐│  统一接口：local/cloud 切换    │
│  upsert / get / query / delete   ││  embed_texts / embed_query    │
│  list_all / count / close        ││  fit_sparse / embed_*_sparse  │
│  + 16 兼容方法                    │  embed_*_hybrid               │
└──────────────────────────────────┘                               │
                                    │  LocalEmbeddingProvider       │
                                    │  └─ EmbeddingService 包装     │
                                    │  CloudEmbeddingProvider       │
                                    │  └─ Qdrant Cloud inference    │
                                    │  get_embedding_provider()     │
                                    └───────────────────────────────┘
                                    ┌── EmbeddingService（112 行）──┐
                                    │  BAAI/bge-small-zh-v1.5       │
                                    │  本地模式（被 Provider 包装）  │
                                    └───────────────────────────────┘
```

---

## 02-项目服务

### 在系统中的角色

项目服务是所有工作流的入口容器。每个研究项目拥有独立的论文库、证据表、知识图谱和报告。项目服务管理项目生命周期和统计信息。

### 模块架构

```text
┌──────────────────────────────────────────────────────────────┐
│            ProjectService（PostgreSQL）                        │
│                                                               │
│  __init__(storage=None)                                       │
│  └─ 接收 PostgresStorage 实例                                 │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  生命周期                                              │    │
│  │                                                       │    │
│  │  create_project(name, desc, discipline, level, goal)  │    │
│  │  └─ 检查 name 唯一性 → INSERT projects 表             │    │
│  │                                                       │    │
│  │  get_project(ref)                                     │    │
│  │  └─ 按 project_id 或 name 查询                        │    │
│  │                                                       │    │
│  │  update_project(project_id, **updates)                │    │
│  │  └─ UPDATE projects 表                                │    │
│  │                                                       │    │
│  │  delete_project(project_id)                           │    │
│  │  └─ 2 步级联：                                        │    │
│  │     ① PostgreSQL delete_project_data（9 张子表）      │    │
│  │     ② Qdrant delete_by_papers                         │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  查询与统计                                            │    │
│  │                                                       │    │
│  │  list_projects() → list[Project]                      │    │
│  │  get_project_stats(ref) → dict                        │    │
│  │  └─ paper_count / parsed_count / card_count           │    │
│  │     evidence_count / graph_node_count / report_count  │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 03-论文库搜索导入

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Elicit** | 系统综述工作流（检索→筛选→数据抽取→报告）；语义搜索 + 结构化提取 | `search_papers()` 多源搜索 + 质量过滤；`import_by_doi()` 结构化导入 |
| **Connected Papers** | 从种子论文构建强连接论文图；相似论文发现 | `HybridRanker` 相似度排序；`search_queries` 查询缓存 |
| **ResearchRabbit** | Collection 管理、Similar Work / Earlier Work / Later Work 扩展 | `PaperLibraryService` 项目级论文库管理 |
| **Litmaps** | Seed map、Discover、Monitor 文献发现与追踪 | `search_adapters` 多源搜索编排 |
| **OpenAlex** | 2.5亿+作品数据库、concepts 带 confidence score、完全开放 API | `OpenAlexAdapter` 搜索适配器 |
| **Semantic Scholar** | 2.25亿+论文、SPECTER2 嵌入、TLDR 摘要 | `SemanticScholarAdapter` 搜索适配器 |

### 在系统中的角色

论文库搜索导入是研究资料的入口。用户通过搜索、DOI 导入、BibTeX 导入或 PDF 上传把论文纳入项目。系统提供多源搜索、去重融合、HybridRanker 排序、质量过滤和一步到位入库能力。

### 模块架构

#### 主搜索流水线（PaperLibraryService.search_papers，928 行）

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                    PaperLibraryService.search_papers()                       │
│                                                                              │
│  用户查询                                                                    │
│     │                                                                        │
│     ▼                                                                        │
│  ┌──────────────────────────────────────────┐                                │
│  │  query_optimizer.refine_query()（225 行） │                                │
│  │  LLM 提取学术关键词，去除修饰词/通用术语   │                                │
│  │  fallback：query ≤3 词时直接返回           │                                │
│  └─────────────────┬────────────────────────┘                                │
│                    ▼                                                          │
│  ┌──────────────────────────────────────────┐                                │
│  │  SearchCache 本地查询匹配                 │                                │
│  │                                          │                                │
│  │  ① 查询相似度匹配（dense only）           │                                │
│  │    Qdrant search_queries 集合             │                                │
│  │    语义搜索 → 余弦相似度 score            │                                │
│  │    score ≥ 阈值 → 从 PG search_queries    │                                │
│  │    取历史 paper_ids                       │                                │
│  │                                          │                                │
│  │  ② 论文相似度匹配（双路交集）             │                                │
│  │    Qdrant paper_profiles 集合             │                                │
│  │    dense 路 top-K ∩ sparse 路 top-K      │                                │
│  │    交集内按排名和排序 → 取 top-N          │                                │
│  │    → 本地相关论文                         │                                │
│  │                                          │                                │
│  │  ③ 命中 → 直接返回                       │                                │
│  │     未命中 → 继续外部搜索                 │                                │
│  └─────────────────┬────────────────────────┘                                │
│      本地未命中     │ 本地命中 → 直接返回                                      │
│                    ▼                                                          │
│  ┌──────────────────────────────────────────┐                                │
│  │  ThreadPoolExecutor 并行搜索              │                                │
│  │                                          │                                │
│  │  ┌────────────┐  ┌────────────┐          │                                │
│  │  │ OpenAlex   │  │  arXiv     │          │                                │
│  │  │ 176 行     │  │  176 行    │          │                                │
│  │  │ urllib     │  │  Atom XML  │          │                                │
│  │  │ 反转索引    │  │  4 字段模式│          │                                │
│  │  │ 摘要重建    │  │  年份后过滤│          │                                │
│  │  └────────────┘  └────────────┘          │                                │
│  │  ┌────────────┐  ┌────────────┐          │                                │
│  │  │ Semantic   │  │ Europe PMC │          │                                │
│  │  │ Scholar    │  │  164 行    │          │                                │
│  │  │  188 行    │  │  免费无key │          │                                │
│  │  │  API key   │  │  HTML清理  │          │                                │
│  │  │  TLDR提取  │  │  PMC/PubMed│          │                                │
│  │  └────────────┘  └────────────┘          │                                │
│  │                                          │                                │
│  │  每个 Adapter 经过 RateManager 控流：     │                                │
│  │  acquire() → search() → record_success() │                                │
│  │  失败时 record_error() → 指数退避         │                                │
│  └─────────────────┬────────────────────────┘                                │
│                    ▼                                                          │
│  ┌──────────────────────────────────────────┐                                │
│  │  SearchResultMerger.merge()（201 行）     │                                │
│  │                                          │                                │
│  │  第一轮：按 dedup key 分组                │                                │
│  │  优先级：DOI > arXiv ID > OpenAlex ID >   │                                │
│  │          S2 ID > title+year+first_author  │                                │
│  │  合并策略：最长摘要 / 最高引用 / 合并 IDs │                                │
│  │                                          │                                │
│  │  第二轮：Jaccard 标题相似度（≥0.85 去重） │                                │
│  └─────────────────┬────────────────────────┘                                │
│                    ▼                                                          │
│  ┌──────────────────────────────────────────────────────────────────────┐     │
│  │  HybridRanker（178 行）                                             │     │
│  │  ┌─────────────────────────────────────────────────────────────┐     │     │
│  │  │ ① hyde_query() → 假设性论文摘要                             │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ② Dense 编码：bge-small-zh 余弦相似度                       │     │     │
│  │  │    query_dense = embed_query(hypothetical)                  │     │     │
│  │  │    paper_dense = embed_texts(corpus)                        │     │     │
│  │  │    dense_scores = cosine_similarity(query, paper)           │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ③ Sparse 编码：TF-IDF(sublinear TF, L2, 10K features)      │     │     │
│  │  │    fit_sparse(corpus) → embed_query_sparse()                │     │     │
│  │  │    sparse_scores = dot_product(query_sparse, paper_sparse)  │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ④ 分别排序得到 dense_ranking + sparse_ranking               │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ⑤ RRF 融合：score[i] += 1/(k + rank + 1)，k=60             │     │     │
│  │  │    RRF 分仅用于排序，不入库。dense_scores[i]（余弦相似度）   │     │     │
│  │  │    设为 dense_score 入库                                    │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ⑥ Top-K 截断：取 RRF 排名前 top_k=60 篇                    │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ⑦ Quality Filter                                            │     │     │
│  │  │    quality_score = 0.75×citation_norm(√归一化)              │     │     │
│  │  │                 + 0.10×citation_velocity(citations/age)     │     │     │
│  │  │                 + 0.15×recency(e^(-0.08×age))              │     │     │
│  │  │    quality ≥ 0.3（不足 min_results=3 时按 quality 保底）    │     │     │
│  │  │    ↓                                                        │     │     │
│  │  │ ⑧ Top-N 截断：取 quality 过滤后前 top_n=30 篇              │     │     │
│  │  └─────────────────────────────────────────────────────────────┘     │     │
│  └──────────────────────────────────┬───────────────────────────────────┘     │
│                                     ▼                                        │
│  ┌──────────────────────────────────────────┐                                │
│  │  保存搜索结果（PG + Qdrant 双写）         │                                │
│  │                                          │                                │
│  │  ① PG search_queries（统一搜索表）        │                                │
│  │    UPSERT INTO search_queries             │                                │
│  │    search_type='candidate'/'review'       │                                │
│  │    query_text + local/remote_paper_ids    │                                │
│  │                                          │                                │
│  │  ② Qdrant search_queries（查询向量）      │                                │
│  │    add_search_query(search_id, query_text)│                                │
│  │    供未来查询相似度匹配                   │                                │
│  │                                          │                                │
│  │  ③ papers_pool[paper_id] → 全局论文池     │                                │
│  │    paper_id = doi_xxx / arxiv_xxx / oa_xxx│                                │
│  │    写入: title/abstract/authors/year/venue │                                │
│  │    + pdf_url/is_pdf_downloaded=False      │                                │
│  │    + is_parsed=False/added_at             │                                │
│  │    已存在则跳过（幂等）                    │                                │
│  └──────────────────────────────────────────┘                                │
└──────────────────────────────────────────────────────────────────────────────┘
```

#### 搜索直接入库（search_candidates，一步到位）

```text
┌──────────────────────────────────────────────────────────────────┐
│  search_candidates(project_id, query)                             │
│     │                                                             │
│     ├─ 调用 search_papers()（含 HybridRanker 排序）               │
│     ├─ _ensure_query() → 写入 queries 表                         │
│     ├─ 遍历结果：                                                 │
│     │   add_paper_metadata() → 去重检查 → 写入 papers 表          │
│     │   save_topic_score() → 写入 topic_scores（含 query_id 关联）│
│     ├─ _save_query_vector() → 写入 Qdrant 查询向量               │
│     ├─ _index_paper_profiles() → 写入 Qdrant paper_profiles      │
│     └─ 返回 SearchResponse（query + results + total_count）       │
│                                                                   │
│  设计：搜索即入库，无两步提交，无内存缓存                         │
└──────────────────────────────────────────────────────────────────┘

PG 表结构：
  queries: query_id PK, query_text, created_at
  topic_scores: (paper_id, query_id) UNIQUE, dense_score, quality_score, source
  topic_scores.paper_id → papers_pool.paper_id CASCADE
```

#### SearchOrchestrator（简单路径，159 行）

```text
┌──────────────────────────────────────────────────────────────────┐
│  SearchOrchestrator.search()                                      │
│  供 API 直接调用的简化路径                                         │
│                                                                   │
│  SearchCache.get() → ThreadPool 并行 → Merger.merge()            │
│  → HybridRanker → SearchCache.set()                               │
│                                                                   │
│  与主路径的区别：无 query_optimizer、无 LLM 过滤                    │
└──────────────────────────────────────────────────────────────────┘
```

#### 辅助组件

```text
┌──────────────────────────────────────────────────────────────────┐
│  RateManager（105 行）— 每源独立限流                               │
│  arXiv: 3.0s / OpenAlex: 0.5s / S2: 1.0s / PMC: 0.34s           │
│  指数退避：错误后 backoff_factor×min_interval（上限 5 分钟）       │
│                                                                   │
│  SearchCache — 查询相似度 + 论文相似度匹配                        │
│  ① Qdrant search_queries 集合：查询向量相似度（dense+sparse+RRF）│
│  ② Qdrant paper_profiles 集合：论文向量相似度                    │
│  ③ PG search_queries 表：历史论文 ID 回取                        │
│  ④ PG papers 表 + Qdrant paper_chunks：本地论文检索              │
│                                                                   │
│  query_optimizer 辅助函数：                                        │
│  multi_query(query, n=4) → 生成 N 个查询变体                      │
│  hyde_query(query) → 生成假设摘要（给 HyDE/Hybrid 用）            │
│  truncate_query(query, max_chars=250) → 截断长查询                 │
│                                                                   │
│  Topic Scoring（paper_library.py 803-928 行）：                    │
│  save_topic_score / get_topic_scores / recompute_topic_scores     │
│  按主题保存每篇论文的相关性分和质量分                              │
│                                                                   │
│  导入方式：                                                        │
│  search_papers()      → 搜索导入                                  │
│  import_doi_list()    → DOI 列表并行查询（Semantic Scholar API）   │
│  import_bibtex()      → BibTeX 正则解析                           │
│  add_uploaded_paper() → PDF 文件上传                              │
└──────────────────────────────────────────────────────────────────┘
```

#### 文献综述搜索流水线（search_for_review）

```text
┌──────────────────────────────────────────────────────────────────┐
│  search_for_review(project_id, query, max_local, max_remote, max_total)
│  供综述生成使用的论文搜索入口（paper_library.py）                │
│                                                                   │
│  参数：max_local=20, max_remote=20, max_total=40（环境变量控制） │
│                                                                   │
│  ① _search_local_papers(project_id, query, max_count)            │
│     │                                                             │
│     ├─ 加载项目论文列表                                          │
│     ├─ 查询相似度匹配（dense only，两级 fallback）：              │
│     │   ├─ 优先：Qdrant search_queries 集合                      │
│     │   │   语义搜索 → 余弦相似度 score                          │
│     │   │   score ≥ 阈值 → 从 PG search_queries 取历史 paper_ids │
│     │   └─ 回退：Jaccard token 相似度                            │
│     │       遍历 PG search_queries WHERE project_id              │
│     │       Jaccard(query_tokens, rec_tokens) > 0.3              │
│     ├─ 候选不足时补充全部项目论文                                │
│     ├─ 论文相似度匹配（双路 RRF 融合）：                          │
│     │   _qdrant_hybrid_search(query, candidate_ids, top_k)        │
│     │   ├─ 优先 paper_profiles 集合（title+abstract 向量）       │
│     │   │   dense top-K + sparse top-K → RRF 融合排序            │
│     │   └─ fallback paper_chunks 集合                            │
│     │       dense top-K + sparse top-K → RRF 融合排序            │
│     └─ 直接取 top-N（RRF 融合后无需 relevance 过滤）→ 返回本地论文 │
│                                                                   │
│  ② _search_remote_papers(query, exclude_ids, max_count)          │
│     │                                                             │
│     ├─ 复用 search_papers() 搜索 4 个学术平台                    │
│     ├─ 去重：排除已有的本地论文                                  │
│     └─ 返回去重后的 top-N                                        │
│                                                                   │
│  ③ 循环补充                                                      │
│     本地不足 → 联网补上 → 仍不足 → 继续搜索                     │
│     直到达到 max_total 或搜索源耗尽                              │
│                                                                   │
│  ④ _save_search_record(project_id, query, local, remote)         │
│     双写：                                                        │
│     ├─ PG search_queries（type=review, local/remote_paper_ids）  │
│     └─ Qdrant search_queries（查询向量化，供未来相似度匹配）     │
└──────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 组件 | 能力 |
| --- | --- |
| PaperLibrary | 论文 CRUD、PDF 上传、DOI/BibTeX 导入、纳入/排除标记 |
| query_optimizer | LLM 关键词提取（refine_query）、多查询变体（multi_query）、HyDE 假设摘要（hyde_query） |
| 4 搜索适配器 | OpenAlex（反转索引摘要重建）/ arXiv（4 字段模式+Atom XML）/ Semantic Scholar（TLDR 提取）/ Europe PMC（HTML 清理+PMC ID） |
| SearchResultMerger | dedup key 分组（DOI > arXiv > S2 > title+year+author）+ Jaccard 标题相似度 ≥0.85 二次去重 |
| HybridRanker | HyDE 假设摘要 → dense(bge-small-zh) + sparse(TF-IDF) → RRF 融合(k=60) → top-K(60) → quality_filter → top-N(30) |
| quality_filter | quality≥0.3 过滤（保底 top-3），quality=0.75×√引用归一化+0.10×引用速度+0.15×e^(-0.08×age)，RRF 融合后直接 top-K 截断，无 relevance 过滤 |
| SearchCache | 查询相似度匹配（dense only 语义搜索，余弦相似度筛选）→ 论文相似度匹配（dense + sparse RRF 融合排序）→ 历史论文回取（PG queries） |
| RateManager | 每源独立限流 + 指数退避（上限 5 分钟） |
| search_candidates | 一步到位：search_papers + 直接写入 papers/queries/topic_scores/Qdrant，无两步提交 |
| Topic Scoring | 按主题保存每篇论文的相关性分和质量分，支持 recompute |
| search_for_review | 文献综述搜索：Qdrant 向量查询相似度匹配（search_queries 集合，dense+sparse+RRF）→ Jaccard 回退 → Qdrant hybrid 检索 → 联网补充 → 循环至 40 篇 |
| queries/topic_scores | PG queries 表记录查询文本，topic_scores 记录论文与查询的关联及 dense/quality 分数 |
| Qdrant search_queries | 搜索查询向量集合（dense+sparse），payload: search_id/project_id/search_type，用于历史查询相似度匹配 |
| 双写模式 | PG 先写（source of truth）→ Qdrant 后写（向量加速）：search_candidates 写 papers/queries/topic_scores + Qdrant，search_for_review 写 queries + Qdrant |

---

## 04-PDF 解析与分块

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **paper2lkg（港科大 KGCP）** | 章节感知解析：不同章节使用不同提取策略；SciERC 兼容实体类型 | `_detect_section()` 章节识别；`_SECTION_PATTERNS` 章节模式 |
| **Scholarcy / SciSpace / Humata** | PDF→摘要/重点/引用定位的单篇论文解析流水线 | `parse_paper()` 完整解析流水线 |
| **Anthropic Contextual Retrieval** | 给每个 chunk 前缀论文标题、作者、章节上下文，提升检索精度 | `_enrich_context()` 上下文注入 |
| **GROBID** | 学术 PDF 结构化解析（章节识别、参考文献提取） | `_extract_references()` 参考文献结构化；章节检测 fallback 链 |

### 在系统中的角色

PDF 解析是把非结构化论文 PDF 转化为结构化 chunks 的关键环节。它支持 5 个解析器 fallback、双栏检测、文本后处理、章节识别、Parent-Child Chunking 和质量评分，是下游卡片生成和证据提取的基础。

### 模块架构

#### ParserService 主流水线（2316 行，单一文件内含全部组件）

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  ParserService.parse_paper(paper_id, force=False)                            │
│                                                                              │
│  PDF 文件                                                                    │
│     │                                                                        │
│     ▼                                                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  _extract_with_fallback（5 个适配器 + 扫描件检测 + OCR fallback）     │    │
│  │                                                                      │    │
│  │  ① 扫描件检测 _detect_scanned_pdf（图片占比 > 80%）                  │    │
│  │     │ 是 → OCR 尝试                                                  │    │
│  │     │ 否 ▼                                                            │    │
│  │  ② PyMuPDF4LLM（GNN 布局分析，Markdown 输出）── 失败 ▼               │    │
│  │  ③ PdfPlumber（基础文本提取）── 失败 ▼                                │    │
│  │  ④ PyMuPDF（双栏重排：_find_column_boundary → _reorder_columns）── 失败│    │
│  │  ⑤ PdfMiner（CID 字体支持）── 失败 ▼                                  │    │
│  │  ⑥ Docling（高质量 Markdown + OCR）── 失败 → 报错                     │    │
│  │                                                                      │    │
│  │  后检测：_detect_garbled_text（CNKI/万方乱码）                        │    │
│  │          _detect_poor_spacing（>20字符单词 >30%）                     │    │
│  │          _detect_watermark                                           │    │
│  └──────────────────────────────┬───────────────────────────────────────┘    │
│                                 ▼                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  TextPostProcessor.process()（303 行起）                              │    │
│  │  fix_cid_artifacts → fix_ligatures(7种) → fix_word_spacing           │    │
│  │  → fix_hyphenation → fix_citation_markers → remove_page_numbers      │    │
│  │  + remove_headers_footers（80% 阈值检测）                            │    │
│  └──────────────────────────────┬───────────────────────────────────────┘    │
│                                 ▼                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  _chunk_by_sections（50+ 模式匹配章节类型）                           │    │
│  │  _detect_section：编号 + 非编号章节标题检测                           │    │
│  │  分块参数：500-900 tokens，50-100 token 重叠                          │    │
│  │  Parent-Child：_create_parent_chunks（target=2000 tokens）           │    │
│  └──────────────────────────────┬───────────────────────────────────────┘    │
│                                 ▼                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  _enrich_context（Anthropic Contextual Retrieval 前缀）              │    │
│  └──────────────────────────────┬───────────────────────────────────────┘    │
│                                 ▼                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  ChunkCleaner.clean()（413 行起）— 6 步管线                          │    │
│  │                                                                      │    │
│  │  _filter_noise_lines                                                 │    │
│  │  ├─ 公式行检测 _is_formula_line（数学符号 + 低词密度）               │    │
│  │  ├─ 轴标签块检测 _find_axis_label_block（5+ 连续短行）              │    │
│  │  └─ 图表标题保留                                                     │    │
│  │       ↓                                                              │    │
│  │  _merge_fragments（同 section_type 内 200-1000 token 前后合并）      │    │
│  │       ↓                                                              │    │
│  │  ChunkDeduplicator.deduplicate（678 行）— 3 层去重                   │    │
│  │  ├─ _exact_dedup（SHA256 哈希）                                     │    │
│  │  ├─ _minhash_dedup（datasketch MinHashLSH, 128 排列, 5-char shingle）│    │
│  │  └─ _cosine_dedup（BM25 余弦相似度 ≥ 0.9）                         │    │
│  │       ↓                                                              │    │
│  │  _filter_low_quality（ChunkQualityScorer.score < 0.3 过滤）         │    │
│  │  ChunkQualityScorer（755 行）5 维加权评分：                          │    │
│  │  length(0.25) + info_density(0.25) + structure(0.20)                 │    │
│  │  + language(0.15) + noise(0.15)                                     │    │
│  │       ↓                                                              │    │
│  │  ChunkRedundancyFilter（856 行）BM25 余弦 ≥ 0.85，保留较长块       │    │
│  │       ↓                                                              │    │
│  │  _update_token_counts（cn_chars//2 + en_chars//4）                  │    │
│  └──────────────────────────────┬───────────────────────────────────────┘    │
│                                 ▼                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  保存：paper_chunks + paper_profiles + paper_sections 三写           │    │
│  │  向量化：embed_paper → 云端推理自动向量（或本地 EmbeddingService）    │    │
│  │  ① L2 paper_chunks：每个 child chunk 一个向量点                     │    │
│  │  ② L0 paper_profiles：title + abstract（论文级向量）                │    │
│  │  ③ L1 paper_sections：有 summary 的章节（章节级向量）               │    │
│  │  section_id 注入：从 paper_sections 表映射到 chunks                  │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  其他方法：                                                                  │
│  download_pdf / download_all_pdfs（并行下载）                                │
│  parse_project_papers（批量解析，ThreadPoolExecutor）                        │
│  get_chunks / get_body_chunks / get_parse_result                            │
│  search_chunks（向量检索 + parent-context 替换）                             │
│  _extract_references（[N] / N. / Author(Year) / 双换行 分割）              │
│  _check_quality_enhanced（乱码/水印/扫描件/双栏 综合检测）                  │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 05-论文卡片生成

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Elicit** | 自定义列提取（Research Question/Method/Finding/Limitation），每条带 source_quote + confidence | `PaperCard` 13字段结构化提取；`source_span` 来源追溯 |
| **Scholarcy** | 单篇论文→摘要/重点/引用定位的卡片式输出 | `PaperCardGenerator` 卡片生成流水线 |
| **Consensus** | Claims + Evidence Table 模式，每条 claim 带 evidence_quote | 卡片的 `key_findings` / `limitations` 字段带 `source_span` |

### 在系统中的角色

论文卡片生成把单篇论文的 chunks 转化为结构化研究摘要（PaperCard）。它是证据表和知识图谱的直接输入源。卡片包含研究问题、方法、发现、局限、未来工作等字段，每个字段都有来源追溯（source_span）。

### 模块架构

```text
┌──────────────────────────────────────────────────────────────────────────┐
│              PaperCardGenerator（636 行）                                  │
│                                                                           │
│  generate(paper_id, regenerate=False) → PaperCard | None                  │
│  └─ 幂等：已有 active 卡片且 regenerate=False → 跳过                     │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ① ContextSelector.select(chunks, max_tokens=8000)（94 行）         │  │
│  │  按 _SECTION_BUDGETS 优先级选择 chunks：                            │  │
│  │  result:1800 / method:1200 / intro:1200 / discussion:1500           │  │
│  │  conclusion:1000 / abstract:800 / limitation:800 / future:600       │  │
│  │  排除 references 类型                                               │  │
│  └──────────────────────────────┬──────────────────────────────────────┘  │
│                                 ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ② _extract_card → LLM 提取                                        │  │
│  │  invoke_json(SYSTEM_PROMPT, chunks_text)                            │  │
│  │  输出字段：research_question / method / data_or_sample              │  │
│  │           key_findings(list[ExtractedClaim]) / limitations          │  │
│  │           future_work / topics / possible_gaps / confidence         │  │
│  │                                                                     │  │
│  │  _validate_extraction → PaperCardExtractionResult 校验              │  │
│  │  _repair_extraction → 字符串列表转 ExtractedClaim                   │  │
│  └────────────────────┬────────────────────────────────────────────────┘  │
│                 失败 │                                                    │
│                       ▼                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ③ _extract_card_fallback（393 行）                                 │  │
│  │  关键词句子提取 → confidence=0.3                                    │  │
│  │  extraction_method = "rule_fallback"                                │  │
│  └──────────────────────────────┬──────────────────────────────────────┘  │
│                                 ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ④ _validate_card（457 行）— 来源校验                               │  │
│  │  ├─ source_span.chunk_id 是否存在于 chunks                         │  │
│  │  ├─ quote 匹配：精确匹配 → 模糊匹配（_fuzzy_match, 阈值 0.6）     │  │
│  │  └─ 泛化检测 _is_generic（10 个通用短语）                          │  │
│  └──────────────────────────────┬──────────────────────────────────────┘  │
│                                 ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ⑤ _compute_scores（499 行）— 3 维质量评分                          │  │
│  │                                                                     │  │
│  │  completeness = 满字段数/6（question/method/data/findings/limits/future）│
│  │  traceability = 有 source_span 的关键字段数 / 关键字段总数          │  │
│  │  specificity = max(0, 1 - generic_count × 0.2)                      │  │
│  │                                                                     │  │
│  │  quality_score = 0.4×completeness + 0.4×traceability + 0.2×specificity│
│  │  confidence 调整：fallback -0.3 / 有 unknown 字段 -0.1             │  │
│  └──────────────────────────────┬──────────────────────────────────────┘  │
│                                 ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ⑥ 保存 + _deactivate_old_cards（旧卡片 active=False）             │  │
│  │  batch_generate(project_id, only_missing=True)                      │  │
│  │  └─ ThreadPoolExecutor(max_workers=4) 并行                         │  │
│  │  get_quality_report → CardQualityReport                             │  │
│  │  └─ recommended_action: "accept" / "regenerate" / "add_sources"     │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  _FIELD_SECTIONS（字段→偏好章节映射）：                                   │
│  key_findings → [result, discussion]                                     │
│  limitations → [discussion, limitation]                                  │
│  method → [method, introduction]                                         │
│  future_work → [discussion, conclusion, limitation]                      │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 06-证据表

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Elicit** | 系统综述数据抽取表（Data Extraction Table），每篇论文抽取 Question/Method/Finding/Limitation | `EvidenceTableService` 13字段证据记录 |
| **Consensus** | Claims + Evidence + Consensus Meter（支持/反对/不确定分类） | `evidence_strength` 证据强度字段 |
| **Rayyan / Covidence / DistillerSR** | 筛选→纳入→排除→抽取→审计的证据管理工作流 | `EvidenceRecord` 结构化抽取 + 来源追溯 |
| **Scite** | 引用上下文（citation context）+ 引用立场分类 | `source_quote` / `citation_context` 字段 |

### 在系统中的角色

证据表是论文卡片和知识图谱之间的桥梁。它把单篇论文卡片中的发现、局限、未来工作转化为项目级结构化证据记录，供综述生成、创新点报告和 QA 使用。

### 模块架构

```text
┌──────────────────────────────────────────────────────────────────────────┐
│         EvidenceTableService（155 行）                                    │
│                                                                           │
│  __init__(storage: PostgresStorage | None = None)                          │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  build_for_project(project_id) → list[EvidenceRecord]               │  │
│  │  └─ 遍历项目所有 PaperCard → _card_to_evidence → 保存               │  │
│  │                                                                     │  │
│  │  build_for_paper(paper_id) → list[EvidenceRecord]                   │  │
│  │  └─ 单篇 PaperCard → _card_to_evidence → 保存                       │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  _card_to_evidence(card: PaperCard) → list[EvidenceRecord]（82 行） │  │
│  │  从 card 的 3 个字段生成 evidence：                                  │  │
│  │  ├─ key_findings[] → 每条一条 EvidenceRecord（evidence_type=finding）│  │
│  │  ├─ limitations[] → 每条一条 EvidenceRecord（evidence_type=limitation）│
│  │  └─ 兜底：research_question+method → 一条 summary 记录              │  │
│  │  每条记录绑定：source_chunk_id + source_quote + evidence_strength   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  query(project_id, filters) → list[EvidenceRecord]（58 行）         │  │
│  │  └─ 按 topic / method / keyword 等字段过滤                          │  │
│  │                                                                     │  │
│  │  query_by_scope(scope: RetrievalScope) → list[EvidenceRecord]（67 行）│
│  │  └─ 先按 scope.paper_ids 过滤                                      │  │
│  │  └─ 再按 scope.topic_ids / method_ids 交叉过滤                      │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                           │
│  normalize_topics(cards) → dict[str, list[str]]（134 行，模块级函数）     │
│  └─ 别名映射合并近似 topic（如 "llm feedback" → "feedback mechanism"）   │
└──────────────────────────────────────────────────────────────────────────┘

EvidenceRecord 字段：
  evidence_id / project_id / paper_id / topic / research_question
  method / data_or_sample / finding / limitation / future_work
  evidence_strength("strong"/"medium"/"weak") / citation_context
  source_chunk_id / source_quote
```

---

## 07-知识图谱

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Microsoft GraphRAG** | LLM 从文本 chunks 抽取实体+关系的流水线；GraphRAG local/global 上下文构建 | `GraphExtractor._extract_from_section()` LLM 提取；`build_graph_context()` 图谱上下文 |
| **Semantic Scholar** | Paper/Author/Venue 节点 + CITES 引用网络 | Paper 节点元数据结构；`_build_cites_edges()` 引用网络 |
| **paper2lkg（港科大 KGCP）** | 章节感知提取：Introduction→Task/Gap、Method→Method/Dataset、Results→Finding/Metric、Discussion→Limitation | `_SECTION_PRIORITY` 提取优先级排序；section_type 字段追踪 |
| **Elicit** | 结构化证据抽取（Research Question/Method/Finding/Limitation），每条带 source_quote 和 confidence | EvidenceRecord 13字段 → 图谱节点映射；`source_quotes` 来源追溯 |
| **Consensus** | Claims + Evidence Table 模式；Consensus Meter 多论文结论聚合（支持/反对/不确定） | `build_consensus_meter()` 多论文立场聚合；`CLAIM_STANCE_PROMPT` 立场分类 |
| **Scite** | Smart Citations：引用立场分类（supporting/contradicting/mentioning）；引用上下文 | `build_consensus_meter()` 中 LLM 立场分类；边的 `evidence_strengths` 属性 |
| **Neo4j llm-graph-builder** | PDF→LLM抽取→图谱写入；Entity 关联到 source Chunk 支持溯源 | `GraphExtractor` → `paper_sections` JSONB → `GraphService` 构图 |
| **Connected Papers** | 从种子论文构建强连接论文图；相似论文发现 | `get_subgraph()` 子图提取；`find_paths()` 路径查询 |
| **Litmaps / ResearchRabbit** | 引用网络扩展（Earlier Work / Later Work）；文献图谱可视化 | `get_neighbors()` 多跳遍历；`_build_cites_edges()` 引用网络 |
| **SciERC** | 6种实体类型标准（Task/Material/Method/Metric/OtherScientificTerm/Generic）+ 7种关系 | NodeType 15种兼容 SciERC；EdgeType 含 USED_FOR/HYPONYM_OF/PART_OF 等 |
| **OpenAlex** | 5种核心实体（Work/Author/Source/Institution/Concept）+ concepts 带 confidence score | 节点类型设计参考；Author 节点 + AUTHORED_BY 边 |
| **Leiden / GraphRAG** | 社区检测 + 层次摘要；global search map-reduce 模式 | `detect_communities()` Leiden + Label Propagation；`build_community_summaries()` |

### 在系统中的角色

知识图谱把论文、主题、方法、发现、局限和 gap 组织成关系网络。它为 ScopeQA 提供图谱上下文，为创新点报告提供 gap 检测，为综述生成提供主题分组。支持社区检测发现研究聚类，支持 Consensus Meter 多论文结论聚合，支持增量更新和质量评估。

### 模块架构

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  GraphExtractor（230 行） + GraphService（2400 行）                           │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  GraphExtractor：从 paper_sections 全文 LLM 提取实体/关系              │  │
│  │                                                                        │  │
│  │  extract_from_paper(paper_id)                                          │  │
│  │  └─ 按 _SECTION_PRIORITY 排序（method/experiment 优先）                │  │
│  │  └─ 每个 section → LLM(kg_extraction prompt) → entities/relations     │  │
│  │  └─ 回写到 paper_sections JSONB（entities/relations/claims/...）       │  │
│  │  └─ 截断 6000 字符，跳过 <80 字符的 section                           │  │
│  │                                                                        │  │
│  │  extract_from_project(project_id, only_missing=True)                   │  │
│  │  └─ 批量提取所有已解析论文                                             │  │
│  │  └─ 跳过 extraction_status="done" 的 section                          │  │
│  │                                                                        │  │
│  │  提取实体类型：Method/Dataset/Metric/Task/Topic/Finding/Limitation     │  │
│  │  提取关系类型：USES_METHOD/USES_DATASET/REPORTS_FINDING/...           │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  全量构建：build_project_graph(project_id, force=False)                │  │
│  │                                                                        │  │
│  │  Step 1: 加载数据                                                      │  │
│  │  ├─ papers（included=True）                                            │  │
│  │  ├─ evidence_records（按 paper_ids 过滤）                              │  │
│  │  ├─ paper_cards（active=True，用于 possible_gaps/future_work）         │  │
│  │  └─ paper_sections（extraction_status="done"，GraphExtractor 写入）    │  │
│  │       │                                                                │  │
│  │  Step 2: normalize_label + resolve_alias + stable_hash → 确定性 ID     │  │
│  │       │                                                                │  │
│  │  Step 3: Evidence 来源节点构建（6 个构建器）                           │  │
│  │  ├─ _ensure_paper_node → Paper 节点                                   │  │
│  │  ├─ _add_topic_node → Topic 节点 + BELONGS_TO_TOPIC 边                │  │
│  │  ├─ _add_method_node → Method 节点 + USES_METHOD 边                   │  │
│  │  ├─ _add_task_node → Task 节点 + STUDIES_TASK 边                      │  │
│  │  ├─ _add_dataset_node → Dataset 节点 + USES_DATASET 边                │  │
│  │  ├─ _add_finding_node → Finding 节点 + REPORTS_FINDING 边             │  │
│  │  └─ _add_limitation_node → Limitation 节点 + HAS_LIMITATION 边        │  │
│  │       │                                                                │  │
│  │  Step 4: Cards 补充                                                    │  │
│  │  ├─ possible_gaps → Gap 节点（source_type="possible_gap"）            │  │
│  │  └─ future_work → Gap 节点（source_type="future_work"）               │  │
│  │       │                                                                │  │
│  │  Step 5: _build_from_sections（从 JSONB 构建补充节点/边）              │  │
│  │  ├─ 7 个 section 来源节点构建器（含 Metric）                           │  │
│  │  ├─ _add_relation_edge → 跨实体关系边                                 │  │
│  │  └─ key_claims → Finding 节点补充                                      │  │
│  │       │                                                                │  │
│  │  Step 6: _build_author_nodes → Author 节点 + AUTHORED_BY 边           │  │
│  │       │                                                                │  │
│  │  Step 7: _build_cites_edges → CITES 边 + GhostPaper 节点              │  │
│  │  ├─ DOI 优先匹配 → 库内论文 CITES 边                                 │  │
│  │  ├─ title 模糊匹配 → 库内论文 CITES 边                               │  │
│  │  └─ 被引 >=2 次的库外论文 → NodeType.GHOST_PAPER 节点                │  │
│  │       │                                                                │  │
│  │  Step 8: _aggregate_gaps → 多篇共同 limitation → Gap 节点             │  │
│  │       │                                                                │  │
│  │  Step 9: 保存到 graphs collection                                      │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  增量更新：incremental_update(project_id, new_paper_ids)              │  │
│  │  ├─ 只加载新增论文数据                                                 │  │
│  │  ├─ 构建新增节点和边（复用全部构建器）                                 │  │
│  │  ├─ 合并到现有图谱（provenance 合并，不覆盖）                         │  │
│  │  └─ 保存更新后的图谱                                                  │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  实体消歧：merge_similar_entities(project_id)                         │  │
│  │  L1: 精确匹配（normalize_label + alias，构建时已处理）                │  │
│  │  L2: 编辑距离 ≤ threshold（_levenshtein 纯 Python 实现）              │  │
│  │  L3: embedding 余弦相似度 ≥ threshold（_cosine_similarity）           │  │
│  │  └─ 合并 provenance + 重定向边                                        │  │
│  │  ⚠ SPECTER2 fallback：检查本地 HF 缓存，未缓存则直接 fallback MiniLM │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  社区检测：detect_communities(project_id, resolution)                 │  │
│  │  ├─ 优先 Leiden 算法（需安装 igraph + leidenalg）                     │  │
│  │  ├─ 回退 Label Propagation（纯 Python，无需额外依赖）                 │  │
│  │  ├─ 创建 Community 节点 + BELONGS_TO_COMMUNITY 边                     │  │
│  │  └─ 过滤掉只有一个成员的社区                                          │  │
│  │                                                                        │  │
│  │  社区摘要：build_community_summaries(project_id)                      │  │
│  │  └─ 汇总社区成员信息 → LLM 生成社区主题摘要                          │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  Consensus Meter：build_consensus_meter(project_id)                   │  │
│  │  ├─ 按 normalized_label 聚类 Finding 节点                             │  │
│  │  ├─ 对每个聚类内的 Finding 用 LLM 判断立场                            │  │
│  │  │   └─ CLAIM_STANCE_PROMPT: supports / contradicts / neutral         │  │
│  │  ├─ 持久化 SUPPORTS/CONTRADICTS 边到图谱                              │  │
│  │  ├─ 计算 consensus_score = (supports - contradicts) / total           │  │
│  │  └─ 计算 agreement_rate = max(supports, contradicts) / total          │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  Gap 检测增强：find_research_gaps_enhanced(project_id)                │  │
│  │                                                                        │  │
│  │  Global Search：global_search(project_id, question)                   │  │
│  │  ├─ Map: 每个社区独立回答问题（基于 community summary）               │  │
│  │  └─ Reduce: 聚合社区回答为综合答案                                    │  │
│  │  ├─ future_work / possible_gap / limitation 三类 Gap 聚合             │  │
│  │  ├─ Method-Dataset 稀疏矩阵（未被组合的方法-数据集对）                │  │
│  │  └─ 稀疏区域检测（低连接度的孤立节点）                                │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  质量评估：compute_quality_metrics(project_id)                        │  │
│  │  ├─ 覆盖度：paper_count, section_extraction_rate, traceability_rate   │  │
│  │  ├─ 实体质量：duplicate_rate, isolated_non_paper_nodes                │  │
│  │  └─ 结构指标：node/edge_type_utilization, avg_degree                  │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  查询方法                                                              │  │
│  │  get_graph / get_node / get_neighbors / get_subgraph / find_paths      │  │
│  │  search_nodes → label 包含匹配                                         │  │
│  │  resolve_graph_scope → graph_node_ids → paper_ids + evidence_ids      │  │
│  │  build_graph_context → 紧凑 GraphRAG 上下文（供 QA）                  │  │
│  │  build_graph_summary → Topic/Method/Gap/Task 全局摘要                 │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  节点类型（16 种）：PAPER/AUTHOR/TOPIC/TASK/METHOD/DATASET/FINDING         │
│  LIMITATION/GAP/INNOVATION/METRIC/OTHER_SCI_TERM/VENUE/INSTITUTION/COMMUNITY│
│  GHOST_PAPER                                                               │
│  边类型（29 种）：核心 9 + SciERC 7 + 立场 2 + QA 溯源 1 + 扩展 10        │
│                                                                              │
│  同义词表（_ALIASES）：~100 条，覆盖 LLM/NLP、ML、指标、方法、数据集      │
│  工具函数：_levenshtein / _cosine_similarity / classify_limitation_type     │
│            compute_gap_confidence / resolve_alias                           │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 完整工作流

#### 流程一：全量构建

```text
用户上传论文 → PDF 解析 → 卡片生成 → 证据表构建
                                          │
                                          ▼
                              POST /kg/extract（LLM 实体提取）
                              ├─ 遍历每篇论文的 paper_sections
                              ├─ 按 section_type 排序（method/experiment 优先）
                              ├─ 每个 section → LLM(KG_EXTRACTION_PROMPT)
                              └─ 回写 entities/relations/claims 到 paper_sections JSONB
                                          │
                                          ▼
                              POST /kg/build（全量构图）
                              ├─ 加载 papers + evidence_records + paper_cards + paper_sections
                              ├─ Step 3: Evidence → 6 类节点 + 边
                              ├─ Step 4: Cards → Gap 节点（future_work/possible_gap）
                              ├─ Step 5: Sections JSONB → 7 类补充节点 + 跨实体边
                              ├─ Step 6: Author 节点 + AUTHORED_BY 边
                              ├─ Step 7: CITES 边 + GhostPaper 节点
                              ├─ Step 8: Gap 聚合（多篇共同 limitation）
                              └─ 保存到 graphs collection
```

#### 流程二：增量更新

```text
新论文上传 → 解析 → 卡片 → 证据
                              │
                              ▼
                  POST /kg/build（带 new_paper_ids）
                  ├─ 只加载新增论文数据
                  ├─ 复用全部构建器生成新节点/边
                  ├─ 合并到现有图谱（provenance 合并）
                  └─ 保存更新后图谱
```

#### 流程三：后处理增强

```text
POST /kg/extract → POST /kg/build
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
   POST /kg/         POST /kg/     POST /kg/
   merge-entities    communities   consensus
   │                 │             │
   ├─ L2 编辑距离    ├─ Leiden/    ├─ Finding 聚类
   ├─ L3 Embedding   │  LabelProp  ├─ LLM 立场判断
   └─ 合并+重定向    ├─ Community  └─ consensus_score
                     │  节点+边
                     └─ LLM 摘要
```

#### 流程四：查询与分析

```text
GET /kg                    → 获取完整图谱
GET /kg/stats              → 节点/边类型统计
GET /kg/quality            → 质量评估（覆盖度/实体质量/结构）
GET /kg/gaps               → Gap 节点列表
GET /kg/gaps/enhanced      → 增强 Gap 检测（稀疏矩阵+孤立节点）
GET /kg/summary            → Topic/Method/Gap/Task 全局摘要
GET /kg/validate           → 来源追溯校验
GET /kg/export/json        → JSON 导出

GET /kg/nodes/{id}         → 单节点详情
GET /kg/nodes/{id}/neighbors → 邻居节点（1-2 跳）
POST /kg/subgraph          → 子图提取（多节点 + 跳数）
POST /kg/paths             → 两节点间路径
POST /kg/search-nodes      → label 包含匹配搜索

POST /kg/scope             → 图谱节点 → paper_ids + evidence_ids
POST /kg/context           → 为 QA 构建紧凑 GraphRAG 上下文
POST /kg/communities/summaries → 社区 LLM 摘要
```

### API 端点清单（23 个）

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/cards` | 批量生成论文卡片 |
| POST | `/evidence/build` | 构建证据表 |
| POST | `/kg/build` | 全量构建知识图谱 |
| POST | `/kg/extract` | LLM 实体/关系提取 |
| GET | `/kg` | 获取完整图谱 |
| GET | `/kg/stats` | 节点/边类型统计 |
| POST | `/kg/subgraph` | 子图提取 |
| GET | `/kg/gaps` | Gap 节点查询 |
| GET | `/kg/gaps/enhanced` | 增强 Gap 检测 |
| GET | `/kg/nodes/{node_id}` | 节点详情 |
| GET | `/kg/nodes/{node_id}/neighbors` | 邻居节点 |
| POST | `/kg/paths` | 路径查询 |
| POST | `/kg/search-nodes` | 节点搜索 |
| POST | `/kg/scope` | 图谱范围解析 |
| POST | `/kg/context` | GraphRAG 上下文 |
| GET | `/kg/summary` | 全局摘要 |
| GET | `/kg/validate` | 来源追溯校验 |
| GET | `/kg/quality` | 质量评估指标 |
| GET | `/kg/export/json` | JSON 导出 |
| POST | `/kg/consensus` | Consensus Meter |
| POST | `/kg/global-search` | GraphRAG Global Search（Map-Reduce） |
| POST | `/kg/merge-entities` | 实体消歧 |
| POST | `/kg/communities` | 社区检测 |
| POST | `/kg/communities/summaries` | 社区摘要 |

### 已修复问题

| 问题 | 根因 | 修复 |
|------|------|------|
| `_build_cites_edges()` 崩溃 `'str' object has no attribute 'edge_type'` | `edges` 是 dict，`for e in edges` 遍历的是 key（字符串）而非 value | 改为 `for e in edges.values()` |
| `paper_sections` 缺少 `relations` 列 | GraphExtractor 写入 relations 到不存在的 JSONB 列，被 upsert 静默丢弃 | postgres.py schema 添加 `relations JSONB DEFAULT '[]'` + ALTER TABLE 补列 |
| SPECTER2 加载卡死 7+ 分钟 | huggingface.co 不可达时 `SentenceTransformer()` 重试超时 ~70s/次 | 加载前检查本地 HF 缓存目录，未缓存则 raise RuntimeError → fallback MiniLM |
| `NodeType.PAPER + is_ghost=True` 语义不清 | GhostPaper 用属性标记而非类型，查询时需额外过滤 | 新增 `NodeType.GHOST_PAPER` 枚举，替换所有 is_ghost 用法 |
| `build_consensus_meter` 不持久化立场边 | 立场判断结果只返回 JSON，不写入图谱 | 分类后创建 SUPPORTS/CONTRADICTS 边，调用 `_save_graph` 持久化 |
| `_aggregate_gaps` 只聚合 limitation | future_work/possible_gap 未跨论文聚合 | 增强为按 source_type 分别聚类，≥2 篇论文共指才创建聚合 Gap |
| Paper 节点无 title | build_project_graph 从 papers 表读取，但 papers 表无 title 列 | 改为从 papers_pool 读取完整元数据 |
| 边引用不存在的节点 276 条 | Finding/Limitation 节点 ID 用 {type}:{pid}:{hash}，entity_name_to_id 用 {type}:{hash} | 统一为 {type}:{hash}，_add_relation_edge 增加节点存在性检查 |
| 边缺少 evidence_id | section 提取的实体生成的边没有关联 evidence record | _build_from_sections 末尾统一为所有边关联同论文 evidence_id |
| "No specific findings" 冗余节点 | evidence_table 为无 findings 的卡片创建 fallback 证据记录 | 移除 fallback 证据记录 |
| 元数据写入 papers 表 | parser 写入 papers 表但该表无 title/abstract 列 | 改为写入 papers_pool 表 |
| 报告写入后无法读取 | Report 模型 `type` 字段在 DB 中是 `report_type`，其余字段无对应列 | `_report_to_db()` / `_db_to_report()` 转换，extra 存 metadata JSONB |
| QA history 字段丢失 | `_save_qa_history` 写入 intent/confidence 等字段，但 qa_history 表只有固定列 | answer→answer 列，其余存 metadata JSONB |
| validation_warnings 被覆盖 | `_validate_response` 用 `=` 覆盖而非追加，导致 json_parse_failed 警告丢失 | 改为 `extend` 追加 |
| 意图规则顺序错误 | "创新机会" 匹配 innovation_generation 而非 gap_analysis | gap_analysis/compare 移到 innovation/method 前面 |

### P1 改动清单（2026-06-05）

| 改动 | 文件 | 内容 |
|------|------|------|
| GHOST_PAPER 枚举 | `enums.py` | `NodeType.GHOST_PAPER = "GhostPaper"` |
| GhostPaper 替换 | `graph_service.py` | `_build_cites_edges` 使用 `NodeType.GHOST_PAPER`，移除 `is_ghost` |
| SUPPORTS/CONTRADICTS 边类型 | `enums.py` | `EdgeType.SUPPORTS` / `EdgeType.CONTRADICTS` |
| 立场边持久化 | `graph_service.py` | `build_consensus_meter` 分类后创建边 + `_save_graph` |
| Gap 跨论文聚合 | `graph_service.py` | `_aggregate_gaps` 增强：future_work/possible_gap/limitation 分别聚类 |
| Paper 节点 title | `graph_builders.py` | `_ensure_paper_node` + `build_project_graph` 从 papers_pool 读取 title/authors |
| node_id 一致性 | `graph_builders.py` | Finding/Limitation 节点 ID 统一为 {type}:{hash}，与 entity_name_to_id 一致 |
| 边 evidence_id 关联 | `graph_builders.py` | `_build_from_sections` 末尾统一为所有边关联同论文 evidence_id |
| 元数据存储 | `parser/service.py` | `_extract_and_update_metadata` 写入 papers_pool 而非 papers |
| paper_cards schema | `services/paper_card.py` | 完整卡片数据存入 extraction JSONB 字段 |
| is_active 列名 | `services/paper_card.py`, `services/scope.py` | 统一查询字段为 is_active |

### 测试覆盖（2026-06-05）

| 测试文件 | 用例数 | 覆盖内容 |
|----------|--------|---------|
| `test_graph.py` | 35 | 工具函数（12）+ E2E 构建/查询/社区/消歧/共识（23） |
| `test_graph_stable_ids.py` | 9 | 确定性重建：stable_hash、normalize_label、两次构建 ID 一致、node_id 格式 |
| `test_graph_traceability.py` | 5 | 可追溯性：非 Paper 节点 paper_ids、边 provenance、validate 指标、孤立边检测 |
| `test_graph_gap_detection.py` | 7 | Gap 检测：find_gaps 结构、enhanced sparse pairs/regions、跨论文聚合、同论文忽略 |
| `test_graph_scope.py` | 5 | Scope 解析：to_graph_context 非 Paper 节点、project_id 注入、边完整性、空输入、graph_subgraph |
| `test_graph_context.py` | 7 | GraphRAG 上下文：build_graph_context text/tokens、summary topics/methods、社区检测结构 |
| `test_graph_edge_cases.py` | 17 | 边界：空项目、不存在项目/节点、空查询、Consensus 零 findings、增量空 IDs、无效 node_ids、global_search |
| `test_graph_extractor.py` | 12 | GraphExtractor 单元测试：截断、畸形输出、JSONB 回写、跳过已提取；SPECTER2 mock |
| `test_pipeline_e2e.py` | 27 | 端到端 Pipeline：PDF 解析、实体提取、卡片、证据表、图谱、社区、质量指标、性能报告 |
| `test_scope_qa_unit.py` | 27 | ScopeQA 单元测试：意图分类、空 scope 拒答、证据评分、引用校验、端到端 answer、LLM fallback |
| **合计** | **151** | |

### 管线测试结果（2026-06-05 更新）

```text
项目 proj_a01b65e0（15 篇论文，8 篇已解析，4 篇有 PDF 未解析，3 篇无 PDF）

GraphExtractor: 8 篇论文完成提取（含 RAG 论文 24 entities, 15 relations）
build_project_graph: 282 nodes, 401 edges（全量构建）
  Paper:15 Finding:76 Method:63 Topic:47 Dataset:32 Limitation:17 Task:18 Metric:9 Gap:5
  REPORTS_FINDING:100 USES_METHOD:75 BELONGS_TO_TOPIC:60 EVALUATED_ON:46
  USES_DATASET:39 HAS_LIMITATION:32 STUDIES_TASK:29 USED_FOR:8 COMPARE:7 EXTENDS:5

质量指标：
  Paper 节点有 title: 15/15（修复前 0/15）
  断边引用: 0（修复前 276）
  节点可追溯性: 98.13%
  边 evidence 覆盖: 401/401 (100%)
  证据记录: 46 条
  孤立非 Paper 节点: 8 个（5 Gap + 3 泛化 Metric）

已修复问题：
  1. Paper 节点无 title → 从 papers_pool 读取元数据
  2. 边引用缺失节点 276 条 → 统一 node_id 为 {type}:{hash} 格式
  3. paper_cards extraction JSONB → 完整卡片数据存入 extraction 字段
  4. active vs is_active 列名不一致 → 统一为 is_active
  5. 边缺少 evidence_id → _build_from_sections 末尾统一关联同论文 evidence
  6. "No specific findings" 冗余 Finding → 移除 fallback 证据记录
  7. 元数据写入 papers 表 → 改为写入 papers_pool 表

to_graph_context（3 papers scope）: 正常返回非 Paper 节点 + 边 ✓
detect_communities: 正常运行 ✓
compute_quality_metrics: 正常运行 ✓
build_graph_summary: 正常运行 ✓
```

---

## 08-RetrievalScope

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Elicit** | 系统综述筛选工作流：按主题/方法/年份筛选论文集合 | 6 种 scope 类型：project / selected_papers / topic / method / year_range / graph_subgraph |
| **Rayyan / Covidence** | 文献筛选→纳入→排除的工作流；cross_filter 交叉过滤 | `_cross_filter()` 多维度交叉过滤 |
| **Scite** | 引用上下文和引用立场分类；引用范围校验 | `ScopeGuard` 范围守卫 + `validate_against_scope()` 越界检测 |
| **Litmaps** | 按图谱子图限定文献范围 | `graph_subgraph` scope 类型 + `GraphService.get_subgraph()` |

### 在系统中的角色

RetrievalScope 是系统的核心边界机制。所有 QA、综述、创新点报告都必须先通过它确定回答范围，确保不引用 scope 外的论文和证据。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│               RetrievalScopeService（581 行 / 21 方法）                │
│                                                                        │
│  resolve(scope_spec) ──▶ 6 种 scope 类型分派                           │
│       │                                                                │
│       ├─ "project" ──▶ _resolve_project                               │
│       │     storage.list_papers(project_id) → 全部 included 论文       │
│       │                                                                │
│       ├─ "selected_papers" ──▶ _resolve_selected                      │
│       │     直接使用 spec.paper_ids，校验存在性                        │
│       │                                                                │
│       ├─ "topic" ──▶ _resolve_topic                                   │
│       │     papers_pool 按 topic keyword 模糊匹配 → cross_filter      │
│       │                                                                │
│       ├─ "method" ──▶ _resolve_method                                 │
│       │     papers_pool 按 method keyword 模糊匹配 → cross_filter     │
│       │                                                                │
│       ├─ "year_range" ──▶ _resolve_year_range                         │
│       │     papers_pool 按 year_min/year_max 过滤 → cross_filter      │
│       │                                                                │
│       └─ "graph_subgraph" ──▶ _resolve_graph_subgraph                 │
│             GraphService.get_subgraph(node_ids, depth)                 │
│             → 提取关联 paper_ids                                      │
│                                                                        │
│  ──────────────── 交叉过滤（resolve 内部） ────────────────            │
│  _cross_filter(papers, topics, methods, year_range)                    │
│    topics: normalize_topics 后做交集                                   │
│    methods: normalize_topics(methods) 后做交集                         │
│    year_range: year_min ≤ paper.year ≤ year_max                       │
│                                                                        │
│  ──────────────── ScopeGuard 构建 ────────────────                     │
│  build_scope_guard(scope) → ScopeGuard                                 │
│    allowed_paper_ids = set(scope.to_paper_ids())                       │
│    allowed_evidence_ids = {ev.id for ev in scope.to_evidence_records()}│
│    allowed_graph_node_ids = {n.id for n in scope.to_graph_context()}   │
│                                                                        │
│  ──────────────── 验证 ────────────────                                │
│  validate_against_scope(response, guard) → Violations[]                │
│    检查 response.citations 中每个引用的 paper_id                       │
│    是否 ∈ guard.allowed_paper_ids                                      │
│    → scope_violations[], valid_citations[]                             │
│                                                                        │
│  ──────────────── 数据提取 ────────────────                            │
│  to_paper_ids(scope) → str[]                                           │
│  to_evidence_records(scope) → EvidenceRecord[]                         │
│    通过 EvidenceTableService.query_by_scope                            │
│  to_paper_cards(scope) → PaperCard[]                                   │
│    通过 storage.get_item("cards", paper_id)                            │
│  to_graph_context(scope) → {nodes, edges, project_id, ...}             │
│    graph_subgraph: GraphService.get_subgraph() 完整子图                │
│    其他 scope: Paper 节点 + 1-hop 关联的非 Paper 节点（Topic/Method 等）│
│  get_scope_filters(scope) → dict                                       │
│    用于 VectorStorage 的 filter 参数                                   │
│  summarize(scope) → str                                                │
│    人类可读描述，如 "3 papers, 12 evidence, topic: transformer"        │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| resolve | 6 种 scope 类型分派 + 交叉过滤（topics/methods/year） |
| build_scope_guard | 构建 ScopeGuard（allowed_paper_ids + allowed_evidence_ids + allowed_graph_node_ids） |
| validate_against_scope | 校验回答引用是否在 scope 内，返回 violations + valid_citations |
| to_paper_ids | scope 内 paper ID 列表 |
| to_evidence_records | scope 内证据（通过 EvidenceTableService.query_by_scope） |
| to_paper_cards | scope 内卡片（通过 storage.get_item） |
| to_graph_context | **增强版**：graph_subgraph 完整子图；其他 scope 返回 Paper + 1-hop 非 Paper 节点 + 关联边，含 project_id |
| get_scope_filters | 向量搜索过滤条件（用于 VectorStorage filter） |
| summarize | 人类可读 scope 描述 |
| _cross_filter | 按 topics/methods/year_range 交叉过滤 |

---

## 09-ScopeQA 与 RAG

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **NotebookLM** | Sources + QA + Artifact 模式：基于上传源问答，生成成果物 | `ScopeQAService` 基于 scope 问答 + 范围声明 |
| **Consensus** | 多论文结论聚合；Research Database 结构化查询 | `retrieve_context()` 多源证据检索 + 融合排序 |
| **Elicit** | 基于选定论文集合的结构化问答；Supporting quotes 可追溯 | `classify_intent()` 意图分类；`source_quote` 来源追溯 |
| **Microsoft GraphRAG** | local/global search 模式；GraphRAG 上下文构建 | `build_graph_context()` 图谱上下文；HierarchicalRetriever 渐进检索 |
| **HippoRAG** | 神经生物学启发的长期记忆；Personalized PageRank 图检索 | 图谱子图检索 + 向量检索混合 |

### 在系统中的角色

ScopeQA 是用户与论文库交互的核心入口。用户选择 scope 后提出问题，系统在 scope 内检索证据、分类意图、生成带引用的回答，并校验引用不越界。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│               ScopeQAService（575+ 行 / 14 方法）                      │
│                                                                        │
│  answer(question, scope_spec, project_id) ──▶ 9 步流水线              │
│       │                                                                │
│       ▼                                                                │
│  ① resolve(scope_spec) ──▶ RetrievalScope                              │
│       │                                                                │
│       ▼                                                                │
│  ② classify_intent(question) ──▶ IntentType                            │
│       │   LLM 分类，keyword fallback（_INTENT_RULES 字典）             │
│       │   9 种意图各有 keyword 触发词 + preferred_fields               │
│       │                                                                │
│       ▼                                                                │
│  ③ build_scope_guard(scope) ──▶ ScopeGuard                             │
│       │   allowed_paper_ids + allowed_evidence_ids + allowed_graph_node_ids│
│       │                                                                │
│       ▼                                                                │
│  ④ retrieve_context(question, scope, intent)                           │
│       │   vector_storage.search(question, filter=scope_filters, top_k=20)│
│       │   → 按 intent.preferred_fields 加权排序                        │
│       │   → _score_evidence(evidence, intent)                          │
│       │     +4 if intent field has content                             │
│       │     +2 if keyword match in evidence                            │
│       │     +1 if source_quote exists                                  │
│       │     ±1 by evidence_strength                                    │
│       │   → dedup by paper_id（保留最高分）                            │
│       │                                                                │
│       ▼                                                                │
│  ⑤ _build_context_block(evidence_list) → str                          │
│       │   拼接 evidence 为 LLM context                                 │
│       │                                                                │
│       ▼                                                                │
│  ⑥ generate_answer(question, context, intent)                          │
│       │   LLM 生成回答，prompt 含 intent 指导                          │
│       │   _build_graph_text(nodes, edges) → 图谱上下文（非 Paper 节点 + 关系）│
│       │   graph_paths 从 graph_edges 构建路径信息                      │
│       │                                                                │
│       ▼                                                                │
│  ⑦ _extract_citations(answer_text, evidence_list)                      │
│       │   从回答中提取 [paper_id] 格式引用                             │
│       │                                                                │
│       ▼                                                                │
│  ⑧ validate_against_scope(citations, scope_guard)                      │
│       │   检查每个引用 paper_id ∈ allowed_paper_ids                    │
│       │   → scope_violations[], valid_citations[]                      │
│       │                                                                │
│       ▼                                                                │
│  ⑨ _build_response() → QAResponse                                     │
│       answer: str                                                      │
│       evidence: EvidenceRecord[]                                       │
│       intent: IntentType                                               │
│       confidence: float                                                │
│       scope_violations: str[]                                          │
│       suggested_actions: str[]                                         │
│                                                                        │
│  ──────────────── 意图规则表（_INTENT_RULES） ────────────────          │
│  ┌──────────────────────┬──────────────────────┬───────────────────┐   │
│  │ 意图                  │ 触发 keywords        │ preferred_fields  │   │
│  ├──────────────────────┼──────────────────────┼───────────────────┤   │
│  │ review_generation    │ 综述, review, survey  │ findings, topics  │   │
│  │ innovation_generation│ 创新, gap, 创新点     │ limitations, future│  │
│  │ limitation_analysis  │ 局限, 不足, limitation│ limitations       │   │
│  │ method_analysis      │ 方法, method, approach│ methods           │   │
│  │ method_compare       │ 比较, 对比, compare   │ methods           │   │
│  │ finding_summary      │ 发现, 结果, finding   │ findings          │   │
│  │ gap_analysis         │ gap, 缺陷, 空白       │ limitations, future│  │
│  │ evidence_check       │ 证据, 支撑, evidence  │ findings          │   │
│  │ trend_analysis       │ 趋势, trend, 方向     │ topics            │   │
│  └──────────────────────┴──────────────────────┴───────────────────┘   │
│                                                                        │
│  ──────────────── QA 历史持久化 ────────────────                       │
│  save_qa_history(project_id, qa_record)                                │
│  list_qa_history(project_id) → QARecord[]                              │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| answer | 8 步流水线：resolve → classify_intent → guard → retrieve_context → generate_answer → _validate_response → save_history |
| classify_intent | keyword fallback（_INTENT_RULES 字典匹配），9 种意图 + 默认 summary |
| retrieve_context | scope.evidence_ids → ScoredEvidence 评分排序 + enhanced diagnostics |
| _score_evidence | 返回 `list[ScoredEvidence]`：+4 intent field / +2 keyword / +1 source_quote / ±1 strength / -2 rejected / -2 low quality |
| generate_answer | LLM 回答，JSON parse failure 降级（raw_response），source_quotes + key_points 提取 |
| _validate_response | **增强**：ScopeGuard 校验 + evidence_id 存在性 + source_quote 校验 + key_points evidence_id 校验 |
| save_qa_history | QA 历史持久化 |
| _build_graph_text | 非 Paper 节点渲染（Topic/Method/Finding/Gap）+ 关系边（限制 15 条） |
| graph_paths | 从 graph_edges 提取 `{src} --{rel}--> {tgt}` 路径信息 |

**DI 增强**：构造函数接受 `scope_service`、`graph_service` 可注入，支持单元测试 mock。

**新增模型**：
- `ScoredEvidence`：evidence_id, paper_id, score, score_breakdown, matched_fields, evidence_type, source_quote, evidence_strength
- `QALLMOutput`：answer, key_points, supporting_evidence_ids, supporting_paper_ids, source_quotes, uncertainty, confidence, suggested_actions

**单元测试**：`test_scope_qa_unit.py` — 27 个测试，FakeLLM + MemoryStorage，无 API/DB 依赖：
- classify_intent（8 种意图关键词分类）
- empty scope 拒答 / evidence scarcity 拒答
- rejected evidence 过滤 / intent field 偏好 / low strength 降权 / ScoredEvidence breakdown
- _validate_response（越界 paper/evidence 过滤、context 存在性、source_quote 校验、key_points 校验、全引用移除降级）
- 端到端 answer（正常回答、diagnostics、scope leak warning）
- LLM 失败 fallback / JSON parse failure raw_response 降级
- retrieve_context（ScoredEvidence 返回、diagnostics 完整性）

---

## 09b-HierarchicalRetriever 渐进式检索

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Microsoft GraphRAG** | 多层检索：local search（细粒度）+ global search（粗粒度） | L0→L1→L2 三层渐进检索 |
| **HippoRAG** | Personalized PageRank 图检索 + 向量检索混合 | 向量检索 + 图谱子图检索混合 |
| **LightRAG** | 双层检索（低层级+高层级）；EMNLP 2025 | 层级回退策略：L2 无结果→L1→L0 |
| **RAGFlow** | 深度文档解析 + 检索增强 | `EmbeddingProvider` local/cloud 统一切换 |

### 在系统中的角色

HierarchicalRetriever 是三层渐进式向量检索服务，利用 Qdrant 的 L0/L1/L2 三层集合实现从论文→章节→chunk 的逐层精筛。它为 ScopeQA 提供向量检索能力，替代纯元数据过滤。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│        HierarchicalRetriever（~300 行 / 10 方法）                      │
│                                                                        │
│  retrieve(query_text, project_id, top_k_*) ──▶ 渐进式检索              │
│       │                                                                │
│       ▼                                                                │
│  ① _embed_query(text) → (dense_emb, sparse_emb)                       │
│       │   云端推理：返回空占位（search_hybrid_by_text 自动处理）        │
│       │   本地模式：EmbeddingService.embed_query + embed_sparse        │
│       │                                                                │
│       ▼                                                                │
│  ② L0: _search_l0 → paper_profiles（project_id 过滤）                  │
│       │   → top_k_papers 个 paper_id                                  │
│       │   若 L0 为空 → 直接回退到 flat L2 检索                         │
│       │                                                                │
│       ▼                                                                │
│  ③ L1: _search_l1 → paper_sections（L0 的 paper_ids 过滤）            │
│       │   → top_k_sections 个 section_id                              │
│       │   若 L1 为空 → 用 paper_ids 直接搜 L2                         │
│       │                                                                │
│       ▼                                                                │
│  ④ L2: _search_l2 → paper_chunks（L1 的 section_ids 过滤）            │
│       │   → top_k_chunks 个 chunk                                     │
│       │   若结果太少（< top_k//3）→ 用 paper_ids 重搜                  │
│       │                                                                │
│       ▼                                                                │
│  ⑤ _enrich_with_context(l2, l1, l0) → list[RetrievalResult]           │
│       │   合并 paper_title / section_title / section_type              │
│       │   layer_scores: {"l0": 0.9, "l1": 0.85, "l2": 0.78}          │
│       │                                                                │
│       ▼                                                                │
│  RetrievalResult + RetrievalDiagnostics                                │
│                                                                        │
│  ──────────────── 回退策略 ────────────────                            │
│  L0 为空 → retrieve_flat_fallback（L2 + project_id where 过滤）        │
│  L1 为空 → L2 + paper_ids 过滤                                        │
│  L2 section_id 过滤太少 → L2 + paper_ids 重搜                         │
│  fallback_used / fallback_reason 记录在 diagnostics 中                 │
│                                                                        │
│  ──────────────── 依赖 ────────────────                                │
│  storage.vector.VectorStorage（search_hybrid 含 where 过滤）           │
│  storage.embedding_provider.get_embedding_provider()                   │
│  └─ cloud: CloudEmbeddingProvider → Qdrant Cloud inference             │
│  └─ local: LocalEmbeddingProvider → EmbeddingService                   │
│  storage.postgres.PostgresStorage（paper/section 上下文补充）          │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| retrieve | 渐进式 L0→L1→L2 检索，自动回退，返回 (results, diagnostics) |
| retrieve_flat_fallback | L0/L1 为空时的 L2 直搜回退（project_id where 过滤） |
| _search_l0 | paper_profiles 集合检索（project_id 过滤） |
| _search_l1 | paper_sections 集合检索（paper_ids 过滤） |
| _search_l2 | paper_chunks 集合检索（section_ids + paper_ids 过滤） |
| _enrich_with_context | 合并 L0/L1 上下文到 L2 chunk 结果 |

### 数据模型

```python
class RetrievalResult(BaseModel):
    chunk_id: str
    paper_id: str
    section_id: str = ""
    chunk_text: str = ""
    section_title: str = ""
    section_type: str = ""
    paper_title: str = ""
    dense_score: float = 0.0
    layer_scores: dict[str, float]  # {"l0": 0.9, "l1": 0.85, "l2": 0.78}
    metadata: dict[str, Any]

class RetrievalDiagnostics(BaseModel):
    l0_candidates: int = 0;  l0_selected: int = 0
    l1_candidates: int = 0;  l1_selected: int = 0
    l2_candidates: int = 0;  l2_selected: int = 0
    fallback_used: bool = False;  fallback_reason: str = ""
    duration_ms: int = 0
```

---

## 10-综述生成

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **GPT-Researcher** | Review-Revise 循环（Writer→Reviewer→Revisor）；上下文压缩（按主题分组摘要） | `LiteratureReviewGenerator` 多步生成 + 质量校验 |
| **STORM / Co-STORM** | 多视角知识整合（按主题、方法、时间、争议组织文献综述） | `collect_materials()` 多维度材料收集 |
| **SciSage** | 多层 Reflector：结构反思、章节反思、证据反思 | 质量校验 + 章节完整性检查 |
| **Elicit** | 系统综述报告生成；基于结构化证据表生成综述 | `build_evidence_matrix()` 证据矩阵 → 综述 |
| **Atlas** | 跨论文综合（不是单篇摘要拼接）；幻觉-验证比率 | 综述质量指标：hallucination_ratio |

### 在系统中的角色

综述生成基于 scope 内的证据矩阵，生成有结构、有来源、有质量校验的文献综述报告。它不是让 LLM 直接写综述，而是先收集材料、构建证据矩阵，再让 LLM 基于结构化材料生成。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│          LiteratureReviewGenerator（569 行 / 12 方法）                 │
│                                                                        │
│  generate(scope_spec, project_id) ──▶ 完整流水线                       │
│       │                                                                │
│       ▼                                                                │
│  ① resolve(scope_spec) ──▶ RetrievalScope                              │
│       │                                                                │
│       ▼                                                                │
│  ①.5 _truncate_scope_papers(scope) — 论文数量控制                      │
│       │   环境变量：REVIEW_MAX_PAPERS=40, LOCAL_MAX=20, REMOTE_MAX=20  │
│       │   按 source_platform 分离本地/远程论文                         │
│       │   排序：0.6×dense_score + 0.4×quality                          │
│       │   选 top-LOCAL_MAX 本地 + 剩余配额给远程                      │
│       │   保存 original_paper_count 到 metadata                        │
│       │                                                                │
│       ▼                                                                │
│  ② collect_materials(scope)                                            │
│       │   evidence = scope.to_evidence_records()                       │
│       │   cards = scope.to_paper_cards()                               │
│       │   graph_context = scope.to_graph_context()                     │
│       │                                                                │
│       ▼                                                                │
│  ③ _build_evidence_matrix(evidence, cards) → dict                      │
│       │   by_topic: {topic: [EvidenceRecord]}                          │
│       │   by_method: {method: [EvidenceRecord]}                        │
│       │   findings: [key_findings 聚合]                                │
│       │   limitations: [limitations 聚合]                              │
│       │   future_works: [future_works 聚合]                            │
│       │                                                                │
│       ▼                                                                │
│  ④ _generate_sections(matrix, scope) → dict[str, Section]             │
│       │   LLM 生成 5 个必选章节                                       │
│       │   _build_graph_text(graph_context) → 图谱关系注入 prompt      │
│       │   _REQUIRED_SECTIONS = {background, methods, findings,         │
│       │                         limitations, future_trends}            │
│       │   每个 section 含 title + content + source_paper_ids           │
│       │                                                                │
│       │   失败时 ──▶ _fallback_sections(matrix)                        │
│       │     规则生成：从 matrix 中提取结构化摘要                       │
│       │                                                                │
│       ▼                                                                │
│  ⑤ _render_markdown(sections, evidence, project_name) → str           │
│       │   标题 + 摘要 + 各章节正文                                    │
│       │   每段后标注 [paper_id] 来源                                   │
│       │   末尾参考文献列表                                             │
│       │                                                                │
│       ▼                                                                │
│  ⑥ validate_review(markdown, scope) → ValidationResult                │
│       │   ├─ 结构完整性：5 个 _REQUIRED_SECTIONS 是否齐全              │
│       │   ├─ 来源覆盖率：section_source_coverage ≥ 0.80               │
│       │   ├─ scope 越界：引用 paper_id ∈ allowed_paper_ids            │
│       │   ├─ 泛化文本：_GENERIC_PHRASES 检测（11 个短语）             │
│       │   │   如 "需要进一步研究" "具有重要意义" "近年来"              │
│       │   └─ 参考文献覆盖：引用 paper 是否在 references 中            │
│       │                                                                │
│       ▼                                                                │
│  ⑦ 保存 Report                                                         │
│       storage.upsert_item("reports", report)                           │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| generate | 7+1 步流水线：resolve → truncate_papers → collect → matrix → sections → markdown → validate → save |
| _truncate_scope_papers | 论文数量控制：按 source_platform 分离本地/远程，0.6×dense_score+0.4×quality 排序，top-20 本地 + 剩余远程 |
| collect_materials | 收集 scope 内 evidence + cards + graph_context |
| _build_evidence_matrix | 按 topic/method 分组 + findings/limitations/future_works 聚合 |
| _generate_sections | LLM 生成 5 个必选章节（background/methods/findings/limitations/future_trends），**prompt 注入图谱关系上下文**（`_build_graph_text()` 渲染非 Paper 节点摘要 + 关键关系） |
| _fallback_sections | LLM 失败时规则生成 5 章节 |
| _render_markdown | 渲染含来源标记和参考文献的 Markdown |
| validate_review | 结构完整性 + 来源覆盖率 + scope 越界 + 泛化检测 + 参考文献覆盖 |

---

## 11-创新点报告

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **PapersFlow** | 反证检测（自动寻找支持/反对论文）；多 Agent 协作 | `findings_contradictions` 信号；supporting/limiting evidence |
| **Consensus** | Consensus Meter（Yes/No/Possibly/Mixed 四级分类）；多论文结论聚合 | 创新点评分中的 evidence 维度 |
| **Atlas** | 跨论文综合；幻觉检测；思维导图可视化 | `evidence_coverage` 验证；跨论文 gap 聚合 |
| **GPT-Researcher** | 树状图谱探索（递归遍历 knowledge graph 找 gap） | `detect_graph_gaps()` 图谱 gap 检测 |
| **Scite** | 引用立场分类（supporting/contrasting/mentioning） | 每个创新点的 supporting_evidence + limiting_evidence |

### 在系统中的角色

创新点报告基于 scope 内的 gap、共同局限和 future work，生成具体、可执行、可追溯的创新方向。它不是泛泛建议，而是有文献支撑、有评分、有风险分析的研究方向。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│         InnovationReportGenerator（700 行 / 16 方法）                  │
│                                                                        │
│  generate(scope_spec, project_id) ──▶ 完整流水线                       │
│       │                                                                │
│       ▼                                                                │
│  ① resolve(scope_spec) ──▶ RetrievalScope                              │
│       │                                                                │
│       ▼                                                                │
│  ② collect_signals(scope) ──▶ 4 来源信号                               │
│       │                                                                │
│       ├─ aggregate_limitations(evidence)                               │
│       │   聚类共同 limitation（出现 ≥2 次的相似表述）                  │
│       │                                                                │
│       ├─ aggregate_future_works(evidence)                              │
│       │   聚类 future_work（出现 ≥2 次的相似方向）                     │
│       │                                                                │
│       ├─ detect_graph_gaps(graph)                                      │
│       │   图谱中 gap 类型节点 + 关联 limitation                        │
│       │   + find_research_gaps_enhanced() sparse pairs/regions         │
│       │                                                                │
│       └─ detect_explicit_gaps(evidence)                                │
│           _GAP_KEYWORDS = 18 个关键词                                  │
│           如 "未解决" "空白" "缺少" "需要改进" "局限性"                │
│           扫描 evidence.text 匹配 → explicit_gap 列表                  │
│                                                                        │
│       ▼                                                                │
│  ③ _build_candidate_skeletons(signals) → Skeleton[]                    │
│       │   限制：limitations ≤5, future_works ≤5,                       │
│       │         graph_gaps ≤3, explicit_gaps ≤3                        │
│       │   共计最多 16 个候选                                           │
│       │   按 description 文本 dedup（去重后通常 8-12 个）              │
│       │                                                                │
│       ▼                                                                │
│  ④ LLM 生成创新点候选                                                  │
│       │   输入：skeletons + evidence_context + graph_context           │
│       │   输出：每个 skeleton 的 expanded innovation point              │
│       │                                                                │
│       ▼                                                                │
│  ⑤ _score_innovation(candidate, evidence, graph) → ScoredInnovation   │
│       │   5 维加权评分：                                               │
│       │   novelty:      0.25 — 与现有工作差异度                        │
│       │   evidence:     0.25 — 支撑证据数量和质量                      │
│       │   feasibility:  0.20 — 技术可行性                              │
│       │   risk:         0.10 — 风险评估（越低越好）                    │
│       │   specificity:  0.20 — 具体性（非泛泛而谈）                    │
│       │   → total_score = weighted sum                                 │
│       │                                                                │
│       ▼                                                                │
│  ⑥ _filter_generic_candidates(candidates) → filtered                  │
│       │   _GENERIC_PHRASES = 20 个空泛短语                             │
│       │   如 "进一步研究" "优化模型" "提高性能" "具有重要意义"         │
│       │   如 "改进方法" "探索新方向" "需要更多数据" "减少误差"         │
│       │   检测 innovation text 中空泛短语占比                          │
│       │   占比过高 → 过滤掉                                            │
│       │                                                                │
│       ▼                                                                │
│  ⑦ _validate_against_scope(innovations, scope_guard)                   │
│       │   每个 innovation 的 evidence_ids ∈ allowed_evidence_ids      │
│       │   每个 innovation 的 paper_ids ∈ allowed_paper_ids            │
│       │   → scope_violations[]                                         │
│       │                                                                │
│       ▼                                                                │
│  ⑧ 保存 Report                                                         │
│       storage.upsert_item("reports", report)                           │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| generate | 8 步流水线：resolve → 4 信号源 → skeleton → LLM → 5 维评分 → 过滤 → 校验 → save |
| collect_signals | 4 来源信号收集（limitations/future_works/graph_gaps/explicit_gaps） |
| aggregate_limitations | 聚类共同 limitation（出现 ≥2 次的相似表述） |
| aggregate_future_works | 聚类 future_work（出现 ≥2 次的相似方向） |
| detect_graph_gaps | 从知识图谱 gap 节点提取信号 + **增强版**：调用 `find_research_gaps_enhanced()` 获取 method_dataset sparse pairs 和 sparse regions |
| _aggregate_gaps | **增强版**：跨论文聚合 future_work/possible_gap/limitation → Gap 节点（≥2 篇论文共指） |
| build_consensus_meter | **增强版**：立场分类后持久化 SUPPORTS/CONTRADICTS 边到图谱 |
| detect_explicit_gaps | _GAP_KEYWORDS（18 个）扫描 evidence 文本 |
| _build_candidate_skeletons | 构建候选骨架（max 5+5+3+3=16，dedup） |
| _score_innovation | 5 维评分（novelty 0.25 / evidence 0.25 / feasibility 0.20 / risk 0.10 / specificity 0.20） |
| _filter_generic_candidates | 20 个 _GENERIC_PHRASES 空泛检测过滤 |
| _validate_against_scope | scope 越界检查 + 来源校验 |

---

## 12-报告版本与导出

### 在系统中的角色

报告版本与导出是成果管理层。它统一承接综述和创新点报告的保存、版本管理、来源索引构建、traceability 校验和格式导出。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│               ReportService（388 行 / 14 方法）                        │
│                                                                        │
│  ──────────────── CRUD ────────────────                                │
│  save_report(project_id, report) → Report                              │
│    auto-fill section_sources = build_source_index(report)              │
│    auto-fill validation_result = validate_report_traceability(report)  │
│    storage.upsert_item("reports", report)                              │
│  list_reports(project_id, type?, status?) → Report[]                   │
│  get_report(report_id) → Report                                        │
│  update_status(report_id, status) → Report                             │
│  archive_report(report_id) → Report                                    │
│  delete_report(report_id) → None                                       │
│                                                                        │
│  ──────────────── 版本管理 ────────────────                            │
│  create_version(report_id) → ReportVersion                             │
│    snapshot 含 scope_snapshot + source_snapshot + content_snapshot      │
│    storage.upsert_item("report_versions", version)                     │
│  list_versions(report_id) → ReportVersion[]                            │
│  get_version(version_id) → ReportVersion                               │
│  restore_version(report_id, version_id) → Report                      │
│    自动先保存当前状态为新版本（auto-save before restore）              │
│    然后用 version.content_snapshot 覆盖 report                         │
│                                                                        │
│  ──────────────── 来源索引 ────────────────                            │
│  build_source_index(report) → SourceIndex                              │
│    paper_sources: {paper_id: {title, citation_count}}                  │
│    evidence_sources: {evidence_id: {paper_id, type, strength}}         │
│    section_sources: {section_title: [paper_ids]}                       │
│    从 report.sections[].content 中解析 [paper_id] 引用                 │
│                                                                        │
│  ──────────────── Traceability 校验 ────────────────                   │
│  validate_report_traceability(report) → ValidationResult               │
│    ├─ paper_exists: 所有引用 paper_id 在 storage 中存在               │
│    ├─ evidence_exists: 所有 evidence_id 在 storage 中存在             │
│    ├─ scope_violations: 引用是否在 scope 内                           │
│    ├─ section_coverage: 每个 section 是否有 ≥1 个来源                 │
│    └─ reference_resolution: 引用格式是否正确                           │
│    → missing_papers[], missing_evidence[], violations[], coverage      │
│                                                                        │
│  ──────────────── 导出 ────────────────                                │
│  export_markdown(report_id) → ExportResult                             │
│    content: Markdown 正文                                              │
│    source_index: 来源索引表                                            │
│    validation_summary: 校验结果摘要                                    │
│    format: "markdown"                                                  │
│    更新 report.export_format = "markdown"                              │
│  export_json(report_id) → ExportResult                                 │
│    content: {report, source_index, validation}                         │
│    format: "json"                                                      │
│    更新 report.export_format = "json"                                  │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 方法 | 功能 |
| --- | --- |
| save_report | 保存报告（auto-fill section_sources + validation_result） |
| list_reports | 项目内报告列表（支持 type / status 过滤） |
| get_report / update_status / archive_report / delete_report | 报告 CRUD |
| create_version | 版本快照（scope_snapshot + source_snapshot + content_snapshot） |
| list_versions / get_version | 版本查询 |
| restore_version | 恢复版本（自动先保存当前状态为新版本） |
| build_source_index | 构建来源索引（paper_sources + evidence_sources + section_sources） |
| validate_report_traceability | 校验 paper 存在性 + evidence 存在性 + scope 越界 + section 覆盖 + 引用解析 |
| export_markdown | Markdown 导出（正文 + 来源索引 + 校验摘要） |
| export_json | JSON 导出（report + source_index + validation） |

**DB 映射修复**（2026-06-05）：Report 模型字段与 `reports` 表 schema 不匹配（`type` vs `report_type`，`status`/`scope`/`paper_ids` 等无对应列）。已通过 `_report_to_db()` / `_db_to_report()` 转换函数解决：
- 写入：`type` → `report_type` 列，其余存入 `metadata` JSONB
- 读取：`report_type` → `type`，从 `metadata` 恢复 `status`/`scope`/`paper_ids`/`evidence_ids` 等
- 同步修复：review_generator、innovation_generator 的写入路径

---

## 13-LLM 提示词与结构化输出

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **Elicit** | 结构化提取 prompt（Research Question/Method/Finding/Limitation），每条带 source_quote | `PAPER_CARD_PROMPT` 13字段提取；`invoke_structured()` schema 校验 |
| **Microsoft GraphRAG** | LLM 实体+关系抽取 prompt；不预设 Schema 由 LLM 自动推断 | `KG_EXTRACTION_PROMPT` 实体/关系提取 |
| **Anthropic** | JSON 输出模式；结构化输出 schema 校验 | `invoke_json()` / `invoke_structured()` + `repair_json_with_llm()` |

### 在系统中的角色

LLM 服务层为所有生成模块提供统一的模型调用接口、JSON 提取与修复、schema 校验、prompt 版本管理和调用日志。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│                    llm/ 子包（5 文件 / 519 行）                        │
│                                                                        │
│  ──────────────── service.py（219 行）────────────────                 │
│  LLMService                                                            │
│    invoke(system_prompt, user_prompt) → str                            │
│      langchain ChatOpenAI/ChatAnthropic 调用                           │
│      自定义 httpx.Client(transport=retries=0) 绕过系统代理              │
│      记录 token 用量和延迟到 LLMCallResult                             │
│    invoke_json(system_prompt, user_prompt) → dict                      │
│      调用 invoke → extract_json(response)                              │
│    invoke_structured(system_prompt, user_prompt, schema) → dict        │
│      调用 invoke → extract_json → validate(schema)                     │
│      校验失败 → repair_json_with_llm(response)                         │
│      仍失败 → raise StructuredOutputError                              │
│    _record_call(system, user, response, tokens, latency)               │
│      写入 llm_calls[] 列表                                             │
│                                                                        │
│  FakeLLMService                                                        │
│    _responses: dict[str, str] — 预设响应映射                           │
│    _calls: list — 记录调用历史                                         │
│    invoke/invoke_json/invoke_structured 按顺序返回 _responses          │
│                                                                        │
│  LLMConfig                                                             │
│    provider: str = "openai"                                            │
│    model_name: str = "mimo-v2.5-pro"（从 LLM_MODEL 环境变量读取）       │
│    temperature: float = 0.2                                            │
│    max_tokens: int = 4096                                              │
│    timeout: float = 60.0                                               │
│    max_retries: int = 2                                                │
│                                                                        │
│  LLMCallResult                                                         │
│    response: str                                                       │
│    input_tokens: int                                                   │
│    output_tokens: int                                                  │
│    latency_ms: float                                                   │
│    model: str                                                          │
│                                                                        │
│  ──────────────── json_utils.py（91 行）────────────────               │
│  extract_json(text) → dict                                             │
│    4 种策略降级：                                                      │
│    ① json.loads(text) — 直接解析                                       │
│    ② 提取 ```json ... ``` code block                                   │
│    ③ 提取 { ... } brace 匹配（处理嵌套）                              │
│    ④ 提取 [ ... ] array 匹配                                          │
│    全部失败 → raise JsonExtractionError                                │
│                                                                        │
│  repair_json_with_llm(broken_text, schema) → dict                     │
│    用 LLM 修复格式错误的 JSON                                          │
│    修复后再次 validate(schema)                                         │
│                                                                        │
│  ──────────────── prompts.py（90 行）────────────────                  │
│  PromptRegistry                                                        │
│    _prompts: dict[str, PromptEntry] — 版本化 prompt 存储              │
│    register(name, template, version="v1")                              │
│    get(name, version?) → str                                           │
│    list_prompts() → dict                                               │
│                                                                        │
│  预定义 prompt：                                                       │
│    PAPER_CARD — 论文卡片生成                                           │
│    SCOPE_QA — 范围问答                                                 │
│    REVIEW — 综述生成                                                   │
│    INNOVATION — 创新点报告                                             │
│                                                                        │
│  ──────────────── logging.py（46 行）────────────────                  │
│  redact_text(text) → str                                               │
│    脱敏 API key、邮箱、完整 prompt                                     │
│  hash_text(text) → str                                                 │
│    SHA256 哈希（用于日志关联）                                         │
│  log_llm_call(call_result) → None                                     │
│    结构化日志记录（含脱敏）                                            │
│                                                                        │
│  ──────────────── errors.py（73 行）────────────────                   │
│  LLMServiceError (base)                                                │
│    ├─ ProviderError — 模型提供商错误                                   │
│    ├─ TimeoutError — 调用超时                                          │
│    ├─ RateLimitError — 限流                                            │
│    ├─ EmptyResponse — 空响应                                           │
│    ├─ JsonExtractionError — JSON 提取失败                              │
│    └─ StructuredOutputError — schema 校验失败                          │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 组件 | 能力 |
| --- | --- |
| LLMService | invoke（原始调用）/ invoke_json（JSON 提取）/ invoke_structured（schema 校验 + repair） |
| FakeLLMService | 测试用 mock（_responses 预设 + _calls 记录） |
| LLMConfig / LLMCallResult | 模型配置 + 调用结果（tokens/latency） |
| json_utils | 4 策略 JSON 提取（json.loads → code block → brace → array）+ LLM repair |
| PromptRegistry | 版本化 prompt 管理，4 个预定义 prompt（PAPER_CARD/SCOPE_QA/REVIEW/INNOVATION） |
| logging | redact_text 脱敏 + hash_text 哈希 + log_llm_call 结构化日志 |
| errors | 7 种错误类型（Provider/Timeout/RateLimit/Empty/JsonExtraction/StructuredOutput） |

---

## 14-API 与任务

### 在系统中的角色

API 层是前端与后端的集成面。它提供 28 个 RESTful 端点，覆盖项目、论文、解析、卡片、证据、图谱、Scope、QA、报告的全部操作，并通过 TaskService 管理异步任务。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│              FastAPI 应用（api/ 子包，路由已拆分为 7 模块）             │
│                                                                        │
│  ──────────────── app.py（~100 行）────────────────                    │
│  create_app() 工厂 + lifespan（启动时初始化 PG + Qdrant）             │
│  中间件：CORS + Request ID 注入（X-Request-ID）                       │
│  健康检查：GET /api/health                                             │
│  register_routers(app) 注册 7 个路由模块                               │
│                                                                        │
│  ──────────────── routes/（7 个路由模块）────────────────              │
│  routes/projects.py    — 项目 CRUD（4 端点）                          │
│  routes/papers.py      — 论文库（6 端点）                              │
│  routes/parsing.py     — PDF 解析（2 端点）                           │
│  routes/knowledge.py   — 卡片/证据/图谱（6 端点）                     │
│  routes/scope_qa.py    — Scope + QA（3 端点）                         │
│  routes/reports.py     — 报告生成/导出（5 端点）                      │
│  routes/tasks.py       — 任务查询（2 端点）                           │
│                                                                        │
│  ──────────────── deps.py（167 行）────────────────                   │
│  17 个依赖注入工厂函数：                                               │
│    get_storage / get_project_service / get_paper_library               │
│    get_parser_service / get_card_generator / get_evidence_table        │
│    get_graph_service / get_scope_service / get_scope_qa                │
│    get_review_generator / get_innovation_generator                     │
│    get_report_service / get_llm_service / get_task_service             │
│    get_vector_storage / get_search_orchestrator / get_evaluator        │
│    auto-detection: hasattr(storage, 'conn') → PG/JSON 选择           │
│                                                                        │
│  ──────────────── tasks.py（88 行）────────────────                   │
│  TaskService                                                           │
│    create_task(project_id, task_type) → Task                           │
│    get_task(task_id) → Task / list_tasks(project_id?, status?)        │
│    状态机：pending → running → succeeded / failed                      │
│                                                                        │
│  ──────────────── models.py（140 行）────────────────                 │
│  15 个 DTO 模型：ApiResponse / ProjectCreate/Update / PaperImport     │
│  ScopeResolveRequest / QARequest / ReportGenerateRequest 等           │
│                                                                        │
│  ──────────────── errors.py（85 行）────────────────                  │
│  5 种错误类型：NotFoundError(404) / ValidationError(400)              │
│  ConflictError(409) / ExternalServiceError(502) / InternalError(500)  │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 组件 | 能力 |
| --- | --- |
| 28 个 API 端点 | 项目(4)/论文(6)/解析(2)/卡片(1)/证据(1)/图谱(4)/Scope(2)/QA(1)/报告(5)/任务(2)/健康(1) |
| routes/ 路由拆分 | 7 个模块：projects/papers/parsing/knowledge/scope_qa/reports/tasks |
| TaskService | 异步任务管理（create/get/update/add_event/list，状态机 pending→running→succeeded/failed） |
| deps.py | 17 个依赖注入工厂函数 + auto-detection PG/JSON |
| errors.py | 5 种错误类型（404/400/409/502/500）+ 统一异常处理器 |
| models.py | 15 个 DTO（ApiResponse/ErrorResponse/ListResponse + 请求模型） |

---

## 15-评估日志监控

### 参考产品与技术来源

| 参考产品/技术 | 借鉴内容 | 对应代码 |
|--------------|---------|---------|
| **LettuceDetect** | 幻觉检测（hallucination detection）；事实核查 | `evaluator` 8 项检查中的引用验证 |
| **RAGAs / DeepEval** | RAG 评估指标体系（faithfulness / answer_relevancy / context_precision / context_recall） | 评估指标收集 |
| **Atlas** | 幻觉-验证比率（hallucination_ratio = 无来源断言数 / 总断言数） | 质量门禁中的引用覆盖率检查 |
| **GPT-Researcher** | Review-Revise 循环中的质量控制；CircuitBreaker 防级联错误 | `gates` 6 道质量门禁 |

### 在系统中的角色

评估日志监控是系统的质量底座。它提供 8 项自动检查、6 道质量门禁、golden test cases 和结构化日志，确保 QA、综述、创新点报告的质量可量化、可回归。

### 模块架构

```text
┌────────────────────────────────────────────────────────────────────────┐
│                  evaluation/ 子包（5 文件 / 755 行）                    │
│                                                                        │
│  ──────────────── evaluator.py（318 行）────────────────               │
│  8 项检查函数：                                                        │
│                                                                        │
│  scope_guard_check(response, scope_guard) → CheckResult                │
│    检查 response 中每个 citation.paper_id ∈ allowed_paper_ids         │
│    → pass/fail + violations[]                                          │
│                                                                        │
│  citation_coverage_check(response, evidence_list) → CheckResult        │
│    citation_count / evidence_count ≥ 阈值（默认 0.5）                  │
│    → pass/fail + coverage_ratio                                        │
│                                                                        │
│  report_traceability_check(report, storage) → CheckResult              │
│    检查 report 中每个 paper_id / evidence_id 在 storage 中存在        │
│    → pass/fail + missing_papers[] + missing_evidence[]                 │
│                                                                        │
│  innovation_specificity_check(innovations) → CheckResult               │
│    检查每个 innovation 是否包含具体方法/数据/场景                      │
│    非 "进一步研究" "优化模型" 等空泛表达                               │
│    → pass/fail + specificity_ratio                                     │
│                                                                        │
│  generic_text_check(text, phrases=None) → CheckResult                  │
│    默认 _GENERIC_PHRASES 检测                                          │
│    → pass/fail + matched_phrases[]                                     │
│                                                                        │
│  redaction_check(text) → CheckResult                                   │
│    检查是否包含未脱敏的 API key、邮箱、prompt                          │
│    → pass/fail + leaked_patterns[]                                     │
│                                                                        │
│  e2e_smoke_check(project_id, storage) → CheckResult                    │
│    端到端冒烟：项目 → 论文 → 卡片 → 证据 → 报告                       │
│    → pass/fail + missing_steps[]                                       │
│                                                                        │
│  refusal_correctness_check(qa_response, scope_guard) → CheckResult     │
│    scope 外问题是否正确拒答                                            │
│    → pass/fail + incorrect_answers[]                                    │
│                                                                        │
│  ──────────────── gates.py（97 行）────────────────                    │
│  6 道质量门禁（阈值）：                                                │
│    scope_guard_pass_rate == 1.0                                        │
│    citation_coverage ≥ 0.50                                            │
│    report_traceability ≥ 0.80                                          │
│    innovation_specificity ≥ 0.70                                       │
│    generic_filter_pass ≥ 0.90                                          │
│    redaction_check_pass == true                                        │
│  evaluate_gates(eval_results) → GateResult[]                           │
│    每个 gate 返回 name/threshold/actual/pass                           │
│                                                                        │
│  ──────────────── golden.py（113 行）────────────────                  │
│  4 个预设 golden test cases：                                          │
│    qa_limitation — QA 应返回 limitation 分析                           │
│    qa_empty_scope_refuse — 空 scope 应拒答                             │
│    review_has_sections — 综述应有 5 个必选章节                         │
│    innovation_not_generic — 创新点不应空泛                              │
│  run_golden_tests(services) → GoldenResult[]                           │
│    每个 case 返回 pass/fail + details                                  │
│                                                                        │
│  ──────────────── metrics.py（86 行）────────────────                  │
│  MetricsCollector                                                      │
│    record(name, value, tags={}) — 记录单条指标                         │
│    summarize() → dict — 按 name 汇总 count/sum/avg/min/max            │
│    write_jsonl(path) — 写入 JSONL 文件                                 │
│    _metrics: list[MetricEntry] — 内存存储                              │
│                                                                        │
│  ──────────────── logging_utils.py（141 行）────────────────           │
│  bind_context(request_id?, task_id?, project_id?)                      │
│    绑定到 structlog contextvars                                       │
│  log_event(event_type, message, **extra)                               │
│    结构化事件日志                                                      │
│  log_operation(operation_name) → context manager                       │
│    自动记录 start/end/duration_ms                                      │
│  redact_text(text) → str                                               │
│    5 种脱敏模式：API key / email / bearer token / password / prompt    │
│  hash_text(text) → str — SHA256                                        │
│  truncate_text(text, max_len=200) → str                                │
│  sanitize_payload(payload, keys_to_redact) → dict                      │
└────────────────────────────────────────────────────────────────────────┘
```

### 已实现能力

| 组件 | 能力 |
| --- | --- |
| evaluator | 8 项检查函数：scope_guard / citation_coverage / report_traceability / innovation_specificity / generic_text / redaction / e2e_smoke / refusal_correctness |
| gates | 6 道质量门禁（scope_guard==1.0, citation≥0.5, traceability≥0.8, specificity≥0.7, generic≥0.9, redaction==true） |
| golden | 4 个预设 golden test cases（qa_limitation / qa_empty_scope / review_sections / innovation_not_generic） |
| metrics | MetricsCollector（record/summarize/write_jsonl） |
| logging_utils | bind_context 上下文绑定 + log_event 结构化日志 + log_operation 耗时 + redact_text 5 种脱敏 + hash/truncate/sanitize |

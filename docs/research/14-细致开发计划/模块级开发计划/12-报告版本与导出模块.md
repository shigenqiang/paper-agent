# 报告版本与导出模块专项开发报告

更新时间：2026-05-28

## 实现状态

P0 全部完成（2026-05-28）。

### 已完成

```text
Report 模型增强: status/section_sources/validation_result/exported_formats/metadata ✅
ReportVersion 增强: version_number/scope_snapshot/source_snapshot/paper_ids/evidence_ids ✅
save_report: 自动提取 section_sources/validation_result ✅
list_reports: 支持 status 过滤 ✅
update_report_status / archive_report / delete_report ✅
create_version: 含 scope_snapshot 和 source_snapshot ✅
list_versions / get_version / restore_version ✅
build_source_index: paper_sources + evidence_sources + section_sources ✅
validate_report_traceability: 缺失/越界/覆盖率 ✅
export_markdown: 含来源索引和校验结果 ✅
export_json: 完整 JSON 导出 ✅
exported_formats 追踪 ✅
```

### 新增模型字段

- `Report`: `status`, `section_sources`, `validation_result`, `exported_formats`, `metadata`
- `ReportVersion`: `version_number`, `scope_snapshot`, `source_snapshot`, `paper_ids`, `evidence_ids`, `validation_result`

报告版本与导出模块负责把综述报告、创新点报告以及后续选题方案转化为可管理、可追溯、可迭代、可校验、可导出的成果物。它不是简单把 Markdown 字符串保存到 JSON 文件，而是成果管理层：统一承接报告生成模块输出的内容、Scope、来源索引、质量校验、版本快照和导出格式。

对应代码：

```text
src/agents_v3/research_workspace/report_service.py
src/agents_v3/research_workspace/models.py
src/agents_v3/research_workspace/storage.py
tests/agents_v3/research_workspace/test_report_service.py
```

建议新增或拆分：

```text
src/agents_v3/research_workspace/report_sources.py
src/agents_v3/research_workspace/report_validation.py
src/agents_v3/research_workspace/report_export.py
src/agents_v3/research_workspace/report_versions.py
src/agents_v3/research_workspace/citation_formatter.py
tests/agents_v3/research_workspace/test_report_traceability.py
tests/agents_v3/research_workspace/test_report_export_markdown.py
tests/agents_v3/research_workspace/test_report_export_json.py
tests/agents_v3/research_workspace/test_report_versions.py
tests/agents_v3/research_workspace/test_citation_formatter.py
```

## 1. 模块定位

### 1.1 在产品链路中的位置

报告版本与导出模块位于所有生成模块之后，是研究工作区的成果层：

```text
Paper Search / PDF Parse
  -> PaperCard
  -> EvidenceRecord
  -> KnowledgeGraph
  -> RetrievalScope
  -> ScopeQA
  -> Literature Review / Innovation Report
  -> ReportService
  -> Version / Validation / Export
  -> Frontend Report Workspace
```

它要承接：

| 上游模块 | 提供内容 | 本模块职责 |
| --- | --- | --- |
| 08 RetrievalScope | scope、paper_ids、evidence_ids、graph_node_ids | 保存范围快照，校验来源是否越界 |
| 09 ScopeQA/RAG | 可保存为 report material 的 QA 回答 | 作为报告草稿素材或附录 |
| 10 综述生成 | review content、section_sources、validation_result | 保存、版本化、导出综述 |
| 11 创新点报告 | innovation content、candidate sources、scores | 保存、版本化、导出创新报告 |
| 13 LLM 结构化输出 | prompt/schema 版本、模型调用信息 | 写入生成元数据，便于复现 |
| 15 评估日志 | report_traceability_check、质量指标 | 作为保存前后质量门禁 |
| 14 API/前端 | 报告列表、详情、版本、导出接口 | 提供稳定响应结构 |

### 1.2 模块边界

本模块负责：

```text
报告保存
报告列表和详情
报告更新
版本创建、列表、恢复和对比
来源索引构建
Traceability 校验
Markdown 导出
JSON 导出
引用格式化
Evidence appendix 导出
归档和删除
前端报告页数据契约
```

本模块不负责：

```text
生成综述正文
生成创新点正文
重新检索论文
重新解析 PDF
判断 LLM 输出是否学术上完全正确
复杂 Word/PDF 排版
多人协作权限系统
```

### 1.3 核心原则

```text
1. 报告是成果物，不只是 content 字符串。
2. 每份报告必须保留 scope snapshot。
3. 每份报告必须能追溯到 paper、evidence、graph node。
4. 版本必须记录内容变化和来源快照。
5. 导出必须包含元信息、正文和来源索引。
6. 保存前可以宽松，最终导出前必须可校验。
7. 综述和创新点报告共用同一套报告基础设施。
8. 当前 JSONStorage 是 MVP 存储，模型设计要能迁移到数据库。
```

## 2. 本地调研结论落地

### 2.1 模块总览对本模块的要求

`00-模块总览与依赖关系.md` 明确报告层需要：

```text
section_sources
traceability
export
version
```

落地为：

| 要求 | 开发落点 |
| --- | --- |
| section_sources | `ReportSourceIndex` 和 `SectionSource` |
| traceability | `ReportTraceabilityValidator` |
| export | `MarkdownExporter`、`JsonExporter`、`ExportPackage` |
| version | `ReportVersionManager`、`ReportVersion` 增强 |

### 2.2 综述生成模块对本模块的要求

10 综述生成模块要求每个主要章节能追溯到证据：

```text
Review sections
  -> section_id
  -> paper_ids
  -> evidence_ids
  -> graph_node_ids
  -> citations
```

本模块必须提供：

```text
save_report(report, source_index, validation_result)
export_markdown(report_id, include_sources=True)
validate_report_traceability(report_id)
create_version(report_id, content, source_index, reason)
```

综述模块不应自己拼接最终导出来源索引，而应把结构化来源交给 ReportService。

### 2.3 创新点报告模块对本模块的要求

11 创新点报告模块强调：

```text
source_signal_ids
supporting_evidence_ids
limiting_evidence_ids
scores
validation_result
```

本模块需要兼容创新报告的来源索引：

```text
candidate_sources
gap_signals
supporting_evidence
limiting_evidence
graph_node_ids
scoring_version
```

这些暂时可放在：

```text
Report.scope.innovation_metadata
ReportSourceIndex.sections
ReportSourceIndex.custom
```

后续模型增强后再迁移到正式 metadata 字段。

### 2.4 RetrievalScope 对本模块的要求

08 RetrievalScope 模块要求报告不能越界引用：

```text
report.paper_ids subset resolved_scope.paper_ids
report.evidence_ids subset resolved_scope.evidence_ids
report.graph_node_ids subset resolved_scope.graph_node_ids
```

现实中可能存在例外：

```text
系统综述报告引用筛选流程中的 excluded paper。
参考文献列表展示未纳入但被引用的背景文献。
用户手动添加外部参考。
```

因此 traceability 校验需要支持策略：

```text
strict_scope=true：越界即 error。
allow_external_references=true：外部来源必须出现在 external_sources，不能混入 evidence_ids。
appendix_only=true：附录材料可不进入正文证据。
```

### 2.5 评估日志模块对本模块的要求

15 评估日志监控模块已经定义：

```text
report_traceability_rate
section_source_coverage
structure_completeness
scope_guard_pass_rate
```

本模块需要产生可计算数据：

```text
report.saved
report.version.created
report.validated
report.exported
report.archived
report.deleted
```

并在 validation result 中提供：

```text
source_coverage
section_source_coverage
scope_violation_count
missing_source_count
unresolved_reference_count
```

### 2.6 API 和前端模块对本模块的要求

14 API/前端联调模块要求成果报告页支持：

```text
报告列表
报告详情
版本列表
正文预览
来源索引
质量校验结果
导出按钮
```

因此 ReportService 不能只返回 Markdown 字符串。它需要能返回前端可直接渲染的结构：

```text
ReportDetailDTO
ReportVersionDTO
ReportSourceIndexDTO
ReportValidationResultDTO
ExportResultDTO
```

## 3. 主流产品和技术做法借鉴

### 3.1 文档产品：Notion / Google Docs

可借鉴：

```text
版本历史
最近更新时间
恢复旧版本
导出 Markdown/PDF/DOCX
文档状态 draft/final/archived
```

落地到本项目：

```text
Report.status
Report.version
ReportVersion.version_number
ReportVersion.reason
restore_version(report_id, version_id)
archive_report(report_id)
```

### 3.2 Git / diff 工作流

可借鉴：

```text
每次变更都有快照。
变更有 reason/message。
可查看 diff。
可恢复历史。
```

落地到本项目：

```text
diff_summary
content_hash
source_snapshot_hash
version_number
changed_sections
```

P0 只做轻量 diff：

```text
字符增删
章节标题变化
来源 ID 变化
```

P1 再做段落级 diff。

### 3.3 Pandoc / Quarto 导出链路

Markdown 是适合作为中间格式的成果文档。MVP 应先保证 Markdown 完整、干净、可复现，再考虑 DOCX/PDF。

落地策略：

```text
P0: export_markdown + export_json
P1: export_markdown_package，包含 markdown、metadata、source_index、references
P2: export_docx/export_pdf，内部可走 Pandoc 或 Python 库
```

### 3.4 Zotero / CSL 引用体系

引用和正文应分离。报告正文里可以使用简短 citation key，导出阶段根据 style 生成参考文献。

MVP 支持：

```text
inline citation: [paper_1]
reference list: Authors. (Year). Title. Venue. DOI/URL.
styles: simple / apa / gbt7714 / bibtex
```

P0 不强求完整 CSL 引擎，但要预留 `citation_style` 参数。

### 3.5 系统综述工具

系统综述的成果输出不只是正文，还包括：

```text
纳入论文列表
排除原因
证据表
质量评价
检索策略
PRISMA flow
```

本项目 P0 先做通用来源索引，P1 支持：

```text
evidence appendix
paper appendix
search strategy appendix
quality assessment appendix
```

## 4. 当前实现分析

### 4.1 当前 ReportService 能力

当前 `ReportService` 方法：

```text
save_report(report)
list_reports(project_id, report_type=None)
get_report(report_id)
create_version(report_id, content, reason)
export_markdown(report_id)
```

已有能力：

| 能力 | 当前状态 |
| --- | --- |
| 保存报告 | `storage.upsert_item("reports", ...)` |
| 查询单个报告 | `get_report()` |
| 项目内报告列表 | `list_reports(project_id)` |
| 类型过滤 | `report_type` 字符串过滤 |
| 创建版本 | 写入 `report_versions` 后更新 report.content/version |
| Markdown 导出 | 返回 report.content + 基础元信息 |

### 4.2 当前模型状态

`Report` 当前字段：

```text
report_id
project_id
type
title
content
scope
paper_ids
evidence_ids
graph_node_ids
created_at
updated_at
version
```

`ReportVersion` 当前字段：

```text
version_id
report_id
content
reason
created_at
```

`ReportType` 当前类型：

```text
literature_review
innovation_report
```

### 4.3 当前存储能力

`JSONStorage` 支持：

```text
load_collection
save_collection
upsert_item
get_item
delete_item
query
```

集合映射已经包含：

```text
reports -> report_id
report_versions -> version_id
papers -> paper_id
evidence_records -> evidence_id
graphs -> graph_id/project_id 存在重复映射问题
```

注意：

```text
storage._guess_id_field 中 graphs 出现两次，后一个 graph_id 会覆盖前一个 project_id。
如果图谱集合当前按 project_id 存储，需要在图谱校验时确认真实数据结构。
```

### 4.4 当前测试覆盖

`test_report_service.py` 当前覆盖：

```text
保存后可查询。
项目内报告列表。
按类型过滤。
不存在报告返回 None。
创建版本保存 content。
创建版本后 Report.version 递增。
导出 Markdown 包含 content 和基础元信息。
不存在报告导出空字符串。
```

### 4.5 当前关键缺口

| 问题 | 当前表现 | 影响 | 优先级 |
| --- | --- | --- | --- |
| ReportVersion 信息太少 | 只有 content/reason | 不能知道第几版、来源是否变化 | P0 |
| 版本创建语义不完整 | version.content 是新内容，但没有 old snapshot | 无法对比和恢复 | P0 |
| 无 source index | 只有报告级 paper_ids/evidence_ids | 前端不能展示章节来源 | P0 |
| 无 traceability 校验 | 保存和导出前不检查来源 | 可能导出缺失或越界引用 | P0 |
| Markdown 导出过简单 | 元信息附在文末 | 不利于机器读取和二次转换 | P0 |
| 无 JSON 导出 | 无结构化成果包 | 前端/调试/迁移不方便 | P0 |
| 无归档状态 | list_reports 无法隐藏旧报告 | 报告列表会混乱 | P1 |
| 无删除策略 | 只能 storage.delete_item，未定义版本处理 | 容易留下孤儿版本 | P1 |
| 无引用格式化 | 不能输出参考文献 | 综述导出不完整 | P1 |
| 无导出选项 | 不能控制是否包含 sources/appendix | 不同场景复用差 | P1 |
| 无导出文件管理 | export_markdown 只返回字符串 | API 下载和导出历史缺基础 | P1 |
| 无恢复版本 | create_version 后不能 restore | 版本历史价值不足 | P1 |
| 无 diff | 不能查看改动摘要 | 用户难以判断版本差异 | P1 |
| 无并发/覆盖保护 | 直接覆盖 report | 前端编辑可能丢内容 | P2 |

## 5. 目标与边界

### 5.1 一句话目标

让综述和创新点报告都能以统一方式保存、版本化、校验来源、导出 Markdown/JSON，并在前端展示版本、来源索引和质量状态。

### 5.2 MVP 成功标准

```text
1. save_report 支持保存 validation_result 和 source_index。
2. create_version 生成 version_number、diff_summary、scope_snapshot、source_snapshot。
3. list_reports 支持 report_type、status、include_archived 过滤。
4. validate_report_traceability 能发现缺失 paper/evidence、scope 越界和正文引用不可解析。
5. export_markdown 输出 YAML front matter。
6. export_markdown 包含来源索引和可选 evidence appendix。
7. export_json 输出 report、versions、source_index、validation、export_metadata。
8. archive_report 和 delete_report 行为明确。
9. 综述报告和创新点报告都能通过同一逻辑导出。
10. 单元测试覆盖版本、导出、校验、归档和删除。
```

### 5.3 非目标

MVP 不做：

```text
1. 完整多人协同编辑。
2. 实时评论和批注。
3. 完整 DOCX/PDF 排版。
4. 完整 CSL 引擎。
5. 云端文件管理。
6. 权限和审计合规系统。
7. 复杂三方文献管理器同步。
```

P2 可扩展：

```text
Pandoc DOCX/PDF
CSL style engine
comment/review workflow
export history files
report templates
database migration
```

## 6. 目标架构

### 6.1 服务拆分

```text
ReportService
  对外门面，负责保存、查询、版本、导出、归档。

ReportVersionManager
  负责 version_number、diff_summary、snapshot、restore。

ReportSourceIndexBuilder
  从 Report、section_sources、storage 中构建来源索引。

ReportTraceabilityValidator
  校验 project、scope、paper、evidence、graph node、正文引用。

MarkdownReportExporter
  生成带 YAML front matter、正文、来源索引、附录的 Markdown。

JsonReportExporter
  生成结构化 ExportPackage。

CitationFormatter
  根据 paper 元信息生成 simple/apa/gbt7714/bibtex 引用。

ReportArchiveManager
  处理 archive、unarchive、soft delete、hard delete。
```

### 6.2 推荐调用链

保存报告：

```text
generator output
  -> ReportService.save_report(report, source_index, validation_result)
  -> normalize report metadata
  -> validate basic integrity
  -> persist report
  -> persist source_index if standalone collection is enabled
  -> log report.saved
```

更新报告：

```text
new content / user edit / regeneration
  -> ReportService.create_version(report_id, content, reason, source_index)
  -> load current report
  -> build diff_summary
  -> create ReportVersion snapshot
  -> update Report.content/version/updated_at/source metadata
  -> validate
  -> log report.version.created
```

导出报告：

```text
export request
  -> get report
  -> validate or load validation_result
  -> build source index
  -> format citations
  -> render markdown/json
  -> return ExportPackage
  -> log report.exported
```

### 6.3 依赖方向

```text
report_service.py
  -> models.py
  -> storage.py
  -> report_sources.py
  -> report_validation.py
  -> report_versions.py
  -> report_export.py
  -> citation_formatter.py
```

导出模块可以依赖 source index 和 citation formatter，但不要反向调用综述生成或创新点生成。

## 7. 数据模型设计

### 7.1 Report 增强

当前模型可逐步增强：

```python
class Report(BaseModel):
    report_id: str
    project_id: str
    type: ReportType
    title: str = ""
    content: str = ""
    scope: dict[str, Any] = Field(default_factory=dict)
    paper_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    graph_node_ids: list[str] = Field(default_factory=list)
    status: str = "draft"
    section_sources: dict[str, Any] = Field(default_factory=dict)
    validation_result: dict[str, Any] = Field(default_factory=dict)
    exported_formats: list[str] = Field(default_factory=list)
    created_by: str = ""
    updated_by: str = ""
    metadata: dict[str, Any] = Field(default_factory=dict)
    created_at: str = Field(default_factory=lambda: datetime.now().isoformat())
    updated_at: str = Field(default_factory=lambda: datetime.now().isoformat())
    version: int = 1
```

兼容策略：

```text
P0 如果暂不修改 Report 模型，可把新增字段放在 Report.scope.report_metadata。
P1 再正式迁移到 Report.metadata、section_sources、validation_result。
```

### 7.2 ReportVersion 增强

```python
class ReportVersion(BaseModel):
    version_id: str
    report_id: str
    version_number: int
    title: str = ""
    content: str
    reason: str = ""
    diff_summary: str = ""
    previous_content_hash: str = ""
    content_hash: str = ""
    scope_snapshot: dict[str, Any] = Field(default_factory=dict)
    source_snapshot: dict[str, Any] = Field(default_factory=dict)
    paper_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    graph_node_ids: list[str] = Field(default_factory=list)
    validation_result: dict[str, Any] = Field(default_factory=dict)
    created_by: str = ""
    created_at: str = Field(default_factory=lambda: datetime.now().isoformat())
```

版本快照要回答：

```text
这一版正文是什么。
这一版对应哪个 scope。
这一版引用了哪些来源。
这一版相对上一版改了什么。
这一版质量校验是否通过。
```

### 7.3 ReportSourceIndex

推荐新增内部模型：

```python
class ReportSourceIndex(BaseModel):
    report_id: str
    project_id: str
    paper_sources: list[dict[str, Any]] = Field(default_factory=list)
    evidence_sources: list[dict[str, Any]] = Field(default_factory=list)
    graph_sources: list[dict[str, Any]] = Field(default_factory=list)
    section_sources: dict[str, dict[str, Any]] = Field(default_factory=dict)
    external_sources: list[dict[str, Any]] = Field(default_factory=list)
    custom: dict[str, Any] = Field(default_factory=dict)
    created_at: str = Field(default_factory=lambda: datetime.now().isoformat())
```

`paper_sources` 示例：

```json
{
  "paper_id": "p1",
  "title": "Paper title",
  "authors": ["A", "B"],
  "year": 2024,
  "venue": "Journal",
  "doi": "10.xxxx/xxxx",
  "url": "https://...",
  "citation_key": "p1",
  "used_in_sections": ["intro", "methods"]
}
```

`evidence_sources` 示例：

```json
{
  "evidence_id": "e1",
  "paper_id": "p1",
  "topic": "feedback",
  "claim": "个性化反馈提升学习参与度",
  "evidence_type": "finding",
  "evidence_strength": "medium",
  "source_chunk_id": "chunk_1",
  "source_quote": "...",
  "used_in_sections": ["findings"]
}
```

### 7.4 SectionSource

```python
class SectionSource(BaseModel):
    section_id: str
    section_title: str = ""
    paper_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    graph_node_ids: list[str] = Field(default_factory=list)
    citation_keys: list[str] = Field(default_factory=list)
    source_quotes: list[str] = Field(default_factory=list)
    confidence: float = 0.0
```

section_id 生成建议：

```text
normalize heading -> slug
重复 slug 后缀 _2/_3
如果生成模块已经给 section_id，优先使用生成模块提供的稳定 ID
```

### 7.5 ReportValidationResult

```python
class ReportValidationResult(BaseModel):
    report_id: str
    project_id: str
    valid: bool
    score: float = 0.0
    issues: list[dict[str, Any]] = Field(default_factory=list)
    warnings: list[dict[str, Any]] = Field(default_factory=list)
    missing_paper_ids: list[str] = Field(default_factory=list)
    missing_evidence_ids: list[str] = Field(default_factory=list)
    missing_graph_node_ids: list[str] = Field(default_factory=list)
    scope_violations: list[dict[str, Any]] = Field(default_factory=list)
    unresolved_references: list[str] = Field(default_factory=list)
    source_coverage: float = 0.0
    section_source_coverage: float = 0.0
    checked_at: str = Field(default_factory=lambda: datetime.now().isoformat())
```

issue 结构：

```json
{
  "code": "missing_evidence",
  "severity": "error",
  "message": "evidence_id e9 does not exist",
  "location": "report.evidence_ids",
  "source_id": "e9"
}
```

### 7.6 ExportOptions

```python
class ExportOptions(BaseModel):
    format: str = "markdown"
    citation_style: str = "simple"
    include_front_matter: bool = True
    include_source_index: bool = True
    include_evidence_appendix: bool = False
    include_graph_appendix: bool = False
    include_validation_result: bool = True
    include_versions: bool = False
    strict_validation: bool = False
    quote_max_chars: int = 240
```

### 7.7 ExportPackage

```python
class ExportPackage(BaseModel):
    report_id: str
    project_id: str
    format: str
    filename: str
    content: str = ""
    content_type: str = ""
    metadata: dict[str, Any] = Field(default_factory=dict)
    validation_result: dict[str, Any] = Field(default_factory=dict)
    source_index: dict[str, Any] = Field(default_factory=dict)
    created_at: str = Field(default_factory=lambda: datetime.now().isoformat())
```

P0 API 可以直接返回 `content`。P1 如果写文件，再增加：

```text
file_path
size_bytes
content_hash
expires_at
```

## 8. 来源索引设计

### 8.1 来源索引的目标

来源索引用来回答：

```text
报告用了哪些论文？
每条证据来自哪篇论文？
每个章节用了哪些来源？
正文中出现的引用 ID 是否都能解析？
导出时如何生成参考文献？
前端点击来源时如何定位到 evidence/source_quote？
```

### 8.2 来源层级

```text
L1 Report-level source
  报告整体 paper_ids/evidence_ids/graph_node_ids。

L2 Section-level source
  每个章节绑定 paper/evidence/graph。

L3 Claim-level source
  P2 支持句子或 claim 级 citation。

L4 Source quote / chunk
  精确到 PDF chunk 和 quote。
```

P0 至少实现 L1 + L2。

### 8.3 section_sources 输入契约

综述和创新报告生成模块应传入：

```json
{
  "intro": {
    "section_title": "研究背景",
    "paper_ids": ["p1", "p2"],
    "evidence_ids": ["e1", "e2"],
    "graph_node_ids": ["topic_feedback"]
  },
  "innovation_1": {
    "section_title": "创新点一",
    "paper_ids": ["p3"],
    "evidence_ids": ["e5", "e6"],
    "graph_node_ids": ["gap_external_validity"]
  }
}
```

如果生成模块暂时没有 section_sources：

```text
ReportSourceIndexBuilder 可以把 report.paper_ids/evidence_ids 作为 __report__ section。
validation_result 增加 warning: missing_section_sources。
```

### 8.4 来源索引构建规则

```text
1. 从 report.paper_ids 加载 papers。
2. 从 report.evidence_ids 加载 evidence_records。
3. 从 report.graph_node_ids 加载 graph nodes。
4. 合并 section_sources 中额外提到的 ID。
5. 校验 ID 是否存在、是否属于 project。
6. 生成 used_in_sections 反向索引。
7. 生成 citation_key。
8. 截断 source_quote，避免导出过长。
```

### 8.5 source_quote 策略

默认导出：

```text
quote_max_chars = 240
超过长度截断。
Markdown 中只显示短 quote。
JSON 导出可保留完整 quote 或设置 include_full_quotes=true。
```

原因：

```text
导出 Markdown 需要可读。
避免把大量 PDF 原文塞入报告正文。
降低版权和隐私风险。
```

## 9. Traceability 校验

### 9.1 校验入口

```python
validate_report_traceability(
    report_id: str,
    *,
    strict_scope: bool = True,
    allow_external_references: bool = False,
) -> ReportValidationResult
```

也支持：

```python
validate_report(report: Report, source_index: ReportSourceIndex | None = None) -> ReportValidationResult
```

### 9.2 基础校验

```text
Report 存在。
Project 存在。
Report.project_id 等于 Project.project_id。
Report.type 是支持类型。
Report.title 非空。
Report.content 非空。
Report.version >= 1。
```

### 9.3 来源存在性校验

```text
paper_ids 都能在 papers 集合中找到。
paper.project_id == report.project_id。
evidence_ids 都能在 evidence_records 集合中找到。
evidence.project_id == report.project_id。
evidence.paper_id 对应 paper 存在。
graph_node_ids 存在于项目图谱。
section_sources 中的 ID 也必须通过上述校验。
```

### 9.4 Scope 校验

如果 `report.scope` 中存在：

```text
paper_ids
evidence_ids
graph_node_ids
```

则检查：

```text
report.paper_ids subset scope.paper_ids
report.evidence_ids subset scope.evidence_ids
report.graph_node_ids subset scope.graph_node_ids
section_sources.paper_ids subset scope.paper_ids
section_sources.evidence_ids subset scope.evidence_ids
```

例外：

```text
external_sources 中声明的外部引用不算 scope violation。
appendix_only 的材料不进入正文覆盖率，但仍需存在。
```

### 9.5 正文引用校验

P0 支持简单模式：

```text
[paper_id]
[evidence_id]
paper_...
ev_...
e...
p...
```

校验：

```text
正文中出现的 paper/evidence 引用是否在 ReportSourceIndex 中。
Report.evidence_ids 中存在但正文和 section_sources 都不用的 ID 给 warning。
正文出现无法解析的引用给 warning 或 error。
```

注意：

```text
不要只靠正文正则判断来源。
结构化 section_sources 才是主依据。
正文引用校验是补充防线。
```

### 9.6 覆盖率计算

```text
source_coverage =
  referenced_source_count / required_source_count

section_source_coverage =
  sections_with_sources / sections_total

traceability_score =
  0.4 * source_coverage
+ 0.3 * section_source_coverage
+ 0.2 * scope_pass
+ 0.1 * no_unresolved_references
```

默认门禁：

```text
traceability_score >= 0.8
scope_violation_count == 0
missing_evidence_count == 0
```

## 10. 版本策略

### 10.1 版本创建语义

当前 `create_version(report_id, content, reason)` 的语义是“创建一个新版本并更新当前报告”。建议保留这个行为，但补齐快照。

推荐流程：

```text
1. 读取当前 Report。
2. previous_content = report.content。
3. next_version_number = report.version + 1。
4. 计算 previous_content_hash 和 content_hash。
5. 生成 diff_summary。
6. 保存 ReportVersion，内容为新版本 content。
7. 更新 Report.content、Report.version、Report.updated_at。
8. 更新 source_index 和 validation_result。
```

### 10.2 是否保存旧版本

为了能恢复最初版本，需要两种策略之一：

方案 A：

```text
save_report 时自动创建 version 1。
create_version 创建 version 2、3、4。
```

方案 B：

```text
Report 当前内容是 latest。
ReportVersion 只保存历史版本。
```

推荐方案 A。原因：

```text
版本列表完整。
恢复 version 1 简单。
导出历史版本简单。
```

### 10.3 diff_summary

P0 规则：

```text
old_length
new_length
added_chars
removed_chars
old_headings
new_headings
added_headings
removed_headings
paper_ids_added
paper_ids_removed
evidence_ids_added
evidence_ids_removed
reason
```

示例：

```json
{
  "summary": "正文增加 312 字，新增 1 个章节，新增 2 条证据引用。",
  "added_chars": 312,
  "removed_chars": 44,
  "added_headings": ["研究趋势"],
  "removed_headings": [],
  "evidence_ids_added": ["e9", "e10"],
  "evidence_ids_removed": []
}
```

P1 可用 `difflib` 生成段落级差异：

```text
changed_paragraphs
changed_sections
unified_diff
html_diff
```

### 10.4 恢复版本

```python
restore_version(report_id: str, version_id: str, reason: str = "restore") -> Report | None
```

行为：

```text
1. 读取目标版本。
2. 调用 create_version，把目标版本 content/source_snapshot 作为新 latest。
3. reason 标记 restore。
4. 不直接把 Report.version 回退，避免版本号倒退。
```

### 10.5 版本列表

```python
list_versions(report_id: str) -> list[ReportVersion]
```

排序：

```text
version_number desc
created_at desc
```

前端展示：

```text
version_number
created_at
reason
diff_summary.summary
validation_result.valid
```

### 10.6 并发和覆盖保护

P0 可以不实现复杂并发，但 API 应预留：

```text
expected_version
```

更新时：

```text
如果 expected_version != report.version，返回 version_conflict。
```

P1 实现：

```python
create_version(report_id, content, reason, expected_version=3)
```

## 11. 导出设计

### 11.1 Markdown 导出

默认输出结构：

```markdown
---
report_id: report_xxx
project_id: proj_xxx
type: literature_review
title: "文献综述"
version: 2
status: draft
scope_summary: "用户选择的 12 篇论文"
paper_count: 12
evidence_count: 36
graph_node_count: 18
citation_style: simple
validated: true
created_at: "2026-05-28T10:00:00"
updated_at: "2026-05-28T10:30:00"
---

# 文献综述

正文...

## 来源索引

### 使用论文

- [p1] Author A, Author B. (2024). Title. Venue. DOI/URL

### 使用证据

- [e1] p1 | finding | 个性化反馈提升学习参与度。 | chunk_1

### 使用图谱节点

- [topic_feedback] Topic: 智能反馈

## 质量校验

- traceability: pass
- source_coverage: 0.92
```

### 11.2 YAML front matter

YAML front matter 只放短元数据：

```text
report_id
project_id
type
title
version
status
scope_summary
paper_count
evidence_count
graph_node_count
citation_style
validated
created_at
updated_at
```

不要把完整 source_index 放入 YAML。完整来源索引放正文末尾或 JSON 包。

### 11.3 来源索引渲染

论文条目：

```text
- [p1] Zhang, L.; Wang, H. (2024). Intelligent feedback in learning. Journal. DOI: ...
```

证据条目：

```text
- [e1] p1 | finding | evidence_strength=medium | quote: "..."
```

图谱节点：

```text
- [topic_feedback] Topic | 智能反馈 | degree=5
```

如果缺来源：

```text
### 来源问题

- missing evidence: e9
- unresolved reference: [ev_missing]
```

### 11.4 Evidence Appendix

当 `include_evidence_appendix=true`：

```markdown
## 证据附录

| evidence_id | paper_id | topic | method | finding | limitation | future_work | source |
| --- | --- | --- | --- | --- | --- | --- | --- |
| e1 | p1 | feedback | quasi-experiment | ... | ... | ... | chunk_1 |
```

默认不启用，避免导出文件过长。

### 11.5 JSON 导出

```json
{
  "export_metadata": {
    "format": "json",
    "exported_at": "2026-05-28T10:30:00",
    "citation_style": "simple",
    "schema_version": "report_export_v1"
  },
  "report": {},
  "versions": [],
  "source_index": {
    "papers": [],
    "evidence_records": [],
    "graph_nodes": [],
    "section_sources": {}
  },
  "validation": {},
  "appendices": {
    "evidence_table": [],
    "references": []
  }
}
```

用途：

```text
前端调试。
跨系统迁移。
后续导入 Word/PDF 生成器。
评估脚本读取。
```

### 11.6 文件命名

```text
{project_id}_{report_type}_{report_id}_v{version}.md
{project_id}_{report_type}_{report_id}_v{version}.json
```

清理规则：

```text
只允许字母、数字、下划线、短横线。
中文标题不直接用于文件名，放 metadata。
```

### 11.7 DOCX/PDF 预留

P2 接口：

```python
export_docx(report_id: str, options: ExportOptions) -> ExportPackage
export_pdf(report_id: str, options: ExportOptions) -> ExportPackage
```

推荐路线：

```text
Markdown -> Pandoc -> DOCX/PDF
Markdown + source index -> template renderer -> DOCX
Markdown -> HTML -> PDF
```

P0 只设计接口，不实现外部依赖。

## 12. 引用格式化

### 12.1 CitationFormatter 职责

```text
把 Paper 元数据转为参考文献字符串。
生成 citation_key。
支持不同 citation_style。
避免正文生成模块自己拼引用。
```

### 12.2 支持格式

P0：

```text
simple
apa_basic
gbt7714_basic
bibtex_basic
```

P1：

```text
CSL adapter
Zotero style import
```

### 12.3 simple 格式

```text
Authors. (Year). Title. Venue. DOI/URL
```

字段缺失处理：

```text
authors 空 -> Unknown authors
year 空 -> n.d.
title 空 -> Untitled paper
venue 空 -> 省略
doi/url 空 -> 省略
```

### 12.4 APA basic

```text
Author, A. A., & Author, B. B. (2024). Title. Venue. https://doi.org/...
```

P0 不强制完全符合 APA 细节，但要比 simple 更接近正式引用。

### 12.5 GB/T 7714 basic

```text
[1] 作者. 题名[J]. 刊名, 年份.
```

规则：

```text
按 source_index.paper_sources 顺序编号。
同一 paper_id 只出现一次。
正文仍可保留 [p1]，参考文献区显示 [1]。
```

### 12.6 BibTeX basic

```bibtex
@article{p1,
  title = {Title},
  author = {Author A and Author B},
  year = {2024},
  journal = {Venue},
  doi = {10.xxxx/xxxx},
  url = {https://...}
}
```

## 13. 保存与更新策略

### 13.1 save_report

推荐签名：

```python
def save_report(
    self,
    report: Report,
    *,
    source_index: ReportSourceIndex | None = None,
    validation_result: ReportValidationResult | None = None,
    create_initial_version: bool = True,
) -> Report:
    ...
```

P0 可以先保持原签名，新增可选参数不破坏旧测试。

行为：

```text
1. 标准化 report.status，默认 draft。
2. 标准化 report.version，默认 1。
3. 如果 source_index 为空，根据 report 字段构建基础来源索引。
4. 如果 validation_result 为空，运行基础 traceability 校验。
5. 把 validation/source_index 摘要写入 report.scope.report_metadata 或 report.metadata。
6. 保存 report。
7. 如果 create_initial_version 且没有 version 1，创建初始版本。
```

### 13.2 update_report

建议新增：

```python
def update_report(
    self,
    report_id: str,
    *,
    title: str | None = None,
    content: str | None = None,
    status: str | None = None,
    source_index: ReportSourceIndex | None = None,
    reason: str = "",
    expected_version: int | None = None,
) -> Report | None:
    ...
```

语义：

```text
内容变化 -> create_version。
只改 status/title -> 更新 report，必要时也可 create metadata version。
```

### 13.3 归档与删除

归档：

```python
archive_report(report_id: str, reason: str = "") -> bool
unarchive_report(report_id: str) -> bool
```

行为：

```text
设置 status=archived。
不删除版本。
list_reports 默认不返回 archived。
```

删除：

```python
delete_report(report_id: str, hard: bool = False) -> bool
```

默认 soft delete：

```text
status=deleted
保留 versions 和 source index
list_reports 默认不返回 deleted
```

hard delete：

```text
删除 reports。
删除 report_versions。
删除 report_source_indexes。
只有明确传 hard=True 才执行。
```

### 13.4 状态枚举

```text
draft
reviewing
final
archived
deleted
```

状态流转：

```text
draft -> reviewing -> final
draft/final -> archived
archived -> draft/final
any -> deleted
```

P0 可只实现：

```text
draft
final
archived
deleted
```

## 14. API 设计

### 14.1 报告列表

```http
GET /api/rw/projects/{project_id}/reports?type=literature_review&status=draft&include_archived=false
```

响应：

```json
{
  "reports": [
    {
      "report_id": "r1",
      "project_id": "proj1",
      "type": "literature_review",
      "title": "文献综述",
      "status": "draft",
      "version": 2,
      "paper_count": 12,
      "evidence_count": 36,
      "validated": true,
      "updated_at": "..."
    }
  ]
}
```

### 14.2 报告详情

```http
GET /api/rw/reports/{report_id}
```

响应：

```json
{
  "report": {},
  "source_index": {},
  "validation_result": {},
  "latest_version": {},
  "available_exports": ["markdown", "json"]
}
```

### 14.3 更新报告

```http
PATCH /api/rw/reports/{report_id}
```

请求：

```json
{
  "content": "updated markdown",
  "reason": "人工修改研究背景",
  "expected_version": 2
}
```

行为：

```text
创建新版本。
返回 updated report。
如果 expected_version 不匹配，返回 version_conflict。
```

### 14.4 版本接口

```http
GET  /api/rw/reports/{report_id}/versions
POST /api/rw/reports/{report_id}/versions
GET  /api/rw/reports/{report_id}/versions/{version_id}
POST /api/rw/reports/{report_id}/versions/{version_id}/restore
GET  /api/rw/reports/{report_id}/versions/{version_id}/diff
```

### 14.5 校验接口

```http
POST /api/rw/reports/{report_id}/validate
```

请求：

```json
{
  "strict_scope": true,
  "allow_external_references": false
}
```

响应：

```json
{
  "valid": true,
  "score": 0.92,
  "issues": [],
  "warnings": [],
  "source_coverage": 0.95,
  "section_source_coverage": 0.88
}
```

### 14.6 导出接口

```http
GET /api/rw/reports/{report_id}/export/markdown?citation_style=gbt7714&include_sources=true
GET /api/rw/reports/{report_id}/export/json?include_versions=true
```

P1：

```http
GET /api/rw/reports/{report_id}/export/docx
GET /api/rw/reports/{report_id}/export/pdf
```

### 14.7 归档和删除

```http
POST   /api/rw/reports/{report_id}/archive
POST   /api/rw/reports/{report_id}/unarchive
DELETE /api/rw/reports/{report_id}?hard=false
```

## 15. 前端交互设计

### 15.1 成果报告页结构

```text
左侧：报告列表和类型过滤
中间：Markdown 正文预览 / 编辑
右侧：来源索引、质量校验、版本历史
顶部：生成、保存版本、校验、导出、归档
```

### 15.2 报告列表

展示字段：

```text
title
type
status
version
updated_at
paper_count
evidence_count
validation status
```

过滤：

```text
报告类型
状态
是否归档
更新时间
是否校验通过
```

### 15.3 报告详情

需要展示：

```text
正文
YAML 元信息摘要
Scope summary
来源统计
质量校验状态
导出选项
```

正文中的来源 ID 可点击：

```text
[p1] -> 打开论文详情
[e1] -> 打开证据详情和 source_quote
[node1] -> 打开图谱节点
```

### 15.4 来源索引面板

Tabs：

```text
论文
证据
图谱
章节来源
问题
```

证据面板字段：

```text
evidence_id
paper_id
topic
method
finding
limitation
source_quote
used_in_sections
```

### 15.5 版本历史面板

展示：

```text
版本号
创建时间
修改原因
diff summary
来源变化
校验状态
恢复按钮
对比按钮
```

### 15.6 导出面板

控件：

```text
格式：Markdown / JSON / DOCX(P2) / PDF(P2)
引用格式：simple / APA / GB/T / BibTeX
包含来源索引
包含证据附录
包含质量校验
包含版本历史
```

导出前：

```text
如果 traceability 校验失败，默认提示用户。
用户可以选择继续导出，但导出文件必须包含问题列表。
```

## 16. 与生成模块集成

### 16.1 综述生成集成

综述生成模块应输出：

```text
Report
section_sources
validation_result
source_index optional
```

保存：

```python
report_service.save_report(
    report,
    source_index=source_index,
    validation_result=validation_result,
)
```

### 16.2 创新点报告集成

创新报告模块应输出：

```text
Report
candidate_sources
gap_signals
validation_result
scores
```

ReportService 将这些材料转换为：

```text
section_sources
source_index.custom["innovation"]
validation_result
```

### 16.3 QA 素材保存

ScopeQA 的回答可以保存为草稿素材：

```python
save_report_material(
    project_id,
    source_type="qa_answer",
    content=answer,
    scope=scope,
    supporting_papers=...,
    evidence_records=...,
)
```

P1 实现：

```text
QA material -> report draft
多个 QA material -> report outline
```

P0 可以只在文档中预留，不急于实现。

## 17. 存储设计

### 17.1 JSONStorage 集合

当前已有：

```text
reports.json
report_versions.json
```

建议新增：

```text
report_source_indexes.json
report_exports.json
report_materials.json
```

P0 如果不新增集合，可先把 source_index 存在：

```text
Report.scope.report_metadata.source_index
Report.scope.report_metadata.validation_result
```

但需要控制大小：

```text
source_quote 截断
不要把完整 evidence 表塞进 Report.scope
```

### 17.2 export 文件目录

P1：

```text
data/research_workspace/exports/
  proj1/
    r1/
      proj1_literature_review_r1_v2.md
      proj1_literature_review_r1_v2.json
```

P0 API 可以只返回字符串，不写文件。

### 17.3 原子写入

当前 `JSONStorage.save_collection` 直接写文件。P1 可改为：

```text
写入临时文件
flush
rename
```

减少写入中断导致 JSON 损坏的风险。

### 17.4 删除一致性

soft delete：

```text
reports.status=deleted
versions 保留
source_indexes 保留
exports 保留或标记
```

hard delete：

```text
delete report
delete report_versions where report_id
delete report_source_indexes where report_id
delete report_exports where report_id
```

## 18. 日志与评估

### 18.1 日志事件

```text
report.save.started
report.saved
report.version.created
report.version.restored
report.validated
report.export.started
report.exported
report.archived
report.deleted
report.error
```

### 18.2 日志字段

```text
project_id
report_id
report_type
version
status
paper_count
evidence_count
graph_node_count
source_coverage
section_source_coverage
validation_passed
export_format
duration_ms
error_code
```

### 18.3 质量指标

```text
report_traceability_rate
section_source_coverage
source_index_build_success_rate
export_success_rate
version_restore_success_rate
markdown_front_matter_valid_rate
unresolved_reference_count
scope_violation_count
```

### 18.4 质量门禁

```text
scope_violation_count == 0
missing_evidence_ids == []
missing_paper_ids == []
section_source_coverage >= 0.80
source_coverage >= 0.80
markdown_front_matter_valid == true
```

导出策略：

```text
strict_validation=true 时，门禁失败不导出。
strict_validation=false 时，可以导出，但必须包含质量问题列表。
```

## 19. 开发阶段

### R0：保持现有测试并补齐兼容层

目标：不破坏当前 `test_report_service.py`。

任务：

```text
T12.0.1 保留 save_report(report) 现有签名兼容。
T12.0.2 保留 list_reports(project_id, report_type=None) 行为。
T12.0.3 保留 create_version(report_id, content, reason) 行为。
T12.0.4 保留 export_markdown(report_id) 返回字符串。
T12.0.5 为新增功能使用可选参数和新方法。
```

验收：

```text
现有 test_report_service.py 继续通过。
新增功能不要求上游模块立即改造。
```

### R1：模型元数据和初始版本

目标：报告保存后具备版本和基础元数据。

任务：

```text
T12.1.1 增强 ReportVersion：version_number、content_hash、scope_snapshot、source_snapshot。
T12.1.2 save_report 自动设置 status=draft。
T12.1.3 save_report 可选创建 version 1。
T12.1.4 create_version 写入 version_number。
T12.1.5 create_version 更新 report.version。
T12.1.6 list_versions(report_id)。
```

验收：

```text
新保存报告可以查到 version 1。
创建版本后版本号稳定递增。
版本包含 scope 和来源快照。
```

### R2：来源索引

目标：报告能展示和导出来源。

任务：

```text
T12.2.1 新增 ReportSourceIndex 内部结构。
T12.2.2 实现 build_source_index(report)。
T12.2.3 从 papers/evidence_records/graphs 加载来源详情。
T12.2.4 支持 section_sources。
T12.2.5 生成 used_in_sections。
T12.2.6 source_quote 默认截断。
T12.2.7 get_report_detail 返回 source_index。
```

验收：

```text
报告详情能看到论文、证据、图谱来源。
section_sources 能反向显示 used_in_sections。
缺失来源会出现在 source_index issues。
```

### R3：Traceability 校验

目标：保存和导出前能发现来源问题。

任务：

```text
T12.3.1 新增 ReportValidationResult。
T12.3.2 实现 validate_report_traceability(report_id)。
T12.3.3 校验 paper/evidence/project 一致性。
T12.3.4 校验 scope 越界。
T12.3.5 校验 section_sources。
T12.3.6 正文引用简单解析。
T12.3.7 计算 source_coverage 和 section_source_coverage。
```

验收：

```text
缺失 paper_id 能被发现。
缺失 evidence_id 能被发现。
scope 外 evidence 能被发现。
没有 section_sources 时给 warning。
```

### R4：Markdown 和 JSON 导出

目标：导出成果可读、可追溯、可被机器读取。

任务：

```text
T12.4.1 export_markdown 增加 ExportOptions。
T12.4.2 输出 YAML front matter。
T12.4.3 输出来源索引。
T12.4.4 输出质量校验结果。
T12.4.5 支持 include_evidence_appendix。
T12.4.6 新增 export_json。
T12.4.7 新增 ExportPackage。
```

验收：

```text
Markdown 开头包含 YAML front matter。
Markdown 包含来源索引。
JSON 包含 report、source_index、validation。
不存在报告导出返回清晰错误或空结果，行为有测试覆盖。
```

### R5：引用格式

目标：报告能输出基础参考文献。

任务：

```text
T12.5.1 新增 CitationFormatter。
T12.5.2 支持 simple。
T12.5.3 支持 apa_basic。
T12.5.4 支持 gbt7714_basic。
T12.5.5 支持 bibtex_basic。
T12.5.6 Markdown 导出增加参考文献区。
```

验收：

```text
同一 paper 只输出一次参考文献。
缺作者/年份/标题时有稳定 fallback。
GB/T 基础格式按顺序编号。
BibTeX key 稳定。
```

### R6：归档、删除、恢复和 diff

目标：报告生命周期完整。

任务：

```text
T12.6.1 archive_report。
T12.6.2 unarchive_report。
T12.6.3 delete_report soft delete。
T12.6.4 hard delete 清理 versions/source_indexes。
T12.6.5 restore_version。
T12.6.6 diff_versions。
T12.6.7 list_reports 默认隐藏 archived/deleted。
```

验收：

```text
归档报告默认不出现在列表。
恢复版本会创建新 latest，而不是回退版本号。
hard delete 后无孤儿版本。
diff_summary 可展示主要变化。
```

### R7：API 和前端联调

目标：前端成果报告页可以使用。

任务：

```text
T12.7.1 Report API 返回 ReportDetailDTO。
T12.7.2 版本列表 API。
T12.7.3 校验 API。
T12.7.4 Markdown/JSON 导出 API。
T12.7.5 前端报告列表。
T12.7.6 前端详情和来源索引面板。
T12.7.7 前端版本历史和恢复。
T12.7.8 前端导出面板。
```

验收：

```text
用户能从报告列表进入详情。
用户能看到来源索引和质量问题。
用户能创建版本、查看版本、恢复版本。
用户能导出 Markdown/JSON。
```

## 20. 测试计划

### 20.1 测试文件

```text
tests/agents_v3/research_workspace/test_report_service.py
tests/agents_v3/research_workspace/test_report_versions.py
tests/agents_v3/research_workspace/test_report_source_index.py
tests/agents_v3/research_workspace/test_report_traceability.py
tests/agents_v3/research_workspace/test_report_export_markdown.py
tests/agents_v3/research_workspace/test_report_export_json.py
tests/agents_v3/research_workspace/test_report_archive_delete.py
tests/agents_v3/research_workspace/test_citation_formatter.py
```

### 20.2 核心测试用例

```text
test_save_report_persists_to_storage
test_save_report_creates_initial_version
test_list_reports_filters_by_type
test_list_reports_excludes_archived_by_default
test_create_version_increments_version_number
test_create_version_saves_scope_snapshot
test_create_version_saves_source_snapshot
test_list_versions_sorted_desc
test_restore_version_creates_new_latest
test_diff_summary_detects_heading_changes
test_build_source_index_loads_papers_and_evidence
test_build_source_index_records_used_in_sections
test_traceability_missing_paper_fails
test_traceability_missing_evidence_fails
test_traceability_scope_violation_fails
test_traceability_unresolved_text_reference_warns
test_export_markdown_has_yaml_front_matter
test_export_markdown_includes_source_index
test_export_markdown_includes_validation_result
test_export_json_contains_report_source_validation
test_archive_report_hides_from_default_list
test_soft_delete_marks_deleted
test_hard_delete_removes_versions
test_citation_formatter_simple
test_citation_formatter_gbt7714_basic
test_citation_formatter_bibtex_basic
```

### 20.3 Fixture 设计

```text
project proj1
papers p1, p2
evidence e1 -> p1
evidence e2 -> p2
graph nodes topic_feedback, method_quasi_experiment
report r1 with paper_ids=[p1,p2], evidence_ids=[e1,e2]
section_sources intro -> p1/e1
section_sources method -> p2/e2
```

异常 fixture：

```text
missing paper p_missing
missing evidence e_missing
scope only includes e1 but report references e2
正文引用 [e_missing]
section_sources references p_outside
```

### 20.4 导出快照测试

Markdown snapshot 至少断言：

```text
以 --- 开头。
包含 report_id。
包含 version。
包含 来源索引。
包含 使用论文。
包含 使用证据。
包含 质量校验。
```

JSON snapshot 至少断言：

```text
export_metadata.schema_version == report_export_v1
report.report_id == r1
source_index.papers length == 2
validation.valid == true/false
```

## 21. 验收标准

### 21.1 功能验收

```text
报告可以保存、查询、列表过滤。
报告可以创建初始版本和后续版本。
报告可以列出版本并恢复历史版本。
报告可以归档、取消归档、软删除和硬删除。
报告可以导出 Markdown。
报告可以导出 JSON。
导出结果包含元信息、正文、来源索引和质量校验。
```

### 21.2 质量验收

```text
缺失 paper/evidence 会被 traceability 校验发现。
Scope 外 evidence 会被 traceability 校验发现。
章节缺来源会给出 warning。
正文中无法解析的引用会给出 warning。
section_source_coverage 可计算。
source_coverage 可计算。
Markdown front matter 可解析。
```

### 21.3 工程验收

```text
现有 report_service 测试不破坏。
新增测试覆盖版本、导出、来源索引和校验。
ReportService 对旧 Report 模型兼容。
导出逻辑不依赖综述/创新模块内部实现。
日志事件覆盖保存、版本、校验、导出。
```

## 22. 最小实现顺序

建议按以下顺序推进：

```text
1. 给 ReportVersion 增加 version_number、scope_snapshot、source_snapshot。
2. save_report 自动创建 version 1。
3. create_version 补齐 version_number 和 diff_summary。
4. 实现 list_versions。
5. 实现基础 build_source_index(report)。
6. 实现 validate_report_traceability(report_id)。
7. 改造 export_markdown，增加 YAML front matter 和来源索引。
8. 增加 export_json。
9. 增加 archive_report 和 soft delete。
10. 再做 CitationFormatter 和 restore_version。
```

优先级判断：

```text
P0 必须先让报告可追溯和可导出。
P1 再做用户体验增强。
P2 再接 DOCX/PDF 和复杂引用标准。
```

## 23. 风险与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| 新模型破坏旧报告 | 旧 JSON 缺新增字段 | Pydantic 默认值 + 兼容 metadata |
| source_index 太大 | Report.scope 被塞满证据全文 | source_quote 截断，P1 独立集合 |
| traceability 误报 | 旧报告没有 section_sources | warning 而非 error |
| scope 校验过严 | 外部参考被误判越界 | external_sources 策略 |
| Markdown 导出不规范 | 元信息不可解析 | 固定 YAML front matter 字段 |
| 版本恢复混乱 | 版本号回退 | restore 创建新版本，不倒退版本号 |
| hard delete 残留数据 | report_versions 孤儿记录 | hard delete 批量清理相关集合 |
| 引用格式不完整 | APA/GB/T 不完全合规 | P0 标记 basic，P1 接 CSL |
| 图谱存储 ID 不一致 | graphs 集合 id 字段映射混乱 | traceability 对 graph 做容错读取，并修正 storage 映射 |

## 24. 参考文档

本地参考：

```text
docs/research/14-细致开发计划/模块级开发计划/00-模块总览与依赖关系.md
docs/research/14-细致开发计划/模块级开发计划/08-RetrievalScope模块.md
docs/research/14-细致开发计划/模块级开发计划/10-综述生成模块.md
docs/research/14-细致开发计划/模块级开发计划/11-创新点报告模块.md
docs/research/14-细致开发计划/模块级开发计划/13-LLM提示词与结构化输出模块.md
docs/research/14-细致开发计划/模块级开发计划/14-API任务与前端联调模块.md
docs/research/14-细致开发计划/模块级开发计划/15-评估日志监控模块.md
```

外部参考：

```text
Pandoc:
https://pandoc.org/

Citation Style Language:
https://citationstyles.org/

Zotero Citation Styles:
https://www.zotero.org/styles

Python difflib:
https://docs.python.org/3/library/difflib.html

Quarto:
https://quarto.org/
```

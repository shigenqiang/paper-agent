# 评估日志监控模块专项开发计划

更新时间：2026-05-28

## 实现状态

P0 全部完成（2026-05-28）。

### 已完成

```text
logging_utils.py: LogContext(contextvars) + bind_context/get_context/clear_context + log_event + log_operation + redact_text/hash_text/truncate_text/sanitize_payload ✅
metrics_collector.py: MetricRecord + MetricsCollector(record/get_records/summarize/summarize_all/write_jsonl) ✅
evaluation.py: scope_guard_check + citation_coverage_check + report_traceability_check + innovation_specificity_check + generic_text_check + redaction_check + e2e_smoke_check + refusal_correctness_check ✅
quality_gates.py: QualityGateResult + QualityGateSummary + check_gate + run_quality_gates ✅
golden_loader.py: GoldenCase + load_golden_cases/save_golden_cases + 4 个内置 golden cases ✅
```

### 新增文件

- `logging_utils.py` — 结构化日志上下文（request_id/task_id/project_id）、log_operation 上下文管理器、日志脱敏（API key/路径/长文本）
- `metrics_collector.py` — 指标收集器（record/summarize/write_jsonl）
- `evaluation.py` — 8 个评估函数（scope_guard/citation_coverage/report_traceability/innovation_specificity/generic_text/redaction/e2e_smoke/refusal_correctness）
- `quality_gates.py` — 质量门禁（check_gate/run_quality_gates）
- `golden_loader.py` — Golden case 加载/保存 + 4 个内置用例

本文档面向 `src/agents_v3/research_workspace`，用于把论文智能工作台的各模块变成可观测、可复盘、可回归、可质量门禁的工程系统。它不是单纯的日志工具，而是贯穿搜索、解析、卡片、证据、图谱、RetrievalScope、ScopeQA、综述、创新点、报告导出、LLM 调用、API 任务和前端联调的质量基础设施。

P0 阶段不建设复杂监控平台，优先实现本地可执行能力：

```text
结构化日志
敏感信息脱敏
LLM 调用指标
模块运行指标
Golden Fixtures
质量评估函数
本地评估 CLI
端到端 smoke quality gate
```

目标文件建议：

```text
src/agents_v3/research_workspace/logging_utils.py
src/agents_v3/research_workspace/evaluation.py
src/agents_v3/research_workspace/quality_gates.py
src/agents_v3/research_workspace/metrics_collector.py
src/agents_v3/research_workspace/golden_loader.py
scripts/evaluate_research_workspace.py
tests/fixtures/research_workspace/
tests/agents_v3/research_workspace/test_logging_utils.py
tests/agents_v3/research_workspace/test_evaluation.py
tests/agents_v3/research_workspace/test_quality_gates.py
```

## 1. 模块定位

### 1.1 一句话定位

评估日志监控模块负责回答三个问题：

```text
系统发生了什么？
结果是否可信？
重构后有没有退化？
```

### 1.2 在产品链路中的位置

```text
Search
  -> Parser
  -> PaperCard
  -> EvidenceTable
  -> KnowledgeGraph
  -> RetrievalScope
  -> ScopeQA/RAG
  -> ReviewReport
  -> InnovationReport
  -> ReportVersion/Export
  -> API/Task/Frontend
  -> Evaluation/Logs/Monitoring
```

评估日志监控模块横向接入所有模块，但不替代业务模块的核心逻辑。它只做四类事情：

| 类型 | 职责 | 产物 |
| --- | --- | --- |
| Observability | 记录结构化日志和运行指标 | JSON 日志、metric records |
| Evaluation | 对 QA、RAG、报告、创新点进行质量评估 | evaluation results |
| Quality Gate | 把指标转成可通过/失败的门禁 | quality gate summary |
| Regression | 用固定 fixtures 发现重构退化 | golden test reports |

### 1.3 与其它模块的关系

| 上游模块 | 本模块接收 | 本模块输出 |
| --- | --- | --- |
| 01 搜索模块 | search session、query、source results、dedup result | search latency、provider success rate、dedup precision |
| 04 PDF 解析与分块 | parse result、chunks、section map | parse success rate、chunk coverage、parse warnings |
| 05 论文卡片 | cards、source spans | card completeness、source span coverage |
| 06 证据表 | evidence records、review_status | evidence traceability、accepted evidence ratio |
| 07 知识图谱 | graph nodes、edges、path query result | graph build stats、graph path validity |
| 08 RetrievalScope | scope object、scope guard | scope size、scope violation count |
| 09 ScopeQA/RAG | QAResponse、retrieved context | scope_guard_pass_rate、citation_coverage、context metrics |
| 10 综述生成 | Report、sections、citations | report_traceability、section_source_coverage |
| 11 创新点报告 | InnovationPoint、scores、supporting evidence | specificity、gap support、feasibility completeness |
| 12 报告版本导出 | report versions、export result | export success rate、format validation |
| 13 LLM 提示词 | llm call、prompt name、schema parse result | token、latency、retry、parse failure rate |
| 14 API 与前端联调 | request、task、frontend action | request/task trace、error taxonomy、demo smoke result |

## 2. 本地调研结论

### 2.1 现有文档给出的硬性要求

结合 `09-ScopeQA与RAG模块.md` 至 `14-API任务与前端联调模块.md`，评估日志监控模块必须承接以下要求：

| 来源 | 关键要求 | 对本模块的要求 |
| --- | --- | --- |
| ScopeQA/RAG | 不能越界引用，答案必须有证据，可拒答 | `scope_guard_check`、`citation_coverage_check`、QA golden cases |
| 综述生成 | 报告要有来源覆盖和章节质量校验 | `report_traceability_check`、`section_source_coverage_check` |
| 创新点报告 | 避免空泛创新点，gap 和 evidence 必须可追溯 | `innovation_specificity_check`、`gap_support_check` |
| 报告版本导出 | 导出结果要可验证、可回滚、可追踪 | `export_validation_check`、report version metrics |
| LLM 提示词 | 结构化输出、JSON 解析失败、token 成本要可见 | `llm.call` metric、schema parse metrics |
| API 任务联调 | request/task/project 三层 trace 要打通 | `request_id`、`task_id`、`project_id` 统一上下文 |

### 2.2 可借鉴的主流做法

本项目第一阶段只借鉴思想，不强依赖外部 SaaS：

| 方向 | 可借鉴点 | 本项目落地方式 |
| --- | --- | --- |
| OpenTelemetry | trace、logs、metrics 使用统一上下文 | P2 提供 adapter，P0 先统一字段 |
| LangSmith 类 LLM Trace | 记录 prompt version、model、latency、token、error | `LLMCallMetric` 和 `llm.call` event |
| RAGAS 类 RAG 评估 | faithfulness、answer relevance、context precision/recall | P0 先做轻量规则评估，P1 可接第三方评估器 |
| Evidently 类质量报告 | 指标快照、漂移、评估报告 | `--write-report` 输出 Markdown/JSON |
| Loguru | 本地日志轮转、结构化记录、异步写入 | 复用现有 Loguru，封装 `logging_utils.py` |
| pytest golden tests | 固定输入输出防回归 | `tests/fixtures/research_workspace` |

### 2.3 v2 结构化日志可复用思想

仓库已有 `src/agents_v2/infra/monitoring/structured_logging.py`，包含：

```text
StructuredLogger
LogAggregator
LogSampler
TraceContext
Span
LogContext
log_operation decorator
```

v3 不建议直接搬运 v2 的完整实现，原因是 v3 research workspace 目前是轻量服务层，直接引入聚合器、采样器、异步 handler 会增加复杂度。建议复用以下思想：

| v2 能力 | v3 采用方式 |
| --- | --- |
| ContextVar 保存请求上下文 | P0 使用 `LogContext` + `contextvars` |
| start/success/fail operation | P0 使用 `log_operation` context manager |
| trace/span 思路 | P1 再扩展 span，P0 只记录 `request_id/task_id/operation` |
| 日志采样 | P2 再做，P0 不采样 warning/error |
| 聚合 flush | P2 再做，P0 直接落本地 JSONL |

## 3. 当前实现分析

### 3.1 已有能力

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| Loguru | 已使用 | `llm_service.py` 等模块直接使用 `loguru.logger` |
| pytest | 已配置 | `tests/agents_v3/research_workspace` 已有较多模块测试 |
| E2E smoke | 已有 | `test_e2e_smoke.py` 覆盖项目、论文、卡片、证据、图谱、QA、综述、创新点、导出 |
| v2 structured logging | 已存在 | 可作为设计参考 |
| JSONStorage | 已存在 | 可承载本地评估 fixtures 和评估结果 |
| Pydantic models | 已存在 | QAResponse、RetrievalScope、Report、InnovationPoint、EvidenceRecord 等可直接作为评估输入 |

### 3.2 当前缺口

| 缺口 | 当前表现 | 风险 | 优先级 |
| --- | --- | --- | --- |
| 无统一日志上下文 | service 各自直接 logger | 线上或本地报错无法串起 request/task/project | P0 |
| 无日志脱敏 | 错误日志可能记录 prompt 或 response 片段 | API key、PDF 文本、用户隐私泄露 | P0 |
| 无 LLM 指标 | 只在失败时记录错误 | latency、token、retry、parse failure 不可见 | P0 |
| 无 Scope 越界评估 | 只有局部测试 | QA/报告可能引用 scope 外论文 | P0 |
| 无 citation coverage 评估 | 没有统一计算 | 答案或报告看似完整但不可追溯 | P0 |
| 无报告质量门禁 | 报告保存前缺少统一检查 | 低来源覆盖、空泛段落进入版本库 | P0 |
| 无创新点质量门禁 | 只靠生成逻辑自约束 | 创新点可能泛泛、无 gap 支撑 | P0 |
| 无 golden fixtures | 测试多为代码级 | 难以比较业务输出质量是否退化 | P0 |
| 无评估 CLI | 无一键质量报告 | 重构、联调、发布前无法快速验收 | P0 |
| 无 API/task trace | 任务执行日志与请求缺少关联 | 前端看不到失败阶段和原因 | P1 |
| 无成本统计 | token 和调用次数没有汇总 | 无法控制 LLM 成本 | P1 |
| 无外部监控适配 | 未接 OpenTelemetry 等 | 后续部署后观测能力不足 | P2 |

### 3.3 重点现有模型

本模块应优先基于现有模型做评估，而不是新建一套平行数据结构：

```text
QAResponse:
  answer
  intent
  scope_summary
  supporting_papers
  evidence_records
  graph_paths
  uncertainty
  suggested_actions
  validation_warnings
  retrieval_diagnostics
  confidence

RetrievalScope:
  paper_ids
  evidence_ids
  graph_node_ids
  graph_edge_ids
  summary
  metadata

Report:
  paper_ids
  evidence_ids
  graph_node_ids
  scope
  content

InnovationPoint:
  name
  description
  gap
  supporting_papers
  limiting_evidence
  feasibility
  risk
  possible_topic
  scores

EvidenceRecord:
  evidence_id
  project_id
  paper_id
  topic
  method
  finding
  limitation
  future_work
  evidence_strength
  source_chunk_id
  source_quote
```

## 4. 开发目标与边界

### 4.1 P0 目标

P0 目标是建立“本地可运行、测试可覆盖、失败可解释”的质量闭环：

```text
1. 所有关键 service 操作有结构化日志。
2. request_id、task_id、project_id 可以贯穿 API、任务和 service。
3. LLM 调用记录 provider、model、latency、token、retry、status、error_type。
4. 日志默认脱敏，不记录完整 prompt、完整 PDF、完整 response、API key。
5. evaluation.py 能评估 ScopeQA、报告、创新点、日志脱敏和 E2E smoke。
6. tests/fixtures/research_workspace 提供稳定 golden cases。
7. scripts/evaluate_research_workspace.py 可输出 JSON 和 Markdown 质量报告。
8. 质量门禁失败时明确列出失败 case、失败指标和建议定位模块。
```

### 4.2 P1 目标

```text
1. 增加搜索、解析、分块、证据、图谱、导出的模块级指标。
2. 增加 RAG context precision/recall 的 golden 评估。
3. 增加 API task 生命周期追踪。
4. 增加 token 成本、provider 失败率、JSON 解析失败率统计。
5. 把评估 CLI 接入 CI 或本地 pre-release 检查。
```

### 4.3 P2 目标

```text
1. 提供 OpenTelemetry adapter。
2. 提供 LangSmith 或同类 LLM trace adapter。
3. 提供本地 dashboard JSON 数据源。
4. 支持不同评估 profile：fast、full、release。
5. 评估结果可按日期和版本保存，形成质量趋势。
```

### 4.4 非目标

P0 不做以下事情：

```text
不搭建 Prometheus/Grafana。
不强依赖 LangSmith、Ragas、Evidently 等外部服务。
不存储完整用户 PDF、完整 prompt、完整 LLM response。
不把主观学术质量完全自动化打分。
不追求一次性覆盖所有指标，优先覆盖会导致产品不可信的问题。
```

## 5. 目标架构

### 5.1 模块分层

```text
src/agents_v3/research_workspace/
  logging_utils.py
    - LogContext
    - bind_context
    - log_event
    - log_operation
    - redact_text
    - hash_text

  metrics_collector.py
    - MetricRecord
    - MetricsCollector
    - summarize_metrics
    - write_jsonl_metrics

  evaluation.py
    - scope_guard_check
    - citation_coverage_check
    - rag_context_check
    - report_traceability_check
    - section_source_coverage_check
    - innovation_specificity_check
    - generic_text_check
    - redaction_check
    - e2e_smoke_check

  quality_gates.py
    - QualityGate
    - QualityGateResult
    - evaluate_quality_gates
    - default_research_workspace_gates

  golden_loader.py
    - GoldenCase
    - GoldenDataset
    - load_golden_dataset
    - validate_fixture_schema

scripts/
  evaluate_research_workspace.py
    - CLI entry
    - target selection
    - JSON/Markdown report output
```

### 5.2 数据流

```text
Service Operation
  -> logging_utils.log_operation()
  -> log_event(start/succeeded/failed)
  -> metrics_collector.record()
  -> JSONL logs / in-memory metrics

Golden Fixtures
  -> golden_loader.load()
  -> evaluation functions
  -> EvaluationResult[]
  -> quality_gates.evaluate()
  -> CLI summary
  -> JSON/Markdown quality report
```

### 5.3 P0 依赖原则

```text
只依赖 Python 标准库、Pydantic、Loguru、pytest 和现有项目代码。
评估函数尽量纯函数化，输入模型或 dict，输出结构化结果。
CLI 不启动真实外部 LLM，不依赖网络。
日志输出默认 JSONL，便于后续被任何平台采集。
```

## 6. 结构化日志设计

### 6.1 统一日志字段

所有结构化事件都应尽量包含以下字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `timestamp` | str | UTC ISO 时间 |
| `level` | str | info/warning/error |
| `event_type` | str | 事件类型 |
| `service` | str | 服务名，如 `ScopeQAService` |
| `operation` | str | 操作名，如 `answer` |
| `status` | str | started/succeeded/failed/skipped |
| `request_id` | str | API 请求 ID |
| `task_id` | str | 后台任务 ID |
| `project_id` | str | 项目 ID |
| `paper_id` | str | 论文 ID，适用于单论文操作 |
| `report_id` | str | 报告 ID |
| `scope_hash` | str | Scope 的稳定 hash，不记录完整 scope 大对象 |
| `latency_ms` | int | 操作耗时 |
| `input_count` | int | 输入对象数量 |
| `output_count` | int | 输出对象数量 |
| `warning_count` | int | 警告数量 |
| `error_type` | str | 归一化错误类型 |
| `error_message` | str | 脱敏且截断后的错误信息 |
| `metadata` | dict | 小型补充信息 |

### 6.2 事件命名规范

事件命名采用 `domain.action.status` 或 `domain.action`：

```text
project.create.started
project.create.succeeded
paper.import.succeeded
search.query.started
search.query.succeeded
parser.parse.failed
chunk.build.succeeded
paper_card.generate.succeeded
evidence.build.succeeded
graph.build.succeeded
scope.resolve.succeeded
qa.answer.succeeded
review.generate.succeeded
innovation.generate.succeeded
report.export.succeeded
llm.call.succeeded
llm.call.failed
task.started
task.progress
task.succeeded
task.failed
evaluation.completed
quality_gate.failed
```

### 6.3 上下文字段传播

建议使用 `contextvars` 保存当前执行上下文：

```python
from contextlib import contextmanager
from dataclasses import dataclass, field
from typing import Any

@dataclass
class LogContext:
    request_id: str | None = None
    task_id: str | None = None
    project_id: str | None = None
    paper_id: str | None = None
    report_id: str | None = None
    metadata: dict[str, Any] = field(default_factory=dict)
```

核心接口：

```python
def bind_context(**kwargs: Any) -> None:
    """把 request_id/task_id/project_id 等写入当前上下文。"""


def get_context() -> dict[str, Any]:
    """返回当前上下文，用于日志和指标自动带上关联 ID。"""


def log_event(event_type: str, level: str = "info", **fields: Any) -> None:
    """输出结构化事件，内部自动脱敏和合并上下文。"""


@contextmanager
def log_operation(service: str, operation: str, **fields: Any):
    """自动记录 started/succeeded/failed 和 latency_ms。"""
```

使用方式：

```python
with log_operation(
    service="ScopeQAService",
    operation="answer",
    project_id=project_id,
    question_hash=hash_text(question),
):
    response = qa_service.answer(project_id, question, scope_payload)
```

### 6.4 start/success/fail 规范

每个关键操作应至少记录两条事件：

```text
operation.started
operation.succeeded
```

失败时记录：

```text
operation.failed
```

失败事件要求：

```text
必须有 error_type。
error_message 必须脱敏和截断。
不记录完整 traceback 到普通业务日志。
debug 模式可记录 traceback_hash 或单独安全文件。
```

### 6.5 日志输出位置

P0 建议：

```text
logs/research_workspace/events.jsonl
logs/research_workspace/metrics.jsonl
logs/research_workspace/evaluation/latest.json
logs/research_workspace/evaluation/latest.md
```

如果现有全局日志配置已有轮转和保留策略，则只需要把 research workspace 的事件作为结构化字段写入同一 Loguru sink。不要重复实现复杂日志轮转。

## 7. 日志脱敏设计

### 7.1 脱敏原则

```text
默认不记录完整用户输入。
默认不记录完整 PDF 文本。
默认不记录完整 prompt。
默认不记录完整 LLM response。
默认不记录 API key、Bearer token、Cookie、Authorization。
用户文件绝对路径默认 hash 或相对化。
source_quote 默认截断。
```

### 7.2 敏感模式

`redact_text` 至少覆盖：

| 类型 | 示例模式 | 输出 |
| --- | --- | --- |
| OpenAI key | `sk-...` | `[REDACTED_API_KEY]` |
| Bearer token | `Bearer abc...` | `Bearer [REDACTED_TOKEN]` |
| 环境变量 | `OPENAI_API_KEY=...` | `OPENAI_API_KEY=[REDACTED]` |
| Authorization | `Authorization: ...` | `Authorization: [REDACTED]` |
| Cookie | `Cookie: ...` | `Cookie: [REDACTED]` |
| 长 prompt | 超过阈值文本 | 截断 + hash |
| PDF 大段文本 | 超过阈值文本 | 摘要长度 + hash |
| Windows 绝对路径 | `C:\Users\...` | `[REDACTED_PATH]` 或相对路径 |

### 7.3 推荐函数

```python
def hash_text(text: str | None) -> str | None:
    """返回稳定短 hash，用于关联同一输入而不泄露原文。"""


def truncate_text(text: str | None, max_len: int = 200) -> str | None:
    """截断长文本，并保留 length/hash 信息。"""


def redact_text(text: str | None, max_len: int = 200) -> str | None:
    """脱敏 token/key/path 后再截断。"""


def sanitize_payload(payload: dict[str, Any]) -> dict[str, Any]:
    """递归清理日志 payload。"""
```

### 7.4 LLM 日志禁止字段

`llm.call` 事件禁止出现：

```text
system_prompt
user_prompt
full_prompt
messages
raw_response
pdf_text
source_quote_full
api_key
authorization
```

允许出现：

```text
prompt_name
prompt_version
prompt_hash
input_chars
output_chars
model
provider
temperature
max_tokens
prompt_tokens
completion_tokens
total_tokens
latency_ms
retry_count
status
error_type
```

## 8. 指标数据模型

### 8.1 MetricRecord

```python
class MetricRecord(BaseModel):
    metric_name: str
    value: float | int | str | bool
    unit: str | None = None
    service: str | None = None
    operation: str | None = None
    project_id: str | None = None
    request_id: str | None = None
    task_id: str | None = None
    tags: dict[str, str] = {}
    timestamp: str
```

### 8.2 EvaluationResult

```python
class EvaluationResult(BaseModel):
    name: str
    passed: bool
    score: float | None = None
    threshold: float | None = None
    severity: str = "error"
    case_id: str | None = None
    project_id: str | None = None
    details: dict[str, Any] = {}
    failures: list[str] = []
    suggested_actions: list[str] = []
```

### 8.3 QualityGateResult

```python
class QualityGateResult(BaseModel):
    gate_name: str
    passed: bool
    required: bool = True
    metric_name: str
    actual: float | bool | str
    expected: float | bool | str
    operator: str
    failures: list[str] = []
```

### 8.4 GoldenCase

```python
class GoldenCase(BaseModel):
    case_id: str
    target: str
    description: str
    input: dict[str, Any]
    expected: dict[str, Any]
    tags: list[str] = []
```

## 9. 评估指标体系

### 9.1 总体指标分层

| 层级 | 目标 | 示例指标 |
| --- | --- | --- |
| 运行稳定性 | 系统是否能跑完 | success rate、latency、failure rate |
| 结构正确性 | 输出格式是否符合契约 | schema parse success、required field completeness |
| 可追溯性 | 结论是否有来源 | citation coverage、traceability rate |
| Scope 安全 | 是否越界引用 | scope_guard_pass_rate、out_of_scope_count |
| RAG 质量 | 检索上下文是否支撑答案 | context precision、context recall、faithfulness |
| 报告质量 | 报告是否完整且有来源 | section source coverage、generic text rate |
| 创新点质量 | 是否具体、可执行、有 gap | specificity、gap support、feasibility completeness |
| 安全合规 | 日志是否泄露敏感数据 | redaction pass、forbidden field count |

### 9.2 ScopeQA 指标

| 指标 | 公式 | P0 阈值 |
| --- | --- | --- |
| `scope_guard_pass_rate` | 通过 scope 检查的 QA cases / QA cases | `1.0` |
| `out_of_scope_citation_count` | 引用 scope 外 paper/evidence 的次数 | `0` |
| `answer_citation_coverage` | 有 evidence 支撑的回答要点 / 回答要点总数 | `>= 0.80` |
| `refusal_correctness` | 证据不足 case 中正确拒答比例 | `>= 0.80` |
| `intent_accuracy` | intent 命中 golden intent 的比例 | `>= 0.75` |
| `uncertainty_when_needed` | 低证据场景有 uncertainty 的比例 | `>= 0.80` |

### 9.3 RAG 检索指标

| 指标 | 公式 | P0/P1 |
| --- | --- | --- |
| `context_precision` | retrieved evidence 中与 golden 相关的比例 | P1 |
| `context_recall` | golden required evidence 被检出的比例 | P1 |
| `faithfulness_light` | 答案中声明可被 evidence 文本关键词支持的比例 | P0 轻量 |
| `answer_relevance_light` | 答案覆盖问题关键词或 expected topics 的比例 | P0 轻量 |
| `retrieval_empty_rate` | 非空 scope 下检索为空的比例 | P0 |

P0 的 faithfulness 不要求复杂 LLM judge，先用规则：

```text
1. 每个结论必须有 evidence_id。
2. evidence_id 必须存在。
3. evidence.paper_id 必须在 scope.paper_ids 内。
4. evidence.source_quote 或 finding/limitation 中至少命中部分问题主题关键词。
```

### 9.4 报告指标

| 指标 | 公式 | P0 阈值 |
| --- | --- | --- |
| `report_traceability_rate` | 报告引用的 evidence/paper 存在且在 scope 内的比例 | `>= 0.80` |
| `section_source_coverage` | 有来源章节数 / 应有来源章节数 | `>= 0.80` |
| `source_coverage` | 被使用 paper 数 / scope paper 数 | `>= 0.60`，大 scope 可降权 |
| `reference_coverage` | 参考文献可解析条目 / 报告引用条目 | `>= 0.80` |
| `generic_text_rate` | 空泛句子数 / 总句子数 | `<= 0.20` |
| `scope_violation_count` | 报告引用 scope 外对象次数 | `0` |

### 9.5 创新点指标

| 指标 | 公式 | P0 阈值 |
| --- | --- | --- |
| `innovation_specificity_rate` | 具体创新点 / 创新点总数 | `>= 0.70` |
| `gap_support_rate` | 有 limiting evidence 或 gap 引用的创新点 / 总数 | `>= 0.70` |
| `evidence_support_rate` | 有 supporting papers/evidence 的创新点 / 总数 | `>= 0.80` |
| `feasibility_completeness` | 方法、数据、风险、可行性字段完整率 | `>= 0.70` |
| `generic_filter_pass_rate` | 非空泛创新点比例 | `>= 0.90` |
| `innovation_scope_violation_count` | 引用 scope 外论文或证据次数 | `0` |

### 9.6 LLM 指标

| 指标 | 含义 | P0/P1 |
| --- | --- | --- |
| `llm_latency_ms` | 调用耗时 | P0 |
| `llm_failure_rate` | 失败调用 / 总调用 | P0 |
| `json_parse_failure_rate` | JSON 解析失败 / JSON 调用 | P0 |
| `llm_retry_count` | retry 次数 | P0 |
| `prompt_tokens` | 输入 token | P0，取不到时为空 |
| `completion_tokens` | 输出 token | P0，取不到时为空 |
| `total_token_cost_estimate` | 估算成本 | P1 |
| `provider_failure_rate` | provider 维度失败率 | P1 |

### 9.7 API/task 指标

| 指标 | 含义 |
| --- | --- |
| `api_request_latency_ms` | API 请求耗时 |
| `task_duration_ms` | 后台任务总耗时 |
| `task_failure_rate` | 任务失败率 |
| `task_stage_failed_count` | 各阶段失败数量 |
| `frontend_poll_success_rate` | 前端轮询成功率 |
| `demo_pipeline_pass_rate` | 演示链路通过率 |

## 10. evaluation.py 设计

### 10.1 核心函数列表

```python
def scope_guard_check(response: QAResponse, scope: RetrievalScope) -> EvaluationResult:
    ...


def citation_coverage_check(response: QAResponse) -> EvaluationResult:
    ...


def rag_context_check(
    retrieved_evidence_ids: list[str],
    required_evidence_ids: list[str],
    allowed_evidence_ids: list[str] | None = None,
) -> list[EvaluationResult]:
    ...


def report_traceability_check(
    report: Report,
    scope: RetrievalScope | None,
    evidence_by_id: dict[str, EvidenceRecord],
    paper_ids: set[str],
) -> EvaluationResult:
    ...


def section_source_coverage_check(report_content: str) -> EvaluationResult:
    ...


def innovation_specificity_check(points: list[InnovationPoint]) -> EvaluationResult:
    ...


def generic_text_check(text: str, domain: str) -> EvaluationResult:
    ...


def redaction_check(log_lines: list[str]) -> EvaluationResult:
    ...


def e2e_smoke_check(summary: dict[str, Any]) -> EvaluationResult:
    ...
```

### 10.2 scope_guard_check

输入：

```text
QAResponse
RetrievalScope
```

检查：

```text
supporting_papers 必须是 scope.paper_ids 子集。
evidence_records[*].evidence_id 必须在 scope.evidence_ids 内，scope.evidence_ids 为空时至少 paper_id 在 scope.paper_ids 内。
graph_paths 引用的 node/edge 必须在 scope graph ids 内。
validation_warnings 中如果已有 scope violation，需要合并到失败原因。
```

输出示例：

```json
{
  "name": "scope_guard_check",
  "passed": false,
  "score": 0.0,
  "threshold": 1.0,
  "failures": [
    "supporting_papers contains paper_3 outside scope"
  ],
  "suggested_actions": [
    "Run citation validation after LLM output and remove out-of-scope ids"
  ]
}
```

### 10.3 citation_coverage_check

P0 不做复杂自然语言切句，只做保守近似：

```text
如果 answer 非空但 evidence_records 为空，则失败。
如果 answer 中出现明显引用标记但 evidence_records 不匹配，则失败。
如果 answer 超过 500 字但 evidence_records 少于 2 条，则 warning。
如果 response.uncertainty 表示证据不足，则允许 citation coverage 降低，但必须有 suggested_actions。
```

### 10.4 report_traceability_check

检查：

```text
report.paper_ids 是否存在。
report.evidence_ids 是否存在。
report.paper_ids 是否在 scope.paper_ids 内。
report.evidence_ids 对应 evidence.paper_id 是否在 report.paper_ids 或 scope.paper_ids 内。
report.content 中出现的 citation id 是否能解析。
report.content 非空且长度达到最小阈值。
```

重要：报告 traceability 不能只检查 `Report.evidence_ids` 字段，还要检查正文中是否有来源标记。否则报告可能保存了 evidence_ids，但正文没有任何可读来源。

### 10.5 section_source_coverage_check

P0 先基于 Markdown 标题和引用模式：

```text
解析二级/三级标题。
跳过 摘要/结论/参考文献 等可低引用章节。
对 背景/方法/主题/发现/不足/未来方向 等章节要求至少一个来源标记。
来源标记可以是 [paper_id]、[evidence_id]、论文标题片段或统一 citation marker。
```

输出字段：

```text
section_count
source_required_section_count
covered_section_count
missing_sections
coverage
```

### 10.6 innovation_specificity_check

每个创新点至少满足：

```text
name 不是泛泛词。
description 包含具体对象、方法、数据、场景或机制中的至少两个。
gap 非空，且不是“研究不足”“需要进一步研究”等泛泛表达。
supporting_papers 非空。
limiting_evidence 或 scores 中能看到 gap/evidence 支撑。
feasibility 非空。
risk 非空或有明确限制条件。
```

空泛表达词表初始建议：

```text
进一步研究
深入探讨
提高效果
优化模型
结合多种方法
构建系统
提升性能
拓展应用
加强分析
探索机制
```

注意：词表只用于发现明显问题，不能作为唯一判断。P1 可以结合结构化字段完整性和 evidence 支撑提升准确率。

### 10.7 redaction_check

检查日志中是否出现：

```text
sk-
OPENAI_API_KEY=
Authorization:
Bearer 后跟长 token
Cookie:
api_key
完整 prompt 字段名
raw_response
超过阈值的大段 PDF 文本
C:\Users\ 绝对路径
```

P0 策略：

```text
只要发现 API key/token 立即失败。
发现 raw_response/full_prompt 立即失败。
发现绝对路径 warning，P1 改为 failure。
发现长 source_quote warning 或 failure，取决于字段名。
```

## 11. Golden Fixtures 设计

### 11.1 目录结构

```text
tests/fixtures/research_workspace/
  README.md
  mini_project.json
  sample_papers.json
  sample_chunks.json
  sample_cards.json
  sample_evidence.json
  sample_graph.json
  sample_scope_cases.json
  golden_qa_cases.json
  golden_rag_cases.json
  golden_review_checks.json
  golden_innovation_checks.json
  golden_export_checks.json
  golden_redaction_cases.json
```

### 11.2 mini_project.json

```json
{
  "project_id": "proj_golden_ai_learning",
  "name": "AI feedback and self-regulated learning",
  "description": "Golden fixture for research workspace evaluation",
  "created_at": "2026-05-28T00:00:00Z"
}
```

### 11.3 golden_qa_cases.json

```json
[
  {
    "case_id": "qa_limitations_001",
    "target": "qa",
    "description": "回答选中论文的共同不足，不能引用 scope 外论文",
    "input": {
      "question": "这些研究共同不足是什么？",
      "scope_payload": {
        "type": "selected_papers",
        "selected_paper_ids": ["paper_feedback_001", "paper_tutor_002"]
      }
    },
    "expected": {
      "intent": "limitation_analysis",
      "required_evidence_ids": ["ev_limit_sample_size_001"],
      "allowed_paper_ids": ["paper_feedback_001", "paper_tutor_002"],
      "forbidden_paper_ids": ["paper_srl_003"],
      "should_refuse": false
    },
    "tags": ["scope", "citation", "limitation"]
  }
]
```

### 11.4 golden_rag_cases.json

```json
[
  {
    "case_id": "rag_context_001",
    "target": "rag",
    "description": "检索学习反馈相关证据",
    "input": {
      "query": "AI feedback improves learning outcomes",
      "scope_id": "scope_all_project"
    },
    "expected": {
      "required_evidence_ids": ["ev_finding_feedback_001"],
      "allowed_evidence_ids": [
        "ev_finding_feedback_001",
        "ev_limit_sample_size_001",
        "ev_future_longitudinal_001"
      ],
      "min_context_recall": 0.8
    },
    "tags": ["retrieval", "context_recall"]
  }
]
```

### 11.5 golden_review_checks.json

```json
[
  {
    "case_id": "review_traceability_001",
    "target": "review",
    "description": "综述报告每个主体章节都应有来源",
    "input": {
      "report_id": "report_review_golden_001"
    },
    "expected": {
      "min_report_traceability_rate": 0.8,
      "min_section_source_coverage": 0.8,
      "max_scope_violation_count": 0
    },
    "tags": ["report", "traceability"]
  }
]
```

### 11.6 golden_innovation_checks.json

```json
[
  {
    "case_id": "innovation_specificity_001",
    "target": "innovation",
    "description": "创新点必须具体、有 gap、有证据支撑",
    "input": {
      "report_id": "report_innovation_golden_001"
    },
    "expected": {
      "min_specificity_rate": 0.7,
      "min_gap_support_rate": 0.7,
      "min_evidence_support_rate": 0.8,
      "max_generic_text_rate": 0.2
    },
    "tags": ["innovation", "specificity"]
  }
]
```

### 11.7 golden_redaction_cases.json

```json
[
  {
    "case_id": "redaction_api_key_001",
    "target": "logs",
    "description": "日志中不能出现 API key",
    "input": {
      "log_line": "LLM failed with OPENAI_API_KEY=sk-test-secret"
    },
    "expected": {
      "must_fail_before_redaction": true,
      "must_pass_after_redaction": true
    },
    "tags": ["security", "redaction"]
  }
]
```

## 12. 本地评估 CLI

### 12.1 命令设计

```text
python scripts/evaluate_research_workspace.py --fixture tests/fixtures/research_workspace --target all --output-json
```

参数：

| 参数 | 说明 | 默认 |
| --- | --- | --- |
| `--fixture` | fixture 目录 | `tests/fixtures/research_workspace` |
| `--target` | `qa/rag/report/innovation/logs/e2e/all` | `all` |
| `--profile` | `fast/full/release` | `fast` |
| `--output-json` | 输出 JSON 到 stdout | false |
| `--write-report` | 写 Markdown 报告 | false |
| `--report-path` | 报告路径 | `logs/research_workspace/evaluation/latest.md` |
| `--fail-under` | 总分低于阈值时 exit 1 | 无 |
| `--strict` | warning 也视为失败 | false |
| `--no-llm` | 禁止真实 LLM 调用 | true |

### 12.2 输出 JSON schema

```json
{
  "passed": false,
  "profile": "fast",
  "target": "all",
  "started_at": "2026-05-28T10:00:00Z",
  "duration_ms": 1234,
  "summary": {
    "case_count": 12,
    "passed_count": 10,
    "failed_count": 2,
    "warning_count": 3
  },
  "metrics": {
    "scope_guard_pass_rate": 1.0,
    "answer_citation_coverage": 0.86,
    "report_traceability_rate": 0.92,
    "innovation_specificity_rate": 0.75,
    "redaction_check_pass": true,
    "e2e_smoke_pass": true
  },
  "quality_gates": [
    {
      "gate_name": "scope_guard_must_pass",
      "passed": true,
      "actual": 1.0,
      "expected": 1.0
    }
  ],
  "failures": [
    {
      "case_id": "review_traceability_001",
      "name": "section_source_coverage_check",
      "message": "Missing source in section: 方法比较"
    }
  ]
}
```

### 12.3 Markdown 报告结构

```text
Title: Research Workspace Evaluation Report

Sections:
Summary
Quality Gates
Metrics
Failed Cases
Warnings
Suggested Actions
Raw Case Results
```

### 12.4 exit code 规则

```text
0: 全部 required gates 通过。
1: 至少一个 required gate 失败。
2: fixture 读取或 schema 校验失败。
3: CLI 参数错误。
```

## 13. 质量门禁

### 13.1 P0 required gates

| Gate | 指标 | 阈值 | 失败影响 |
| --- | --- | --- | --- |
| `scope_guard_must_pass` | `scope_guard_pass_rate` | `== 1.0` | 阻塞 |
| `no_out_of_scope_citation` | `out_of_scope_citation_count` | `== 0` | 阻塞 |
| `redaction_must_pass` | `redaction_check_pass` | `true` | 阻塞 |
| `e2e_smoke_must_pass` | `e2e_smoke_pass` | `true` | 阻塞 |
| `report_traceability_min` | `report_traceability_rate` | `>= 0.80` | 阻塞 |
| `section_source_coverage_min` | `section_source_coverage` | `>= 0.80` | 阻塞 |
| `innovation_specificity_min` | `innovation_specificity_rate` | `>= 0.70` | 阻塞 |
| `generic_filter_min` | `generic_filter_pass_rate` | `>= 0.90` | 阻塞 |

### 13.2 P0 warning gates

| Gate | 指标 | 阈值 |
| --- | --- | --- |
| `qa_citation_coverage_warning` | `answer_citation_coverage` | `>= 0.80` |
| `llm_failure_rate_warning` | `llm_failure_rate` | `<= 0.05` |
| `json_parse_failure_warning` | `json_parse_failure_rate` | `<= 0.10` |
| `review_generic_text_warning` | `generic_text_rate` | `<= 0.20` |

### 13.3 P1 gates

```text
context_precision >= 0.70
context_recall >= 0.80
faithfulness_light >= 0.80
search_provider_success_rate >= 0.90
parse_success_rate >= 0.90
export_success_rate >= 0.95
task_failure_rate <= 0.05
```

## 14. 各模块接入计划

### 14.1 Search

记录事件：

```text
search.query.started
search.provider.succeeded
search.provider.failed
search.merge.succeeded
search.dedup.succeeded
search.rank.succeeded
```

指标：

```text
search_latency_ms
provider_latency_ms
provider_failure_rate
raw_result_count
dedup_result_count
duplicate_removed_count
ranking_score_distribution
cache_hit_rate
```

P0 只记录关键数量和失败；P1 再做 dedup precision 的 golden 评估。

### 14.2 Parser

记录事件：

```text
parser.parse.started
parser.parse.succeeded
parser.parse.failed
chunk.build.succeeded
section.detect.succeeded
```

指标：

```text
parse_success_rate
parse_latency_ms
chunk_count
avg_chunk_tokens
section_detection_rate
empty_chunk_count
```

质量检查：

```text
非空 PDF 不能产生 0 chunk。
chunk 必须有 paper_id、chunk_id、text、start/end 或 section_title。
section_title 缺失率超过阈值时 warning。
```

### 14.3 PaperCard

记录事件：

```text
paper_card.generate.started
paper_card.generate.succeeded
paper_card.generate.failed
```

指标：

```text
card_completeness_rate
source_span_coverage
card_llm_failure_rate
card_json_parse_failure_rate
```

质量检查：

```text
research_question/method/key_findings/limitations 至少核心字段非空。
source_spans 中的 chunk_id 必须存在。
confidence 低于阈值时记录 warning。
```

### 14.4 EvidenceTable

记录事件：

```text
evidence.build.started
evidence.build.succeeded
evidence.review_status.updated
```

指标：

```text
evidence_count
accepted_evidence_ratio
low_quality_evidence_ratio
evidence_source_quote_coverage
```

质量检查：

```text
EvidenceRecord 必须有 paper_id。
finding/limitation/future_work 至少一项非空。
source_chunk_id 或 source_quote 缺失时 warning。
```

### 14.5 KnowledgeGraph

记录事件：

```text
graph.build.started
graph.build.succeeded
graph.path_query.succeeded
```

指标：

```text
graph_node_count
graph_edge_count
isolated_node_count
path_query_latency_ms
graph_evidence_link_rate
```

质量检查：

```text
graph node/edge 必须能回溯到 paper/card/evidence。
Graph path 不能替代 evidence citation，只能作为解释链路。
```

### 14.6 RetrievalScope

记录事件：

```text
scope.resolve.started
scope.resolve.succeeded
scope.guard.created
scope.guard.failed
```

指标：

```text
scope_paper_count
scope_evidence_count
scope_graph_node_count
scope_resolution_latency_ms
scope_empty_rate
scope_violation_count
```

质量检查：

```text
selected_papers scope 只能包含选中论文。
topic scope 必须解释匹配依据。
empty scope 必须有明确 summary。
```

### 14.7 ScopeQA/RAG

记录事件：

```text
qa.answer.started
qa.retrieve.succeeded
qa.generate.succeeded
qa.validate.succeeded
qa.answer.succeeded
qa.answer.failed
```

指标：

```text
qa_latency_ms
retrieval_latency_ms
generation_latency_ms
scope_guard_pass_rate
answer_citation_coverage
refusal_rate
json_parse_failure_rate
llm_failure_rate
```

质量检查：

```text
答案不能引用 scope 外 paper/evidence。
证据不足时必须说明不确定或拒答。
LLM 输出 citations 必须经过二次校验。
```

### 14.8 ReviewReport

记录事件：

```text
review.generate.started
review.materials.collected
review.generate.succeeded
review.quality_check.succeeded
review.quality_check.failed
```

指标：

```text
review_generation_latency_ms
review_source_coverage
section_source_coverage
report_traceability_rate
review_llm_failure_rate
generic_text_rate
```

质量检查：

```text
主体章节必须有来源。
报告 evidence_ids 必须存在。
报告正文引用必须能解析。
空泛段落比例超过阈值时 warning 或 failure。
```

### 14.9 InnovationReport

记录事件：

```text
innovation.generate.started
innovation.candidates.generated
innovation.quality_check.succeeded
innovation.generate.succeeded
```

指标：

```text
innovation_candidate_count
innovation_specificity_rate
gap_support_rate
evidence_support_rate
feasibility_completeness
generic_filter_pass_rate
```

质量检查：

```text
创新点必须具体到方法、数据、场景或机制。
gap 必须来自 limiting evidence 或报告材料。
supporting_papers 必须非空且在 scope 内。
```

### 14.10 ReportVersion/Export

记录事件：

```text
report.save.succeeded
report.version.created
report.export.started
report.export.succeeded
report.export.failed
```

指标：

```text
report_version_count
export_latency_ms
export_success_rate
export_format_validation_pass
markdown_render_warning_count
```

质量检查：

```text
Markdown 导出必须包含标题、正文、来源或参考信息。
导出失败必须记录 report_id 和 format。
版本保存前可选择执行 quality gates。
```

### 14.11 LLMService

记录事件：

```text
llm.call.started
llm.call.succeeded
llm.call.failed
llm.json_parse.failed
```

指标：

```text
llm_latency_ms
prompt_tokens
completion_tokens
total_tokens
retry_count
json_parse_failure_rate
provider_failure_rate
```

实现重点：

```text
invoke 和 invoke_json 内部统一包裹 log_operation。
解析失败只记录 response hash 和前 200 字以内脱敏片段。
如果 provider 返回 token usage，提取并记录。
如果取不到 token usage，至少记录 input_chars/output_chars。
```

### 14.12 API/Task/Frontend

记录事件：

```text
api.request.started
api.request.succeeded
api.request.failed
task.started
task.progress
task.succeeded
task.failed
frontend.poll.succeeded
```

指标：

```text
api_request_latency_ms
task_duration_ms
task_progress_stage
task_failure_rate
frontend_poll_latency_ms
```

上下文要求：

```text
API 入口生成 request_id。
后台任务生成 task_id。
所有 service 调用继承 request_id/task_id/project_id。
前端可通过 task_id 查询阶段、进度、错误类型和建议动作。
```

## 15. 开发阶段计划

### E0：现状固化与基线

目标：明确当前系统能跑到哪里，避免加日志时破坏业务。

任务：

```text
E0.1 梳理 research_workspace 所有 service 的关键入口。
E0.2 记录当前 E2E smoke 覆盖链路。
E0.3 确认 QAResponse、RetrievalScope、Report、InnovationPoint 字段。
E0.4 建立 docs 中指标与代码模块的映射表。
```

验收：

```text
有模块入口清单。
有当前测试清单。
明确 P0 必接入的 service 和操作名。
```

### E1：logging_utils 基础能力

目标：统一结构化日志上下文。

任务：

```text
E1.1 新增 logging_utils.py。
E1.2 实现 LogContext、bind_context、get_context。
E1.3 实现 log_event，输出结构化字段。
E1.4 实现 log_operation，自动记录 started/succeeded/failed 和 latency_ms。
E1.5 增加基础单元测试。
```

验收：

```text
log_operation 成功时有 started/succeeded。
log_operation 异常时有 failed、error_type、latency_ms。
context 中的 request_id/task_id/project_id 能自动进入日志。
```

### E2：脱敏与安全日志

目标：避免日志泄露敏感数据。

任务：

```text
E2.1 实现 redact_text、hash_text、truncate_text。
E2.2 实现 sanitize_payload 的递归处理。
E2.3 加入 API key、Bearer、Authorization、Cookie、路径、长文本规则。
E2.4 新增 golden_redaction_cases.json。
E2.5 新增 test_logging_redaction.py。
```

验收：

```text
API key/token 不会出现在日志输出中。
prompt/raw_response 字段被拒绝或替换。
source_quote 被截断。
redaction_check 可检测未脱敏日志。
```

### E3：LLMService 指标接入

目标：让所有 LLM 调用可观测。

任务：

```text
E3.1 在 LLMService.invoke 中接入 log_operation。
E3.2 记录 provider、model、temperature、max_tokens。
E3.3 记录 input_chars、output_chars、latency_ms。
E3.4 尝试提取 token usage。
E3.5 invoke_json 记录 json_parse_failure。
E3.6 错误日志统一 error_type。
```

验收：

```text
LLM 成功和失败都有结构化事件。
JSON 解析失败不输出完整 response。
测试覆盖成功、异常、JSON 解析失败。
```

### E4：evaluation.py 核心评估

目标：实现 P0 质量检查纯函数。

任务：

```text
E4.1 实现 EvaluationResult。
E4.2 实现 scope_guard_check。
E4.3 实现 citation_coverage_check。
E4.4 实现 report_traceability_check。
E4.5 实现 section_source_coverage_check。
E4.6 实现 innovation_specificity_check。
E4.7 实现 generic_text_check。
E4.8 实现 redaction_check。
```

验收：

```text
Scope 外 paper_id 能被识别。
不存在的 evidence_id 能被识别。
正文无来源章节能被识别。
空泛创新点能被识别。
敏感日志能被识别。
```

### E5：Golden Fixtures 与 Loader

目标：固定可复盘测试样本。

任务：

```text
E5.1 创建 tests/fixtures/research_workspace。
E5.2 编写 mini_project、papers、chunks、cards、evidence、graph。
E5.3 编写 golden QA/RAG/review/innovation/redaction cases。
E5.4 实现 golden_loader.py。
E5.5 加 fixture schema 校验。
```

验收：

```text
fixture 可被 CLI 和 pytest 共同读取。
fixture ID 稳定，不依赖运行时随机 ID。
fixture 中引用关系完整。
```

### E6：quality_gates 与评估 CLI

目标：一键执行质量门禁。

任务：

```text
E6.1 实现 QualityGate 和 QualityGateResult。
E6.2 实现 default_research_workspace_gates。
E6.3 实现 scripts/evaluate_research_workspace.py。
E6.4 支持 --target qa/rag/report/innovation/logs/e2e/all。
E6.5 支持 --output-json 和 --write-report。
E6.6 质量门禁失败时 exit 1。
```

验收：

```text
本地命令能输出 JSON summary。
失败 case 有 case_id、metric、actual、expected。
required gate 失败时 CLI exit 1。
```

### E7：各业务模块日志接入

目标：关键链路具备 operation trace。

任务：

```text
E7.1 ProjectService 接入 project.create。
E7.2 PaperLibrary/Search 接入 paper.import/search.query。
E7.3 ParserService 接入 parser.parse/chunk.build。
E7.4 PaperCardGenerator 接入 paper_card.generate。
E7.5 EvidenceTableService 接入 evidence.build。
E7.6 GraphService 接入 graph.build/path_query。
E7.7 RetrievalScopeService 接入 scope.resolve。
E7.8 ScopeQAService 接入 qa.answer/retrieve/validate。
E7.9 ReviewGenerator 接入 review.generate/quality_check。
E7.10 InnovationGenerator 接入 innovation.generate/quality_check。
E7.11 ReportService 接入 report.save/export。
```

验收：

```text
E2E smoke 运行后能看到完整项目链路事件。
关键失败能定位到 service、operation、project_id。
```

### E8：API/task/front-end 联调指标

目标：前端能看到任务进度和错误阶段。

任务：

```text
E8.1 API middleware 生成 request_id。
E8.2 Task manager 生成 task_id。
E8.3 任务状态包含 stage、progress、error_type、suggested_action。
E8.4 前端轮询结果和后端 task log 对齐。
E8.5 demo pipeline 输出 evaluation summary。
```

验收：

```text
同一次前端操作的 request_id/task_id 能贯穿服务日志。
任务失败时前端能显示失败阶段。
```

### E9：P1/P2 扩展

任务：

```text
E9.1 RAG context precision/recall 扩展。
E9.2 搜索 dedup precision 和 provider SLA。
E9.3 parser section coverage 和 parse quality。
E9.4 token cost report。
E9.5 OpenTelemetry adapter。
E9.6 LangSmith 或同类 trace adapter。
E9.7 本地 dashboard 数据导出。
```

验收：

```text
release profile 可输出更完整质量报告。
外部 adapter 可开关，不影响本地测试。
```

## 16. 测试计划

### 16.1 新增测试文件

```text
tests/agents_v3/research_workspace/test_logging_utils.py
tests/agents_v3/research_workspace/test_logging_redaction.py
tests/agents_v3/research_workspace/test_llm_metrics.py
tests/agents_v3/research_workspace/test_evaluation.py
tests/agents_v3/research_workspace/test_quality_gates.py
tests/agents_v3/research_workspace/test_golden_loader.py
tests/agents_v3/research_workspace/test_evaluate_script.py
tests/agents_v3/research_workspace/test_e2e_quality_gate.py
```

### 16.2 必测场景

| 场景 | 预期 |
| --- | --- |
| `log_operation` 正常执行 | 有 started/succeeded，latency_ms 大于等于 0 |
| `log_operation` 抛异常 | 有 failed，包含 error_type，不吞异常 |
| `bind_context` 后记录事件 | 自动包含 request_id/task_id/project_id |
| 日志 payload 含 API key | 输出被脱敏，redaction_check 通过 |
| LLM JSON 解析失败 | 记录 json_parse_failure，不输出完整 response |
| QA 引用 scope 外 paper | scope_guard_check 失败 |
| QA 无证据但给确定答案 | citation_coverage_check 失败或 warning |
| 报告 evidence_id 不存在 | report_traceability_check 失败 |
| 报告主体章节无来源 | section_source_coverage_check 失败 |
| 创新点只有“优化模型” | innovation_specificity_check 失败 |
| CLI target=logs | 只跑日志脱敏 cases |
| CLI required gate 失败 | exit code 为 1 |
| E2E smoke summary 正常 | e2e_smoke_check 通过 |

### 16.3 测试数据原则

```text
测试不调用真实 LLM。
测试不访问网络。
测试不依赖系统时间，必要时 freeze timestamp。
fixture ID 固定。
失败断言要检查 failures 中的具体原因。
```

## 17. 实施细节建议

### 17.1 error_type 归一化

建议统一错误类型：

```text
ValidationError
NotFoundError
ScopeViolationError
LLMProviderError
LLMJsonParseError
RateLimitError
TimeoutError
StorageError
ExportError
UnknownError
```

### 17.2 latency 记录

使用 `time.perf_counter()`，不要用 wall clock 计算耗时：

```python
start = time.perf_counter()
try:
    ...
finally:
    latency_ms = int((time.perf_counter() - start) * 1000)
```

### 17.3 JSONL 写入

P0 可以先通过 Loguru sink 或简单 append 写 JSONL。若写入 metrics 文件：

```text
每行一个 JSON。
所有 payload 必须先 sanitize。
写入失败不能影响主业务流程，但应记录 warning。
```

### 17.4 评估函数设计原则

```text
优先纯函数，便于测试。
不在评估函数中调用真实 LLM。
不要让评估函数修改业务对象。
输出必须机器可读。
失败结果必须给 suggested_actions。
```

### 17.5 阈值配置

阈值放在 `quality_gates.py` 中，并支持 CLI profile 覆盖：

```text
fast: 只跑 P0 required gates。
full: 跑 P0 + P1 规则指标。
release: full + 更严格阈值 + 导出报告。
```

## 18. 风险与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| 指标过多导致落地慢 | 文档很好但代码推进慢 | P0 只实现 required gates |
| 日志接入污染业务代码 | service 中到处是日志字段 | 用 `log_operation` 降低侵入 |
| 脱敏误删有效调试信息 | 日志看不到问题 | 记录 hash、长度、prompt_name、error_type |
| 规则评估误判 | 好答案被判失败 | P0 对主观质量用 warning，硬门禁只管可追溯和越界 |
| fixture 过时 | 业务模型更新后测试失效 | golden_loader 做 schema 校验，fixture 随模型迁移 |
| 外部平台依赖不稳定 | CI 受网络和账号影响 | P0/P1 不依赖外部服务 |
| token usage 取不到 | 不同 provider 返回格式不同 | 允许为空，至少记录 chars 和 latency |

## 19. 里程碑排期

### 第 1 周：日志和脱敏基线

```text
完成 logging_utils.py。
完成 redaction 工具和测试。
接入 LLMService 基础指标。
```

### 第 2 周：评估核心

```text
完成 evaluation.py P0 函数。
完成 quality_gates.py。
完成基础 fixtures。
```

### 第 3 周：CLI 和业务接入

```text
完成 evaluate_research_workspace.py。
接入 ScopeQA、Review、Innovation、ReportService。
E2E smoke 后输出 evaluation summary。
```

### 第 4 周：联调和扩展

```text
接入 API/task trace。
补充搜索、解析、图谱指标。
完善 Markdown 质量报告。
准备 P1 RAG 指标。
```

## 20. 验收标准

### 20.1 P0 验收

必须满足：

```text
1. 新增 logging_utils.py、evaluation.py、quality_gates.py。
2. LLMService 成功、失败、JSON parse failure 都有结构化日志。
3. 日志脱敏测试覆盖 API key、Bearer token、prompt、raw_response。
4. scope_guard_check 能发现 scope 外 paper/evidence。
5. report_traceability_check 能发现不存在 evidence 和 scope 外引用。
6. section_source_coverage_check 能发现主体章节无来源。
7. innovation_specificity_check 能发现空泛创新点。
8. tests/fixtures/research_workspace 有可加载 golden cases。
9. scripts/evaluate_research_workspace.py 可运行并输出 JSON summary。
10. required quality gates 失败时 CLI exit 1。
```

### 20.2 P1 验收

```text
1. Search、Parser、PaperCard、Evidence、Graph 有核心指标。
2. RAG context precision/recall 可基于 golden cases 计算。
3. API request_id 和 task_id 能贯穿后台任务。
4. 评估报告可输出 Markdown。
5. token 成本和 provider 失败率可汇总。
```

### 20.3 P2 验收

```text
1. OpenTelemetry adapter 可选启用。
2. LLM trace adapter 可选启用。
3. release profile 可生成质量趋势数据。
4. 本地 dashboard 或 JSON 数据源可查看最近评估结果。
```

## 21. 推荐实施顺序

最短闭环顺序：

```text
1. logging_utils.py
2. redaction_check
3. LLMService metrics
4. scope_guard_check
5. report_traceability_check
6. innovation_specificity_check
7. golden fixtures
8. quality_gates.py
9. evaluate_research_workspace.py
10. ScopeQA/Review/Innovation 接入
```

不要先做外部监控平台。当前阶段最容易影响产品可信度的问题是：

```text
Scope 越界引用
报告没有来源
创新点空泛
LLM 失败不可见
日志泄露敏感信息
重构后没有 golden 回归
```

## 22. 参考文档

本模块应持续对齐以下本地文档：

```text
docs/research/14-细致开发计划/模块级开发计划/09-ScopeQA与RAG模块.md
docs/research/14-细致开发计划/模块级开发计划/10-综述生成模块.md
docs/research/14-细致开发计划/模块级开发计划/11-创新点报告模块.md
docs/research/14-细致开发计划/模块级开发计划/12-报告版本与导出模块.md
docs/research/14-细致开发计划/模块级开发计划/13-LLM提示词与结构化输出模块.md
docs/research/14-细致开发计划/模块级开发计划/14-API任务与前端联调模块.md
docs/research/08-日志与监控/日志系统调研报告.md
docs/research/10-知识图谱/RAG与知识图谱检索评估标准调研报告.md
```

外部方向仅作为 P1/P2 适配参考：

```text
OpenTelemetry: trace/logs/metrics 统一上下文
RAGAS: RAG 评估指标思想
LangSmith: LLM trace 和 prompt/version 观测
Evidently: 质量报告和指标快照
Loguru: 本地结构化日志输出
```

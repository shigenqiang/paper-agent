# 模块总览与依赖关系

更新时间：2026-05-28

本目录是 `src/agents_v3/research_workspace` 的模块级专项开发计划集合。目标是把论文知识库分析 Agent 从“已有 service 雏形”推进到“可运行、可追溯、可测试、可前端联调”的产品级 MVP。

## 1. 总体主链路

```text
Project
  -> Paper Library / Search / Import / Upload
  -> PDF Parser / Chunk
  -> Paper Card
  -> Evidence Table
  -> Knowledge Graph
  -> Retrieval Scope
  -> Scope QA / RAG
  -> Literature Review
  -> Innovation Report
  -> Report Version / Export
```

支撑链路：

```text
Models / Storage
LLM Service / Prompt Schema
API / Task Progress
Evaluation / Logging / Monitoring
Frontend Integration
```

## 2. 已深化模块清单

| 顺序 | 文件 | 模块定位 | 深化重点 |
| --- | --- | --- | --- |
| 00 | `00-模块总览与依赖关系.md` | 全局依赖 | 主链路、依赖顺序、验收门禁 |
| 01 | `01-核心模型与存储模块.md` | 数据契约 | Pydantic 模型、JSONStorage、安全写入、迁移 |
| 02 | `02-项目服务模块.md` | 项目入口 | 项目状态、配置、统计、健康检查、演示项目 |
| 03 | `03-论文库搜索导入模块.md` | 搜索入库 | OpenAlex/arXiv/CrossRef、多源融合、限流、缓存、去重 |
| 04 | `04-PDF解析与分块模块.md` | 来源地基 | 多解析器、章节、页码、chunk、参考文献分离 |
| 05 | `05-论文卡片生成模块.md` | 单篇论文结构化 | 章节上下文选择、schema 输出、source span、质量分 |
| 06 | `06-证据表模块.md` | 项目事实层 | evidence schema、来源校验、审核流、导出、证据强度 |
| 07 | `07-知识图谱模块.md` | 关系组织 | property graph、稳定 ID、Gap、GraphRAG、traceability |
| 08 | `08-RetrievalScope模块.md` | 范围边界 | Scope 解析、组合过滤、ScopeGuard、空范围解释 |
| 09 | `09-ScopeQA与RAG模块.md` | 问答入口 | 证据检索、拒答、引用校验、RAG 评估 |
| 10 | `10-综述生成模块.md` | 综述成果 | Evidence Matrix、分批综合、章节来源、质量校验 |
| 11 | `11-创新点报告模块.md` | 创新方向 | GapSignal、评分、项目匹配、反证/限制证据 |
| 12 | `12-报告版本与导出模块.md` | 成果管理 | 版本、Markdown/JSON 导出、来源索引、traceability |
| 13 | `13-LLM提示词与结构化输出模块.md` | 模型调用 | structured output、repair、prompt version、日志脱敏 |
| 14 | `14-API任务与前端联调模块.md` | 产品联调 | FastAPI、TaskService、SSE/轮询、前端四页契约 |
| 15 | `15-评估日志监控模块.md` | 质量系统 | 结构化日志、golden cases、RAG/report/innovation 门禁 |

## 3. 当前代码映射

| 产品模块 | 当前代码 | 当前状态 |
| --- | --- | --- |
| 数据模型 | `models.py` | 已有核心 Pydantic 模型，需要扩展任务、证据、Scope、评估字段 |
| 存储 | `storage.py` | 已有 JSONStorage，需要原子写、显式 collection 契约和迁移 |
| 项目管理 | `project_service.py` | 已有 CRUD 和统计，需要状态、配置、健康检查、级联删除 |
| 论文库搜索 | `paper_library.py`、`search/` | v3 已有多源搜索雏形，需产品化 search session 和 commit |
| PDF 解析 | `parser_service.py` | 已有 pdfplumber + 章节分块，需要模型字段、参考文献分离和质量报告 |
| 论文卡片 | `paper_card.py` | 已有 LLM + fallback，需要 schema、source spans、幂等和质量分 |
| 证据表 | `evidence_table.py` | 已有卡片转证据，需要 evidence_type、claim、review、traceability |
| 知识图谱 | `graph_service.py` | 已有轻量图，需要稳定 ID、来源属性和 GraphRAG |
| Scope | `scope.py` | 已有范围解析，需要 ScopeGuard、组合过滤、空范围解释 |
| QA | `scope_qa.py` | 已有问答生成，需要检索排序、拒答、引用校验 |
| 综述 | `review_generator.py` | 已有生成和 fallback，需要 Evidence Matrix、章节来源和质量门禁 |
| 创新点 | `innovation_generator.py` | 已有 gap/limitation 信号，需要证据强约束和评分体系 |
| 报告 | `report_service.py` | 已有保存、版本、Markdown 导出，需要来源索引和 traceability |
| LLM | `llm_service.py` | 已有 invoke/invoke_json，需要结构化输出、repair、metrics、脱敏 |
| API/任务 | 待新增 | 需要 FastAPI router、TaskService、前端契约 |
| 评估监控 | 待新增 | 需要 evaluation.py、logging_utils.py、golden fixtures |

## 4. 依赖顺序

推荐开发顺序：

```text
1. Models + Storage
2. ProjectService
3. PaperLibrary + Search
4. ParserService
5. PaperCardGenerator
6. EvidenceTableService
7. GraphService
8. RetrievalScopeService
9. ScopeQAService
10. LiteratureReviewGenerator
11. InnovationReportGenerator
12. ReportService
13. LLM / Prompt / Schema hardening
14. API + Task + Frontend
15. Evaluation + Logging + Monitoring
```

关键依赖：

```text
PaperCard 依赖 PaperChunk。
EvidenceRecord 依赖 PaperCard 和 SourceSpan。
KnowledgeGraph 依赖 EvidenceRecord。
RetrievalScope 依赖 Paper、EvidenceRecord 和 KnowledgeGraph。
ScopeQA、Review、Innovation 都依赖 resolved scope。
Report 必须记录 scope、paper_ids、evidence_ids、graph_node_ids。
Evaluation 必须覆盖 QA、Review、Innovation 和日志脱敏。
```

## 5. 主流做法落地映射

| 主流做法 | 本项目落地 |
| --- | --- |
| 系统综述工具的 extraction form | EvidenceTableSchema、EvidenceColumn、review_status |
| Elicit/Consensus 的结构化论文快照 | PaperCard + source spans + quality score |
| GraphRAG 的实体图谱和 local/global search | GraphService + related_evidence_ids + graph summaries |
| RAG 的 hybrid retrieval 和 faithfulness 评估 | EvidenceRetriever + ScopeGuard + golden QA |
| PRISMA/Cochrane/GRADE 的可追溯综述 | Evidence Matrix + section_sources + certainty 字段 |
| FastAPI/OpenAPI 的契约化 API | `/api/rw` router + Pydantic request/response |
| OpenTelemetry/LangSmith/Ragas 的评估监控 | structured logs + LLM metrics + evaluation.py |

## 6. 全局质量门禁

模块实现完成后，必须能证明：

```text
1. 从创建项目到报告导出全链路可运行。
2. 所有正式回答和报告都能追溯到 paper、evidence 或 graph node。
3. Scope 外内容不会进入 QA、综述或创新点报告。
4. unknown、伪证据、无来源强结论不会污染 evidence table。
5. PDF 解析失败、LLM 失败、搜索失败都有状态和日志。
6. 每个核心模块都有单元测试。
7. 端到端 smoke test 能稳定复现演示项目。
8. 日志不泄露 API key、完整 prompt、完整 PDF 文本。
9. 报告导出包含范围、论文、证据和版本信息。
10. 创新点必须有 gap、证据链、风险和可行性评分。
```

## 7. 统一测试矩阵

| 层级 | 测试类型 |
| --- | --- |
| 模型层 | model_validate/model_dump、默认值、旧数据兼容 |
| 存储层 | atomic write、collection id、query、migration |
| 搜索层 | adapter mock、限流、缓存、去重、融合排序 |
| 解析层 | 状态流、chunk metadata、references split、失败场景 |
| 抽取层 | schema 校验、source span、fallback、质量分 |
| 证据层 | evidence_type、traceability、review、export |
| 图谱层 | stable ids、source properties、subgraph、gap |
| Scope 层 | project boundary、empty scope、guard、组合过滤 |
| QA/RAG | retrieval ranking、refusal、citation validation |
| 报告层 | section_sources、traceability、export、version |
| 工程层 | API contract、task progress、logging redaction、evaluate script |

## 8. 参考依据

本目录综合参考：

```text
docs/research/05-学术搜索与解析/
docs/research/10-学术QA系统/
docs/research/10-知识图谱/
docs/research/11-产品方案与开发/
docs/research/12-优化方案/
docs/research/13-综合调研/
docs/research/14-细致开发计划/
docs/development/
docs/getting-started/frontend-overview.md
```

# Agent记忆系统与数据持久化融合方案

> 更新时间: 2026-05-02 | 版本: v3.0 (精简版)

---

## 一、架构概述

### 1.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                    UnifiedMemoryManager                       │
├──────────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐ │
│  │ShortTerm│ │ Session │ │LongTerm │ │Episodic │ │UserProf│ │
│  │(内存)  │ │(PG JSONB)│ │(Qdrant) │ │(PG)    │ │(PG)    │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └────────┘ │
├──────────────────────────────────────────────────────────────┤
│  服务层: MemoryExtractor / RetrievalEngine / ForgettingCtrl │
├──────────────────────────────────────────────────────────────┤
│  存储层: PostgreSQL (sessions/profiles) + Qdrant (向量) + Neo4j(图)│
└──────────────────────────────────────────────────────────────┘
```

### 1.2 各层职责

| 记忆层 | 存储 | 淘汰策略 |
|-------|------|---------|
| **ShortTerm** | 内存 | LRU + TTL(1h) |
| **Session** | PostgreSQL JSONB分区 | TTL(30天) |
| **LongTerm** | Qdrant + Neo4j | 重要性衰减 |
| **UserProfile** | PostgreSQL | 置信度衰减 |

---

## 二、存储设计

### 2.1 PostgreSQL 表

```sql
-- 会话表 (分区TTL)
CREATE TABLE sessions (
    id UUID PRIMARY KEY,
    task_id VARCHAR(255) NOT NULL,
    messages JSONB DEFAULT '[]',
    summary TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP GENERATED ALWAYS AS (created_at + INTERVAL '30 days') STORED
) PARTITION BY RANGE (created_at);

-- 实体索引表
CREATE TABLE memory_index (
    id SERIAL PRIMARY KEY,
    memory_id VARCHAR(255) UNIQUE NOT NULL,
    memory_type VARCHAR(50),
    importance FLOAT DEFAULT 0.5,
    tags TEXT[],
    qdrant_id VARCHAR(255),
    neo4j_entity_id VARCHAR(255)
);
```

### 2.2 Neo4j 图结构

```cypher
// 节点: Agent, Task, Memory, Concept, Paper
// 关系: EXECUTED, HAS_MEMORY, TAGGED_AS, PREFERS, CITES
```

---

## 三、重要性等级与保留策略（核心）

### 3.1 五级重要性体系

| 等级 | 分数 | 语义 | S参数（秒）| 保留时间 |
|------|------|------|-----------|---------|
| **CRITICAL** | 1.0 | 永不遗忘 | ∞ | 永不 |
| **HIGH** | 0.8 | 长期保留 | 604,800 (7天) | 7天 |
| **MEDIUM** | 0.5 | 标准衰减 | 86,400 (1天) | 1天 |
| **LOW** | 0.3 | 快速遗忘 | 3,600 (1小时) | 1小时 |
| **FORGOTTEN** | <0.1 | 已删除 | <3,600 | - |

### 3.2 保留分数计算公式

**核心公式**: `retention = importance × e^(-t/S)`

- `importance`: 初始重要性 (0.0-1.0)
- `t`: 距上次访问的时间（秒）
- `S`: 记忆强度参数（基于等级）

```python
def retention_score(self) -> float:
    t = time.time() - self.last_accessed
    S = self.get_strength_parameter()  # CRITICAL=∞, HIGH=7天, MEDIUM=1天, LOW=1小时
    if S == float('inf'):
        return 1.0
    return self.importance * math.exp(-t / S)

def should_forget(self, threshold: float = 0.1) -> bool:
    return self.retention_score() < threshold
```

### 3.3 重要性评分算法

**公式**: `score = relevance×0.3 + explicit×0.4 + recency×0.2 + access_boost×0.1`

其中 `access_boost = min(access_count / 10, 1.0)`

### 3.4 S参数衰减曲线

```
保留率
100% ─────────────────────────────────────────── CRITICAL (∞)
 70% │───────── HIGH (7天半衰)
     │
 50% │────────────────── MEDIUM (1天半衰)
     │
 37% ───────────────────────────────────────────
     │──── LOW (1小时半衰)
  0% └──────────────────────────────────────────► 时间
    0    1h    1d    7d    30d
```

### 3.5 等级动态调整

**升级**: 7天内访问>10次 → HIGH；用户明确标记 → CRITICAL
**降级**: 30天未访问 → MEDIUM；过时信息 → LOW

---

## 四、记忆流转机制（核心）

### 4.1 流转触发条件

| 阶段 | 触发条件 | 操作 |
|------|---------|------|
| STG→SESSION | 消息数≥30 OR 任务结束 | 批量存储到PG |
| STG→LONG_TERM | 重要性≥0.7 OR 用户标记 | 存入Qdrant+Neo4j |
| LONG_TERM→FORGOTTEN | retention<0.1 | 删除 |
| 任意→COMPRESS | token超限 | LLM生成摘要 |

### 4.2 双向流转图

```
                    新记忆                              召回路径
                      │                                  │
                      ▼                                  │
┌──────────┐    [消息≥30]    ┌──────────┐                │
│ ShortTerm│ ────────────► │ Session  │                │
│ (内存)   │                │ (PG JSONB)│                │
└────┬─────┘                └────┬─────┘                │
     │                           │                        │
     │[重要性≥0.7]              │                        │
     ▼                           ▼                        │
┌──────────┐               ┌──────────┐                 │
│ LongTerm │               │ LongTerm │                 │
│ (Qdrant) │               │ (Neo4j)  │                 │
└────┬─────┘               └──────────┘                 │
     │                                                  │
     │[召回触发]                                        │
     │  - 短期未命中                                    │
     │  - 频繁访问(7天≥5次)                             │
     │  - 高相关性(>0.8)                                │
     │  - Top3检索结果                                  │
     └──────────────────────────────────────────────► ┌──────────┐
                                                       │ ShortTerm│
                                                       │ (缓存)   │
                                                       └──────────┘
```

### 4.3 召回算法

```python
async def recall_direct(self, key: str):
    # 1. 查短期记忆
    value = await self.short_term.get(key)
    if value: return value
    # 2. 查长期记忆并缓存
    value = await self.long_term.recall(key)
    if value:
        await self.short_term.add(key, value)  # 层级提升
    return value
```

### 4.4 SM-2间隔复习

公式: `I(1)=1, I(2)=6, I(n)=I(n-1)×EF (n>2)`
EF更新: `EF' = EF + 0.1 - (5-q)×(0.08 + (5-q)×0.02)`

---

## 五、存储配置

### 5.1 Docker Compose

```yaml
services:
  postgres:
    image: postgres:17-alpine
    ports: ["5432:5432"]
  qdrant:
    image: qdrant/qdrant:latest
    ports: ["6333:6333", "6334:6334"]
  neo4j:
    image: neo4j:5.18-community
    ports: ["7474:7474", "7687:7687"]
```

### 5.2 改造对应

| 现有模块 | 目标存储 | 改造 |
|---------|---------|------|
| `memory/unified.py` | 统一入口 | 集成Qdrant/Neo4j |
| `memory/long_term.py` VectorStore | Qdrant | TF-IDF→HNSW |
| `memory/long_term.py` GraphStore | Neo4j | JSON→Cypher |
| `memory/postgres_storage.py` | PostgreSQL | 扩展JSONB |

---

## 六、升级路径

| 阶段 | 周期 | 内容 |
|------|------|------|
| 一 | 1-2周 | PostgreSQL JSONB + 分区表 + 流转机制 |
| 二 | 2-3周 | Qdrant向量存储 + HNSW索引 |
| 三 | 3-4周 | Neo4j图数据库 + 实体提取 |

---

**版本**: v3.0 (精简版)

**搜索记录**: 25次搜索 (详见原始版本 v2.1)
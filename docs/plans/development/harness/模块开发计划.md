# Harness 模块开发计划

> 规划日期：2026-05-02 | 版本：v1.0

---

## 模块概述

Harness是质量保障体系的核心，包含5大组件。

### 核心组件

```
Harness = ⟨Evaluator, CheckpointManager, CircuitBreaker, HITLManager, AuditTrail⟩
```

| 组件 | 说明 |
|------|------|
| **Evaluator** | 质量评估器，5维度评分 |
| **CheckpointManager** | 检查点管理器，支持暂停/恢复/时间旅行 |
| **CircuitBreaker** | 熔断器，防止级联故障 |
| **HITLManager** | 人机协作管理器 |
| **AuditTrail** | 审计溯源，记录完整操作 |

### CircuitBreaker 配置

```python
thresholds = {
    "llm_error_rate": 0.3,       # LLM调用错误率 >30% → 熔断
    "consecutive_failures": 5,   # 连续失败5次 → 熔断
    "timeout_seconds": 300,      # 单步超过5分钟 → 熔断
    "cost_limit": 5.0,          # 单次会话超过$5 → 熔断
}
states = ["CLOSED", "OPEN", "HALF_OPEN"]
```

### HITL 介入点

| 介入点 | 说明 |
|--------|------|
| `after_outline` | 大纲完成后需人工确认 |
| `after_literature` | 文献综述完成后需审核 |
| `after_section` | 每章节完成后可选审核 |
| `before_final` | 终稿前需全面审核 |
| `on_low_quality` | 质量评分<阈值时强制中断 |

---

## 核心文件

```
src/agents_v2/harness/
├── __init__.py
├── evaluator.py
├── checkpoint_manager.py
├── circuit_breaker.py
├── hitl_manager.py
└── audit_trail.py
```

---

**版本**：v1.0
**规划日期**：2026-05-02
